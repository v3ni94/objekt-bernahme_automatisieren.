# Stand der Umsetzung

Fortschreibung nach jedem Arbeitsschritt (Umsetzungsplan, Abschnitt 7). Neueste Einträge oben.

| Datum | Meilenstein | Schritt | Ergebnis | Tests |
|---|---|---|---|---|
| 15.09.2026 | Betrieb | Worker laufen seit etwa 18:25 UTC wieder, nicht über den Workflow gestartet; Datenbank-Container um etwa 19:02 UTC neu erstellt; Last 19:08 UTC Load 9,6, steal 83 bis 85 Prozent | Messung `last-status` 19:08 UTC (Lauf 239): Load 9,55 / 9,11 / 7,89, vmstat us 3 / sy 1 / idle 10 bis 39 / **steal 57 bis 85 Prozent**, top us 3,3 / idle 12,1 / **steal 83,0 Prozent**, PSI some avg10 86 Prozent (avg300 85), 18 bis 37 laufbereite Prozesse, 0 Zombies, 2 angemeldete Benutzer. `ps` (Lauf 240, 19:10 UTC): worker, worker-io, worker-nlp und beat „Up 45 minutes“, db „Created 8 minutes ago“ (neu erstellt, nicht nur neu gestartet), web, redis, backup „Up 11 hours“. Zwischen Lauf 238 (17:43 UTC) und 239 (19:07 UTC) gab es keinen Workflow-Lauf; Start und Neuerstellung erfolgten direkt auf dem Server (SSH oder Container-Oberfläche), nicht durch die Betreuung. CPU-Grenzen laut inspect weiterhin gesetzt: worker 1,0, worker-io 0,5, worker-nlp 0,5, web 1,0, db 1,0 Kerne. Top-Prozesse über 3 s: unsere MariaDB 95 Prozent, unser Python 81, Paperless-Webserver 69, Paperless-Celery 65, next-server 54, containerd 46, dockerd 40. Image unverändert ce60b6b. Bewertung: Mit steal über 80 Prozent und CPU-Druck 86 Prozent ist der Server für alle Anwendungen erneut zäh; Empfehlung an die Geschäftsführung, die Worker bis zur Klärung mit dem Hoster wieder anzuhalten (`worker-stop`), Entscheidung liegt beim Auftraggeber, da der Start von dort kam. |
| 15.09.2026 | Betrieb | Zwischenstand 17:47 UTC: 46,93 Prozent fertig, Verarbeitung weiter gestoppt, steal unverändert | Abfrage `doc-status` (Lauf 237): 37.974 Dokumente, davon fertig 17.820 = 46,93 Prozent (filed 1.324, review 13.595, duplicate 2.420, classified 481), offen 20.154 (registered 13.927, hashed 6.089, error 138). Am 14.09. 21:58 UTC waren es 32,14 Prozent; der Zuwachs von 14,8 Punkten entstand zwischen 21:58 und dem Stopp um 12:08:49 UTC, die letzten dreieinhalb Stunden davon unter dem CPU-Entzug des Wirts. Offene Jobs: discover 13.542, ocr_chunk 4.391 (5 als laufend markiert, Worker stehen), merge_pages 2.736, file_to_drive 1.739 (1.248 warten auf die Drive-Schreibsperre des Objekts, 87 auf einen nicht registrierten Hauptordner der Eigentümerakte), render_previews 1.693, hash 139. Läufe: 8 laufend (Objekte 445, 457, 462, 467, 469, 470, 472, 477), 55 wartend, 14 fertig, 16 fehlgeschlagen. Neue Fehlerklasse: 39 classify-Jobs mit `TypeError: '<' not supported between instances of 'NoneType' and 'datetime.date'` (Datumsvergleich ohne Datum), Korrektur offen, wird mit dem nächsten Image ausgeliefert. Fehlerdokumente 138 (vorher 69). Lastmessung `last-status` 17:47 UTC (Lauf 238): Load 1,19 / 2,12 / 1,96, us 2 bis 3 / idle 28 bis 45 / **steal 52 bis 68 Prozent**, PSI some avg10 24 Prozent, unsere Container weiter ohne Last; Zombies unverändert 332, also seit dem Stopp keine neuen Healthcheck-Abbrüche. Der Hoster-Befund von 12:55 UTC besteht fort. |
| 15.09.2026 | Betrieb | Volllast trotz gestoppter Verarbeitung: Hypervisor entzieht dem VPS die CPU (steal) | Meldung des Auftraggebers 12:50 UTC, CPU weiter maximal, obwohl unsere Worker seit 12:08:49 UTC stehen. Messung `last-status` 12:53 UTC: Load 1,37 / 1,75 / 2,00 auf 8 Kernen, unsere Container web 0,06 Prozent, db 0,03 Prozent, redis 0,45 Prozent. Messung erweitert (nproc, vmstat mit Spalte st, PSI, top-Zweitbild, Commit f021a26) und 12:55 UTC wiederholt: vmstat us 3 / sy 1 bis 2 / idle 18 bis 34 / **steal 62 bis 78 Prozent**, top 12:56 UTC us 1,9 / sy 1,5 / idle 43,9 / **steal 52,5 Prozent**, PSI some avg10 22 Prozent, 15 bis 5 laufbereite Prozesse, 16,5 GB Speicher frei, kein Swap. Befund: Der Server ist von innen nahezu leer, mehr als die Hälfte der CPU-Zeit wird vom Wirt nicht zugeteilt. Ursache liegt beim Hoster (Drosselung nach Dauerlast oder überbuchter Wirt), nicht in unserer Anwendung. Verbleibende eigene Last des Hosts: Paperless-OCR (ein tesseract-Prozess 100 Prozent, Paperless verarbeitet eigene Eingänge), zwei postgres-Prozesse 69 und 39 Prozent, Paperless-Webserver 13 Prozent. Dritte Messung 12:59 bis 13:00 UTC nach Skript-Pull (a48e5ed): vmstat seit Neustart des Hosts us 3 / sy 1 / idle 19 / wa 1 / **steal 76 Prozent** (erste Zeile, Mittel seit 08:38 UTC, also auch über die Zeit mit laufenden Workern), aktuell steal 28 und 59 Prozent, top steal 36,3 Prozent bei idle 60,2, Load 2,7 bis 2,9, PSI some avg10 4,5 Prozent; unsere Container weiter ohne Last (web 0,06, db 0,03, redis 0,57 Prozent). Nebenbefund: 332 Zombie-Prozesse auf dem Host, alle unter mariadbd (PID 1) im Container objektakte-db-1; Erklärung: der Healthcheck `healthcheck.sh` (alle 10 s, Timeout 5 s) läuft bei CPU-Knappheit in den Timeout, seine Kindprozesse fallen an PID 1, mariadbd holt sie nicht ab. Zombies kosten keine CPU. Abhilfe vorbereitet: `init: true` für den Dienst db in docker-compose.yml, wirksam beim nächsten `docker compose up -d` (kurze Datenbankunterbrechung, daher nicht sofort angewendet). Aktion `last-status` brach mit Exit 141 (SIGPIPE durch head hinter top) ab, Begrenzung nach awk verlegt. Empfehlung: Ticket beim Hoster mit den Messwerten (steal), Nachfrage zu Drosselung und Rücksetzzeitpunkt; Verarbeitung bleibt bis zur Klärung gestoppt, ein Wiederanlauf würde den ohnehin verknappten Kernen weitere Last hinzufügen. |
| 15.09.2026 | Betrieb | Verarbeitung angehalten (Anweisung Geschäftsführung, Server weiter auf Volllast) | Trotz Grenzen (2 Kerne zu 50 Prozent) meldete der Auftraggeber weiter Volllast. Neue Aktionen `worker-stop` und `worker-start` (`docker compose stop` und `start` für worker, worker-nlp, worker-io, beat; web, db, redis laufen weiter, Jobs bleiben erhalten). 12:08:49 UTC gestoppt. Messung 12:09 UTC: Load 4,4 / 6,0 / 6,3 statt 11,5 / 13,8 / 22,4 um 11:08 UTC; verbleibende Verbraucher des Hosts: eine MariaDB 93 Prozent, node 52 (n8n oder Homarr), Paperless-Celery 52, Homarr und Paperless-Webserver je 25. Web-Protokoll ohne Webhook-Flut, nur Bereitschaftsprüfungen. Befund zur Serverhistorie: Host um etwa 08:38 UTC neu gestartet (Betriebszeit 3:31 um 12:09 UTC, davor 6 Tage), unsere Worker kamen danach mit der alten Konfiguration (7 OCR-Prozesse) automatisch hoch. Wiederanlauf erst nach Freigabe mit `worker-start` (Grenzen 1,0 / 0,5 / 0,5 und niedrige Priorität bleiben) | |
| 15.09.2026 | Betrieb | Drosselung auf 2 Kerne zu 50 Prozent mit niedriger Priorität (Anweisung Geschäftsführung) | Die übrigen Anwendungen des Servers reagierten nicht mehr; der Smoke-Test 10:36 UTC zeigte auch die eigene Weboberfläche ohne Antwort (Traefik 404, Bereitschaftsprüfung im Timeout). Deployment 212 (Image-Bau, 09:57 bis 10:36 UTC ohne Ende) abgebrochen, die laufenden Container blieben unverändert. Neue Deploy-Aktion `worker-drosseln Kerne[+Anteil]`: setzt ohne Neustart die Zeitquote (`docker update --cpus`) und niedrige Priorität (`--cpu-shares 128`) der drei Worker-Container und versucht die Celery-Poolgröße anzupassen (`pool_shrink` wurde zweimal abgelehnt, alle Prozesse beschäftigt, für die CPU-Last unerheblich). 10:43 UTC: OCR-Worker 3 Kerne, Ablage 1; 10:58 UTC: OCR-Worker 2 Kerne zu 50 Prozent (Quote 1,0), Ablage 0,5, Klassifikation 0,5, alle mit niedriger Priorität; parallele Objekte 3, dann 2; `.env` dauerhaft auf 2 OCR-Prozesse, 4 Ablage-Threads, 1 Klassifikationsprozess mit denselben Quoten (Vorlage gleich). Weitere lesende Aktionen `last-status` (Load, CPU je Container, Grenzen) und `work-bereinigen` (Arbeitsverzeichnisse in einem Durchgang, Vorschau und `echt`). Durchsatz damit grob ein Achtel der Stufe 2; Anhebung außerhalb der Arbeitszeit mit derselben Aktion möglich. Messung `last-status` 11:08 UTC: Load 11,5 / 13,8 / 22,4 (1, 5, 15 Minuten), Grenzen bestätigt (worker 1,0, worker-io 0,5, worker-nlp 0,5, web 1,0, db 1,0); größte Verbraucher des Hosts n8n (node und wget rund 220 Prozent), mariadbd 92, tesseract 83, Paperless granian 37, Homarr 25. `work-bereinigen echt` (Lauf 226, 11:09 bis 11:28 UTC): 12.323 Arbeitsverzeichnisse gelöscht, 8.848 behalten (Dokument `hashed` oder offener Job, überwiegend wartende Ablage), frei vorher 43,0 GB, nachher 74,7 GB. Smoke 11:29 UTC: Anwendung über Traefik wieder erreichbar (HTTPS 200, Zertifikat gültig), Bereitschaftsprüfung im Container weiter im Timeout. Transit-Bereinigung (13 GB) folgt mit dem nächsten Deployment (Kommando erst im neuen Image), empfohlen außerhalb der Arbeitszeit | |
| 15.09.2026 | Betrieb | Last zurückgenommen (Anweisung Geschäftsführung) | Der Server ist mit Paperless, n8n, Kimai, Outline und weiteren Anwendungen geteilt; Load 20 bis 25 auf 8 Kernen mit Stufe 2, CPU-Last zu hoch. Zurückgenommen auf 4 OCR-Prozesse (`WORKER_CPUS` 4), 8 Ablage-Threads mit 1 CPU, 4 parallele Objekte. Sofort wirksam über die neue Aktion `worker-drosseln` (Celery `pool_shrink` auf dem laufenden Worker, kein Neustart) und `config-set processing.max_parallel_objects=4`; dauerhaft über `env-set` (wirksam mit dem nächsten `deploy`). Vorlage `deploy/env.produktion` entsprechend. Folge: Durchsatz sinkt etwa auf die Hälfte der Stufe 2, die Fertigstellung verschiebt sich entsprechend | |
| 15.09.2026 | Betrieb | Platte des VPS läuft voll: Befund, Aufräumfrist, Transit-Kopien, Reserve | Meldung des Auftraggebers. Befund (`befund`, neue Aktion `disk-status`, 08:05 bis 09:53 UTC, die Messung selbst dauerte wegen der I/O-Last fast zwei Stunden): Platte 387 GB, belegt 342 GB, frei 45 GB (89 Prozent). Anteil der Anwendung rund 69 GB: `work/` 48 GB (21.192 Arbeitsverzeichnisse, 21.165 älter als 6 Stunden, 235 älter als 48 Stunden), `transit/` 13 GB (10.516 Dateien, Downloads der Paperless-Übernahme), `previews/` 6,2 GB (9.507 Dokumente, 60.242 Seitenbilder), `ocr-cache/` 1,8 GB. Außerhalb der Anwendung: Paperless-Medien 170 GB, n8n 11,7 GB, Docker-Images 48,6 GB (10,1 GB freigebbar), Container 16 GB, Build-Cache 4 GB. Ursachen im Quelltext: `work/` wurde erst 48 Stunden nach Abschluss geräumt (`processing.work_orphan_hours`), die Transit-Kopie wurde nach der Ablage nur für Uploads gelöscht (nicht für Paperless-Downloads), eine volle Platte (`DISK_RESERVE_GB` 18) ließ Hash-Jobs als Fehler enden. Maßnahmen: `processing.work_orphan_hours` auf 1 gesetzt (Lauf 210, ohne Neustart; der Sweeper räumt minütlich Verzeichnisse ohne offenen Job, deren Dokument nicht mehr `hashed` ist), `_remove_transit_copy` löscht die Kopie auch für Paperless nach der Ablage, `DiskFull` wird zum Wartegrund (30 Minuten, `DeferJob`), Kommando `transit_bereinigen` und Aktion `transit-bereinigen` (Vorschau, `echt`) für die liegen gebliebenen Kopien abgelegter Dokumente, Aktion `disk-status` misst aus den Containern (Datenverzeichnisse gehören uid 10001). Deployment und Nachmessung folgen | `test_verarbeitung_alle.py` (+2) |
| 14.09.2026 | M5 | Lauf endet, sobald nur noch Ablagejobs offen sind (Freigabe Geschäftsführung) | Nachmessung 19:40 UTC (`doc-status`, Lauf 201): 37.012 Dokumente, abgeschlossen 9.971 = 26,94 Prozent, seit 16:15 UTC rund 630 je Stunde statt 860. Befund: OCR-Warteschlange leer, alle acht laufenden Objekte nur noch in der Ablage nach Drive (6.418 wartende Ablagejobs, 1.679 an der Schreibsperre ihres Objekts, Ablage je Objekt seriell); Objekt 82 hielt seit 05:37 UTC einen Platz mit fünf offenen Ablagejobs, 68 Objekte warteten, die sieben OCR-Prozesse standen still. Änderung: `maybe_finish_run` schließt einen Lauf ab, sobald außer Ablagejobs kein Job des Objekts offen ist (bisher nur Ablagejobs ohne Objektordner); die Ablage läuft unabhängig weiter, der Platz wird frei, `schedule_runs` startet das nächste Objekt. Weil das Nachräumen beim Laufabschluss Objekte mit offenen Jobs überspringt, stößt die letzte erledigte Ablage eines abgeschlossenen Laufs das Nachräumen erneut an (`jobs._after_filing`, Auslöser `filing`). Architektur 7.5a ergänzt. Deployt mit Lauf 202 (21:09 UTC). Nachmessung 21:10 UTC (Lauf 203): fünf Läufe in der Ablagephase abgeschlossen (Objekte 133, 82, 342, 216, 359), fünf neue gestartet (411, 437, 444, 445, 457), OCR-Warteschlange wieder gefüllt (hash 7 laufend, 197 wartend); 37.837 Dokumente, abgeschlossen 11.153 = 29,48 Prozent; 5.371 Ablagejobs laufen unabhängig weiter (5.314 an der Schreibsperre ihres Objekts). Läufe mit fehlgeschlagenen Jobs (defekte Dateien) enden als `failed`, die Dokumente stehen als Fall `job_failed` im Review Center | `test_verarbeitung_alle.py` (Test zur wartenden Ablage erweitert), Testsuiten pipeline, sync, drive grün |
| 14.09.2026 | Betrieb | Kapazität Stufe 2 innerhalb der 8 Kerne (Freigabe Geschäftsführung) | Nachfrage nach mehr Leistung; der Server hat 8 Kerne und 31,3 GiB, mehr Hardware steht nicht zur Verfügung. Stand 13:53 UTC (`doc-status`, Lauf 189): Dokumente aktiver Objekte 35.218, abgeschlossen (abgelegt, Prüfung, Dublette, klassifiziert) 5.846 = 16,60 Prozent, offen 29.372 (registriert 24.890, gehasht 4.447, Fehler 35); Objekt 82 fertig, 133, 203, 216, 342 und 343 in Arbeit, 70 Objekte mit Dokumenten noch unberührt (größte 503 mit 4.307, 467 mit 4.119, 469 mit 3.888); seit 07:01 UTC 5.380 Dokumente in 6,9 Stunden, rund 780 je Stunde, linear rund 38 Stunden Rest. Umgesetzt über `env-set` (Lauf 192) und `deploy` (Lauf 193): `OCR_PROCESSES` 6 auf 7, `WORKER_CPUS` 7, `WORKER_MEM` 6144m, `IO_CONCURRENCY` 12 auf 16, `WORKER_IO_CPUS` 2.0, `WORKER_IO_MEM` 1536m; `processing.max_parallel_objects` 6 auf 8 (Lauf 191); Vorlage `deploy/env.produktion` gleich. `drive.max_requests_per_second` wurde auf 8 gesetzt (Lauf 190) und auf 5 zurückgenommen: der Token-Bucket entsteht je Adapteraufruf (`get_adapter` je Aufgabe) und begrenzt nur Bursts innerhalb einer Aufgabe, nicht die Summe über Threads; eine Anhebung bringt keinen Durchsatz, Beschreibung im Katalog und in der Admin-Anleitung berichtigt. Erwartung: 10 bis 25 Prozent mehr Durchsatz, keine Verdopplung; darüber hinaus nur weniger OCR-Arbeit oder mehr Hardware. Auffälligkeit aus `doc-status`: Lauf 23 (Objekt 216) mit 5.140 nie versandten offenen Jobs, 5.371 wartende und 6.185 übersprungene Wiederholungsjobs `hash`, 1.654 wartende Wiederholungsjobs `discover`; `doc-status` weist nie versandte Jobs jetzt je Art mit Beispielen aus. Im Quelltext gefundene Ursache für Wiederholungsjobs und Doppelnachrichten: `ingest.ensure_run` rief bei jedem Dokumenteneingang (Paperless-Übernahme, Upload) für einen laufenden Lauf `dispatch_run` über alle Dokumente des Objekts auf; für Dokumente mit erledigtem Startjob entstand je Aufruf ein weiterer `discover #n`, wartende Hash-Jobs wurden erneut versandt. Behebung: `ensure_run(obj, documents=[...])` legt für einen laufenden Lauf nur die Startjobs der neuen Dokumente an (`enqueue_start_job`, aus `dispatch_run` herausgelöst); Aufrufer Paperless-Import, Upload-Kommando und Upload-Ansicht übergeben ihre Dokumente, ohne Dokumente bleibt der volle Nachversand. Nachmessung 14:22 UTC nach dem Deployment (Lauf 195): Lauf 23 mit 16.226 offenen Jobs, davon 9.512 nie versandt (hash 8.838, analyze_pages 676, alle Wiederholungsjobs, jüngster 14:22:34 angelegt, Beispiele `analyze_pages #2` und `#3` mit erledigtem Basisjob und Dokument `hashed`). Zweite Ursache im `dispatch_run` selbst: ein Dokument bleibt `hashed`, während seine OCR-Blöcke warten, der Startjob `analyze_pages` ist erledigt; jeder Durchlauf legte dafür einen weiteren `#n` an, der das Dokument erneut analysierte und die OCR-Nachrichten doppelt versandte. Behebung: `dispatch_run` überspringt Dokumente mit offenem Job (Kette läuft), `analyze_pages` überspringt einen Wiederholungsjob mit `chain_running`, solange OCR-Blöcke oder Zusammenführung offen sind. Stand 14:22 UTC: 35.225 Dokumente, abgeschlossen 6.211 = 17,63 Prozent (Deployment mit Neustart der Worker 14:01 bis 14:21 UTC dazwischen); Objekt 216: 3.960 gehasht, 448 in Prüfung, 325 Dubletten. Deployt mit den Läufen 196 (14:43 UTC, Dokumenteneingang) und 197 (15:04 UTC, dispatch_run und analyze_pages). Nachmessung 15:05 UTC (Lauf 198): nie versandte Jobs in allen acht laufenden Läufen 0, keine neuen Wiederholungsjobs; 36.210 Dokumente aktiver Objekte (Paperless-Übernahme läuft weiter), abgeschlossen 6.808 = 18,80 Prozent; acht Läufe laufend (133, 82, 343, 342, 203, 216, 336, 359), 68 wartend; Objekt 216 mit 3.540 gehasht, 832 in Prüfung, 325 Dubletten, 26 klassifiziert. Noch wartende Wiederholungsjobs (hash 9.091, analyze_pages 3.325) enden als `skipped`, überzählige über `jobs-bereinigen echt` bereinigt. Weiter offen: 45 Dokumente im Status error (PDFium-Formatfehler bei einzelnen Dateien, als Fall `job_failed` sichtbar), 1.297 Ablagejobs warten auf die Drive-Schreibsperre ihres Objekts (Ablage je Objekt seriell) | `test_verarbeitung_alle.py` (+2), `test_altbestand.py` (an vorläufige Objektordner angepasst), Testsuiten pipeline, sync, unit, Drive grün |
| 14.09.2026 | M5 | Nachversorgung wartet, solange der Versand läuft | Beim Start der Objekte 203 und 216 (4.733 Dokumente) dauerte der Jobversand länger als zehn Minuten; die Nachversorgung im Sweep stieß ihn ein zweites Mal an, jeder Job erhielt zwei bis drei Nachrichten (unschädlich, `reserved: False`, aber unnötig). `repair_interrupted_runs` überspringt Läufe, deren Objekt in den letzten fünf Minuten Jobs angelegt oder versandt hat (`REPAIR_QUIET_MINUTES`). Stand 13:01 UTC: 5.352 von 35.210 Dokumenten abgeschlossen = 15,20 Prozent, 1.051 in der Hash-Phase (Objekt 216), Warteschlangen `ocr` 8.145, `io` 14.364, alle 18 Worker-Plätze belegt; CPU-Last vorübergehend niedriger, weil die Hash-Phase (Download und Prüfsumme) wenig Rechenzeit braucht, OCR folgt | `test_verarbeitung_alle.py` (Sweep-Test erweitert) |
| 14.09.2026 | Betrieb | Kapazität erhöht (Freigabe Geschäftsführung) | Engpass um 11:52 UTC: OCR und Klassifikation der vier laufenden Objekte fertig, 14.385 Nachrichten in `io` (Ablage nach Drive) auf vier Threads; Server mit 8 Kernen und 31 GB nicht ausgelastet. Betriebswerte: `OCR_PROCESSES` 4 → 6, `WORKER_CPUS` 4 → 6, `WORKER_MEM` 3584m → 5120m, `IO_CONCURRENCY` 4 → 12, `WORKER_IO_CPUS` 0.5 → 1.0, `WORKER_IO_MEM` 512m → 1024m (Vorlage `deploy/env.produktion` ebenso); `processing.max_parallel_objects` 4 → 6 (Katalogobergrenze 4 → 8). Deploy-Aktion `env-set` nimmt mehrere Paare mit `+`, `reconcile-all ohne-ordner` gleicht nur Objekte ohne Objektordner ab. Ausführung: `env-set` (Lauf 180), Deployment (Lauf 181, 12:39 UTC), `config-set processing.max_parallel_objects=6` (Lauf 182), `reconcile-all ohne-ordner` (Lauf 183: ein Objekt, 608, Ordner angelegt; alle Objekte mit Dokumenten haben jetzt einen Ordner). Stand 12:46 UTC: 6 Läufe laufend (133, 82, 343, 342, neu 203 und 216), 70 wartend; Warteschlangen `io` 13.351 (12 Threads in Arbeit), `ocr` und `classify` leer; Dokumente aktiver Objekte 35.210 (Inventur der Ordnerabgleiche registriert weitere Drive-Dateien), abgeschlossen 5.098 = 14,48 Prozent (Zähler unverändert seit 11:52 UTC, weil die vier alten Läufe nur noch Ablage offen hatten und die Worker neu gestartet wurden) | Katalogtests |
| 14.09.2026 | M4, M5 | Objektordner ohne vollständige Anschrift, Ablage blockiert Läufe nicht | Anforderung: Ordner anlegen oder Objekte ohne Ordner überspringen. Objekte 133 und 342 hatten keinen Drive-Ordner (Ort fehlt), 2.833 Ablagejobs warteten, die laufenden Läufe hätten nie geendet und die vier Plätze blockiert. Neu: `drive.object_folder_fallback_pattern` (Vorgabe `{number} {name}`): fehlt die Anschrift für das Namensmuster, legt der Ordnerabgleich einen vorläufigen Objektordner aus Nummer und Bezeichnung an (Hinweis im Lauf, Erkennung über die Nummer, Name in Drive anpassbar; leerer Wert stellt die alte Regel wieder her). `maybe_finish_run` schließt einen Lauf ab, wenn nur noch Ablagejobs auf den Objektordner warten (sie laufen nach Anlage weiter). „Verarbeitung für alle Objekte starten“ stößt für Objekte ohne Ordner die Ordneranlage an und nennt sie in der Meldung. Deployt mit Lauf 175 (10:29 UTC), danach `reconcile-all` (Lauf 176, läuft im Worker weiter, SSH-Sitzung nach 80 Minuten freigegeben). Stand 11:52 UTC: 133 und 342 haben ihren Objektordner (103 von 210 aktiven Objekten mit Ordner, 2 ohne); Dokumente aktiver Objekte 33.218, abgeschlossen 5.098 = 15,35 Prozent (09:45 UTC: 10,84 Prozent), offen 28.120, davon 26.103 Inventurjobs der 72 wartenden Läufe; die vier laufenden Läufe haben nur noch Ablagejobs offen (3.980 `file_to_drive`), Rate seit 07:01 UTC rund 950 Dokumente je Stunde | `test_drive_reconcile.py` (Anlage ohne Anschrift, ohne Ersatzmuster), `test_object_numbers_units.py` (+1), `test_verarbeitung_alle.py` (+2) |
| 14.09.2026 | Betrieb | Steuerungsaufgaben in eigener Queue `control` | Nach dem Deployment der Nachversorgung blieben die Läufe 20 und 21 unversorgt: der Minuten-Sweep und der Operationsversand liefen in der Queue `io` hinter rund 11.700 wartenden Paperless- und Ablagenachrichten und kamen nicht zum Zug. `pipeline.sweep`, `pipeline.schedule_runs`, `sync.dispatch_due`, `status.check_alerts`, `drive.hourly_read_test` und `drive.daily_forced_refresh` laufen jetzt in der Queue `control` (Beat-Einträge, Task-Deklarationen, Start-Sweep), `worker-io` konsumiert `control,io,ai,lists` im Wechsel; `redis-status` zeigt `control` mit an. Deployt mit Lauf 171 (09:39 UTC); Nachmessung 09:45 UTC: Läufe 19, 20, 21 nachversorgt (`documents_total` 261, 567, 403; alle offenen Jobs am Lauf und versandt), Objekte 342 und 343 in Arbeit (330 und 369 gehasht, 24 und 168 Dubletten, 47 und 29 in Prüfung); Dokumente aktiver Objekte 30.448, abgeschlossen 3.300 = 10,84 Prozent; Warteschlangen `control` 5, `ocr` 2.047, `classify` 54, `io` 13.071 (Paperless-Operationen und 2.833 wartende Ablagejobs, überwiegend Objekt 133 ohne Drive-Ordner) | Testsuiten pipeline, sync, unit grün |
| 14.09.2026 | M5 | Unterbrochene Läufe werden nachversorgt | Nachprüfung 08:56 UTC (`doc-status` mit laufenden Läufen je Objekt): Läufe 20 (Objekt 343) und 21 (Objekt 342) seit 05:37 UTC laufend, `documents_total` leer, 567 und 403 offene Jobs ohne Lauf und nie versandt: beim Sammelstart wurden die Läufe als laufend markiert, der Jobversand brach am vollen Redis ab. Sweep jetzt mit `repair_interrupted_runs` (laufende Läufe ohne `documents_total`, älter als 10 Minuten, erneut `dispatch_run`) und Zuordnung wartender Jobs ohne Lauf zum laufenden Lauf ihres Objekts vor dem Nachversand (`redispatch_lost_jobs`). Objekt 133 läuft normal (Prüfung 1.885, klassifiziert 249, Paperless-Übernahme 2.352 Dokumente), Objekt 82 wartet auf Ablage; Redis 15 MB, `ocr` 154, `classify` 1.134, `io` 11.730 Nachrichten (Paperless-Operationen und wartende Ablagejobs des Objekts 133 ohne Drive-Ordner) | `test_verarbeitung_alle.py` (+1) |
| 14.09.2026 | M5 | Wiederholungsjobs begrenzt, Bereinigung, Wiederaufnahme gedeckelt | Befund nach dem Neustart (`doc-status` 06:41 UTC): 11.036 wartende Wiederholungsjobs `analyze_pages` (#n) und 1.750 `ocr_chunk` für 137 Dokumente des Objekts 133, einzelne Dokumente mit über 300 Jobs (142 davon erledigt): `enqueue` legte bei abgeschlossenem Basisjob bei jedem Aufruf einen weiteren `#n` an, obwohl der vorige noch wartete, und `ensure_run` rief `dispatch_run` bei jedem Dokumenteneingang auf. Behebung: ein offener Wiederholungsjob gilt als derselbe Job (kein weiterer `#n`, Lauf wird nachgetragen); Kommando `jobs_bereinigen` und Deploy-Aktion `jobs-bereinigen` (Vorschau, `echt`) setzen überzählige wartende `#n` auf `skipped` (Grund „doppelter Wiederholungsjob“, Protokoll `processing.jobs_dedupe`); `reset_failed_documents` nimmt ein Dokument im Status `error` höchstens dreimal automatisch wieder auf (`RESET_LIMIT`), danach bleibt es mit offenem Fall `job_failed` stehen. `doc-status` weist Wiederholungsjobs und Dokumente mit Mehrfachjobs aus. Deployt mit Lauf 158; Bereinigung auf dem Server (Lauf 159, 07:00 UTC): 11.519 überzählige Wiederholungsjobs auf `skipped` (analyze_pages 10.058, ocr_chunk 1.444, hash 17), 180 behalten. Nachmessung 07:01 UTC: Warteschlange `ocr` von 221.919 auf 95.300 Nachrichten gesunken, `io` 6.267, Redis 1 GB Grenze; Dokumente aktiver Objekte 27.976, davon abgeschlossen (abgelegt, Prüfung, Dublette, klassifiziert) 466 = 1,67 Prozent, offen 27.510 = 98,33 Prozent (registriert 27.358, gehasht 152); offene Jobs discover 27.075, hash 290, analyze_pages 201, ocr_chunk 119, file_to_drive 76; Läufe 4 laufend, 72 wartend. Weiter offen: Ursache der wiederholt scheiternden Zusammenführung bei einzelnen Dokumenten des Objekts 133 (jetzt nach drei Wiederaufnahmen als Fall `job_failed` sichtbar) | `test_verarbeitung_alle.py` (+3) |
| 14.09.2026 | Betrieb | Störung: Redis voll, Verarbeitung stand seit 13.09.2026 23:30 UTC | Befund über `logs` und die neue Aktion `redis-status`: Redis mit 200,01 MB von 200 MB belegt (`noeviction`), Warteschlange `ocr` mit 197.145 Nachrichten bei rund 31.700 offenen Jobs, `io` mit 4.919; alle Worker in Neustartschleife (`OutOfMemoryError: command not allowed when used memory > 'maxmemory'`), Beat konnte keine Aufgaben senden, Dokumentstände unverändert gegenüber 23:30 UTC. Zwei Quellen des Mehrfachversands: `ingest.ensure_run` rief bei jedem Eingang eines Dokuments (Paperless-Übernahme) `dispatch_run` für den laufenden Lauf auf und versandte damit alle offenen Jobs des Objekts erneut; `sync.dispatch_due` versandte jede Minute die 200 ältesten wartenden Operationen erneut. Der vom Auftraggeber gemeldete HTTP 500 beim Knopf „Verarbeitung für alle Objekte starten“ entstand durch die Redis-Sperre in `schedule_runs` bei vollem Redis; zusätzlich lief der Start bis dahin im Web-Request | |
| 14.09.2026 | M5, M8 | Behebung: Versandzeitpunkt je Job und Operation, Sammelstart im Hintergrund, Webhook, Redis-Grenze | `ProcessingJob.dispatched_at` und `SyncOperation.dispatched_at` (Migrationen `pipeline.0004`, `sync.0002`): `send` merkt den Versand; `dispatch_run` versendet nur Jobs ohne Nachricht unterwegs (Fenster 48 Stunden), der Beat-Sweep versendet je Minute höchstens 500 wartende Jobs laufender Läufe, die nie versandt wurden oder deren Versand älter als 48 Stunden ist (`redispatch_lost_jobs`); `sync.dispatch_due` hält höchstens 200 Operationen gleichzeitig unterwegs (Fenster 60 Minuten), Wiederholung, Zurückstellung und Freigabe setzen den Versand zurück. „Verarbeitung für alle Objekte starten“ legt nur wartende Läufe an, der Start läuft als Aufgabe `pipeline.schedule_runs` (Fallback Sweep). Paperless-Webhook liest JSON-String, doppelt kodiertes JSON und nackte Zahl, Unlesbares ergibt 400 statt 500 (Dauerflut im Web-Log). Deploy-Aktionen `redis-status` (nur lesend) und `env-set` (freigegebene Betriebswerte in `.env`); Server-.env auf `REDIS_MEM=1536m`, `REDIS_MAXMEMORY=1024mb` (Vorlage `deploy/env.produktion` ebenso), Deploy-Läufe 143 und 144; die vorhandenen Nachrichten bleiben erhalten (AOF) und werden nach dem Deployment abgearbeitet. Deploy-Lauf 145 scheiterte am Seed-Schritt (`store.invalidate` traf auf das noch volle Redis, weil Redis erst in Schritt 6 neu erzeugt wurde): `deploy.sh` bringt Redis jetzt vor Migration und Seed auf den Stand der `.env`, `store.invalidate` warnt bei nicht erreichbarem oder vollem Cache statt abzubrechen | `test_paperless_pull.py` (+1), `test_verarbeitung_alle.py` (+1), `test_operations.py` (Dispatcher und Beat-Task angepasst), `test_config_store_cache.py` (1) |
| 13.09.2026 | M5 | „Alles aufarbeiten“ und „Verarbeitung für alle Objekte starten“ | Altbestand: Sammelaufarbeitung aller Quellordner mit Objekt (`takeover.run_all`, Celery-Aufgabe `drive.takeover_all`, Knopf, Kommando `altbestand_aufarbeiten`, Deploy-Aktion `altbestand-aufarbeiten`, Stand im Cache, Protokoll `drive.takeover_all`; Fehler je Ordner halten die anderen nicht an, gesperrte Objekte kommen am Ende erneut an die Reihe). Verarbeitung: `start_runs_for_all` reiht je aktivem Objekt mit offener Arbeit einen Nachlauf ein (Knopf auf der Objektliste, Kommando `verarbeitung_alle_starten`, Deploy-Aktion `verarbeitung-alle`, Protokoll `processing.start_all`). Ausführung auf dem Server 13.09.2026 (Deploy-Läufe 129 und 130): Sammelaufarbeitung für 104 Quellordner als Hintergrundaufgabe eingereiht; 72 Nachläufe für Objekte mit offener Arbeit eingereiht, Start nacheinander nach `processing.max_parallel_objects` | `test_altbestand.py` (+1), `test_verarbeitung_alle.py` (2) |
| 13.09.2026 | M8 | Gesamtlauf Speicherpfade | Knopf „Alle zugeordneten Pfade: Feld setzen und übernehmen“, Kommando `paperless_feld_setzen` und Deploy-Aktion `paperless-feld-alle` (Vorschau, `echt`): alle Speicherpfade mit Objekt nacheinander, Feld nur bei Dokumenten ohne Wert, danach ein Bestandslauf mit allen Nummern; Stand `field_fill:all`, Aufgabe `sync.storage_path_fill_all`. Keine doppelte Übernahme: wartet für ein Paperless-Dokument bereits ein `paperless_pull`, reihen Webhook, Abgleich und Bestandslauf keine zweite Operation ein (`pending_pull`). Ausführung auf dem Server 13.09.2026 (Deploy-Lauf 124): 55 zugeordnete Speicherpfade, 10.967 Dokumente, Feld bei 10.812 gesetzt, 155 bereits gesetzt, 0 abweichend, Bestandslauf 2 mit 34 Objektnummern gestartet; die Übernahme nach Anwendung und Drive läuft über die Operationsliste | `test_speicherpfade.py` (+2), `test_paperless_pull.py` (+1), `test_inventory.py` (+1) |
| 13.09.2026 | M5 | Nachräumen der Drive-Ordner nach der Verteilung | Anforderung: bestehende Altordner werden über Registrierung und Ablage in die Struktur überführt (bereits so), danach gehen leere Ordner außerhalb der Sollstruktur und nicht registrierte Temporär- und Systemdateien automatisch in den Drive-Papierkorb, erst wenn kein Job des Objekts mehr offen ist und nach Live-Prüfung, dass der Ordner leer ist. `apps.drive.auto_cleanup` (Aufgabe `drive.auto_cleanup`, Auslöser `maybe_finish_run`), Bäume Objektordner und aufgearbeitete Quellordner, Schutz für Struktur, Objektwurzel, Eingangsordner, Dokumente (auch Dubletten), Google-Dokumente, unbekannte Dateien; leerer Quellordner verschwindet aus der Altbestand-Tabelle; Schalter `drive.auto_cleanup_enabled` (an), Protokolle `drive.auto_cleanup`, `drive.trash`; Dubletten weiter nur von Hand | `test_altbestand.py` (+3: Quellordner nach Aufarbeitung, Objektordner mit Sperren und Schalter, leerer Quellordner) |
| 13.09.2026 | M5 | Massenanlage der Objekte aus dem Altbestand | Kommando `altbestand_objekte_anlegen` (Deploy-Aktion `altbestand-objekte`, Argument `echt`, sonst Vorschau): je Nummer der Altbestand-Tabelle ohne Objekt ein Objekt; Bezeichnung und Verwaltungsart aus `db/seeds/objektregister.csv` (Immoware24-Export 01.07.2026: 67 aktive, 3 in Abrechnung, 75 abgegebene Objekte; nur Nummer, Bezeichnung, Verwaltungsart, Status), sonst aus dem Ordnernamen mit Vorgabe WEG; Anschrift nur, wenn Straße, Hausnummer und Ort eindeutig sind (durch Komma getrennt in beliebiger Reihenfolge oder mit PLZ; Ort ohne Ziffern; Unklares bleibt leer, dann wartet der Objektordner auf die Nachpflege); Plausibilität Register gegen Ordnername (gemeinsames Wort oder Wortanfang), sonst gilt der Ordnername und beide stehen in den Notizen; Ausführung auf dem Server 13.09.2026 (Deploy-Lauf 118): 99 Objekte angelegt, 100 Quellordner gebunden, 99 Ordneranlagen eingereiht (Objektordner entsteht nur bei vollständiger Anschrift, sonst nach der Nachpflege); Hinweise in den Notizen, Quellordner gebunden, Ordneranlage angestoßen, Protokoll `object.create` mit `source altbestand_bulk`; idempotent | `test_altbestand.py` (+12: 11 Anschriftfälle, Massenanlage mit Vorschau, Register, Bindung, Ordneranlage, Wiederholung, ohne Register) |
| 13.09.2026 | M8 | Speicherpfade: Altbestand aus Paperless je Objekt | Seite `/verwaltung/sync/speicherpfade/` (sync.manage): Speicherpfade aus Paperless mit Dokumentzählern (Zwischenspeicher eine Stunde), Objektnummer aus dem führenden Zahlenblock, Zuordnung zu aktiven Objekten, Vorschau je Pfad (ohne Feld, gleicher Wert, abweichender Wert mit IDs), „Feld setzen und übernehmen“ setzt das Feld MHV Objekt per `bulk_edit` in Paketen von 100 nur bei Dokumenten ohne Wert (nie überschreiben), Stand je Pfad im Cursor `field_fill:<id>`, Protokoll `sync.paperless_field_fill`, danach echter Bestandslauf nur für diese Objektnummer; Celery-Aufgabe `sync.storage_path_fill`. Client und Fake: `list_storage_paths`, `get_storage_path`, Filter `storage_path` in Seitenlisten. Audit-Register um `drive.trash`, `drive.trash_failed`, `drive.takeover_source_prune` ergänzt (fehlten) | `tests/integration/sync/test_speicherpfade.py` (16: 9 Nummernfälle, Übersicht, Vorschau, Befüllung mit Paketen und Bestandslauf, Übernahme, Abbruch, Verweigerung, Oberfläche und Rechte); Seite in `test_seiten_rendern.py` |
| 13.09.2026 | M8 | Bestandslauf Paperless je Objekt | Das Feld Umfang (Objektnummern) wirkt jetzt auch beim Paperless-Lauf: je Nummer nur die Dokumente mit genau diesem Wert im Feld MHV Objekt (`custom_field_query`, Schreibweisen mit und ohne führende Nullen, geführte Objektnummer), Seitenstand je Wert, statt aller rund 96.000 Dokumente. Manifest unterscheidet „ohne Feld“ von „Feld verweist auf kein angelegtes Objekt“ (`details.object_field`), die Übernahme meldet dies ebenso als Grund. Anleitung „Altbestand aus Paperless je Objekt übernehmen“ (docs/betrieb/paperless-sync.md 6.1). Pilot am 13.09.2026 produktiv: Objekt 82 durchgängig (Dokument aus Paperless ins Objekt und nach Drive, neun Einzelabrechnungen nach Paperless), danach Modus `full`, Standard `paperless.import_only_with_object` an, Cursor ab jetzt | `test_inventory.py` (+2: Umfang Trockenlauf, Umfang echter Lauf), `test_paperless_client.py` (+1), `test_paperless_fake.py` (+1) |
| 12.09.2026 | M8 | Synchronisation Paperless-ngx und Google Drive | Ergänzungsauftrag Dokumentenübernahme: kontrollierter beidseitiger Abgleich mit der Anwendung als einzigem Koordinator (`apps.sync`). Identität je Dokument (`Document.uuid`), Verknüpfungen je System und Rolle (`sync_links`), Versionen, dauerhafte Operationsliste mit Idempotenz, Sperren und Rückstau (`sync_operations`), Cursor, Bestandsläufe mit Manifest, Lernbeispiele und versionierte Zuordnungsregeln. Paperless-Client über die dokumentierte API (Token, API-Version 9 und 10, Aufgabenverfolgung, UUID-Suche) mit In-Memory-Fake; Drive-Adapter um Änderungsprotokoll, appProperties und Export erweitert. Eingang aus Paperless, Drive und Upload läuft über das technische Eingangsobjekt (aus Objektlisten, Ordnerabgleich und Vollständigkeit ausgenommen) durch die vorhandene Pipeline in die mehrstufige Objektzuordnung (Belege, Schwellen, Widersprüche blockieren die Automatik); Konflikte und Löschungen werden nie automatisch aufgelöst oder gespiegelt (Fälle `sync_conflict`, Löschmarkierung nach Bestätigung). Oberfläche: `/verwaltung/sync/` (Verbindung, Kennzeichnung, Bestandsläufe, Operationen), `/eingang/` (Zuordnung, Konflikte, Dubletten, Formate, Fehler), Review-Aktionen Zuordnen, Ablehnen, Konflikt entscheiden. Rechte `sync.manage`, `inbox.work`; Webhook `/webhooks/paperless/` mit gemeinsamem Kopfzeilengeheimnis (Paperless signiert nicht). Konfiguration: 27 Schlüssel `paperless.*` und `sync.*` (Hauptschalter aus, Modus readonly), Secrets `paperless_token` und `paperless_webhook_token` (Platzhalter durch `deploy.sh`), Beat-Einträge, Tabellenrechte. Deployt am 12.09.2026 (Deploy-Lauf 104, Stand `3e71462`, Smoke-Test Lauf 105); der erste Versuch scheiterte an der UUID-Migration auf Bestandszeilen (MariaDB rollt Schemaänderungen nicht zurück), die Migration prüft seitdem den Ist-Zustand und läuft auf halb migrierten Datenbanken durch. Anbindung aus; Aktivierung blockiert bis Adresse, Version und API-Token der Paperless-Instanz vorliegen (docs/betrieb/paperless-sync.md, Pilot mit `paperless.mode` pilot und Pilotobjekten) | Unit 79 (`test_paperless_client.py` 27, `test_paperless_fake.py` 9, `test_drive_changes.py` 11, `test_assignment_normalize.py` 12, `test_assignment_candidates.py` 20); Integration 94 in `tests/integration/sync/` (Ausgang 7, Eingang und Webhook 12, Drive-Änderungen 12, Bestandslauf 14, Operationen 20, Konflikte und Einrichtung 11, Dokumenteneingang und Lernen 14 und 4); Seiten `sync_admin`, `inbox_list` in `test_seiten_rendern.py`; Migrations-Roundtrip |
| 12.09.2026 | M6 | Sonderakten ohne Wettlauffehler | `doc-status 82` zeigte fehlgeschlagene Entscheidungsjobs mit „Duplicate entry ‚5-Unzugeordnet‘ für uq_owner_files_name_active“: zwei Entscheidungsjobs legten die Sonderakte gleichzeitig an. `owner_file_unassigned` und `owner_file_unknown_unit` fangen den Integritätsfehler im Sicherungspunkt ab und verwenden die inzwischen vorhandene Akte. Die dort ebenfalls gelisteten Rechtefehler („INSERT command denied … owner_files“) stammen aus Läufen vor dem Deployment vom 11.09.2026; die Tabellenrechte werden seitdem bei jedem Deployment nachgezogen | `test_sonderakten_wettlauf.py` (3) |
| 12.09.2026 | M4 | Sammellauf Ordnerabgleich robust | Deploy-Lauf 90 (`reconcile-all`) legte die Stammakte-Unterordner bei allen Objekten an, brach aber am Ende beim Schreiben der Sammelfassung ab (`/data/exports` im Web-Container schreibgeschützt). Der Sammellauf meldet einen Schreibfehler jetzt als `summary_error` statt zu scheitern; die Deploy-Aktion läuft im Dienst `worker-io` mit beschreibbarem Exportverzeichnis | `test_drive_reconcile.py` (+1) |
| 12.09.2026 | M2, M7, M10 | Sollbestand der Stammakte, Teil 2 | Seeds nach der Checkliste der Geschäftsführung: 93 Dokumentarten der Stammakte an ihren Unterordnern (jetzt 101 in 02, 156 gesamt), 95 Regeln `R-02-*` (149 gesamt), 54 Prüfpositionen `sa_*` der Kategorie `stammakte` mit Zeilen und Gruppe für die Nachforderung (78 Prüfpunkte, 100 Textbausteine); Regeln gegen eigene Beispiele, Beispiele der vorhandenen Regeln und den Testkatalog geprüft (Konflikte durch Wortgrenzen, Negativbedingungen und Prioritäten aufgelöst); Bezeichnerregister neu erzeugt | Regeltests je Regel (149), `test_katalog.py` (T07 jetzt hausmeistervertrag), `test_seeds_catalog.py`, `test_stammakte_katalog.py` (+1: Seed-Katalog und Gruppe im Schreiben), `test_drive_reconcile.py` (25 Anlageaktionen je Objekt) |
| 12.09.2026 | M5 | Altbestand „Aufräumen“ | Nach der Aufarbeitung: Vorschau und Ausführung je Quellordner (Admin, `drive.cleanup`, Step-up). Dubletten, Temporär- und Systemdateien, danach leere Ordner in den Drive-Papierkorb, wahlweise 06/03_Dubletten; verbleibende Dateien und Strukturordner tabu, offene Jobs sperren; `trash()` im Adapterprotokoll und Google-Adapter (files.update trashed); Protokoll `drive.trash`, Dokumente der Dubletten stillgelegt, leerer Quellordner verschwindet aus der Tabelle | `test_altbestand.py` (+3) |
| 12.09.2026 | M4 | Mieter aus Dokument anlegen | Praxisfall Objekt 82: Mietverträge blieben ohne Mieter, Akte und Liste, weil Mieterstammdaten nur über den Listenimport entstanden. Neu: `apps.classification.leasefacts` liest Mieter, Mitmieter, Einheit oder Lage, Mietbeginn, Mietende, Kaltmiete, Vorauszahlungen und Kaution aus dem Text; der Fall „Mieter unbekannt“ trägt die Werte und den Vorschlag `create_tenant`; Stufe 3 liefert bei aktivem Provider einen Block `lease` (Schema, Prompt 2026-09-12.1, Schalter `ai.extract_lease_facts`); im Review Center Abschnitt „Mieter aus dem Dokument anlegen“ (vorbelegt), die Bestätigung legt Mieter, Mitmieter, Einheit (auch neu, unabhängig von der Sollzahl), Mietverhältnis, Zuordnung und Mieterakte an, verschiebt das Dokument dorthin und erzeugt die Mieterliste neu; `doc-status <Objektnummer>` zeigt je Dokument Stufen, Entitätenzähler und Fälle ohne Namen | `tests/unit/test_leasefacts.py` (6), `tests/integration/review/test_mieter_aus_dokument.py` (3), `tests/integration/ai/test_lease_stage3.py` (2) |
| 12.09.2026 | M1 | Akten-Vorlage nach Verwaltungsart | Hinweis der Geschäftsführung: Mietverwaltung hat einen Eigentümer je Haus. `file_plan`: rental eine Eigentümerakte je Objekt (`object_owner`, „Eigentümer“, Name folgt den Eigentümern) und Mieterakten je Einheit; weg Eigentümerakten je Einheit ohne Mieterakten; weg_with_se beides. Unbenutzte Platzhalter werden stillgelegt (Protokoll), Ordner in Drive bleiben; Kategorie 05 geht in der Mietverwaltung direkt in die Objektakte (Pipeline und Review Center); Migration `parties.0002` | `tests/integration/pipeline/test_unit_files.py` (+4, Fixture auf weg_with_se) |
| 12.09.2026 | M5 | Altbestand „Aktualisieren“ | Liest alle Quellordner neu; gelöschte oder im Papierkorb liegende Ordner verschwinden aus der Liste (Protokoll `drive.takeover_source_prune`), vorübergehende Drive-Fehler entfernen nichts; ersetzt „Alle neu lesen“ | `test_altbestand.py` (+1) |
| 12.09.2026 | M1 | Zweiter Faktor nur für Pflichtrollen, Gerät 90 Tage merken | Vorgabe der Geschäftsführung: nur Admins brauchen TOTP, Sachbearbeiter freiwillig (`security.mfa_required_roles`, Vorgabe `admin`, ersetzt `MFA_REQUIRED`); nach dem Code „Dieses Gerät merken?“ für `MFA_TRUST_DAYS` Tage (allauth-Vertrauenscookie, erlischt bei Passwortwechsel und TOTP-Zurücksetzung); Nutzerliste zeigt Pflicht oder freiwillig | `tests/integration/test_mfa_policy.py` (8) |
| 11.09.2026 | M4, M5 | Praxisfall Objekt 82 (240 Dokumente aus dem Altbestand) | Ursachen: mehrere Drive-Ordner mit der Nummer 82 (Fall „Objektnummer doppelt“ ohne Entscheidungsmöglichkeit, Ablage wartete unsichtbar), fehlende Worker-Rechte auf Akten- und Objekttabelle (drei Dokumente auf Fehler, ein Abgleich dauerhaft „läuft“), rund 40 Temporärdateien als Fälle. Umgesetzt: Ordnerwahl im Fall („Als Objektordner verwenden“), „Objektordner festlegen“ in der Objektansicht, Hinweis und Wartegründe auf Objekt- und Dokumentseite, Abgleich stößt nach der Wahl automatisch an, Worker-Rechte, Abgleich endet bei internem Fehler als fehlgeschlagen, Temporärdateien werden übersprungen (`drive.takeover_ignore_patterns`), `doc-status` zeigt Abgleiche, Knoten, Fälle und Wartegründe | `tests/integration/pipeline/test_objektordner.py` (5), `test_grants_sql.py` (+2) |
| 11.09.2026 | M2, M4, M5, M13 | Gegenprüfung des Tagesstands | Sechs Prüfer je Dimension (Akten, Pipeline, Übernahme, Oberfläche, Betrieb, Nebenläufigkeit) über den Diff des Tages, 49 Befunde, je Befund drei unabhängige Gegenprüfer. Behoben: Mieterakte wurde bei Ablage auf einen Mitmieter verkürzt; neue Eigentümergruppe nach Wechsel ohne geführten Namen; Einheit umbenannt ohne Aktenfolge; laufender und künftiger Eigentümer in einer Akte; Platzhalteranlage ohne Sperre; Aktenordner ohne Objekt-Schreibsperre, Auftrag ohne Wiederholung bei Drive-Fehlern; Übernahme: Jobversand in der Transaktion, Anhängen an wartenden oder Dry-Run-Lauf, Doppelklick, Einzelauswahl in großen Ordnern, unlesbare Ordnereingabe, abweichender Idempotenzschlüssel; Google-Dokumente ohne Download in die Prüfung; Mieterakten-Anlage bei parallelen Dokumenten; Oberfläche: aktive Navigation für Verwaltungsseiten, Kartenrahmen der Konto- und Anmeldeseiten, Warnhinweis Archivieren, Filterformulare, Knopfhierarchie, Kandidatenliste der Massenbearbeitung (war leer), Abmelden-Knopf, Zeilenmarkierung beim Zeigen, Schlüsselüberschrift, Kontraste, Tabellen und Kopf auf schmalen Bildschirmen, Fallansicht-Raster, Seitenbilder, Sichtenzeile, Betriebstexte der Drive-Seite, Abbrechen mit Rücksprung, Spaltenzahl der Dokumentliste; Betrieb: config-set lehnt Fehler mit Meldung ab, Doku der Wertschreibweise, Schlüsselzahl im Handbuch. Bewusst belassen und begründet: Ordnerzugriff für Sachbearbeiter, Dateien aus fremden Objektordnern, app-seed je Deployment (keine im Admin gepflegten Seed-Daten). 13 Gegenprüfer der Dimension Pipeline brachen wegen erschöpfter Nutzungskontingente ab; die Befunde dort wurden von Hand geprüft | Regressionstests in `test_unit_files.py` (+4), `test_drive_takeover.py` (+1), `test_defer_without_drive.py` (+1), `test_nav_section.py`; Gesamtsuite grün |
| 11.09.2026 | M4, M5 | Altbestand-Tabelle | Neue Seite „Altbestand“ (Berechtigung objects.write): Quellordner per Link oder ID aufnehmen (ohne Doppelte), Namen und Objektnummern aus Drive lesen, Zuordnung zum Objekt über den Zahlenwert, „Objekt anlegen“ vorbelegt aus dem Ordnernamen (Bindung nach dem Speichern), „Aufarbeiten“ übernimmt rekursiv in das Objekt und stößt bei fehlender Struktur den Ordnerabgleich an, Zähler, Status, Lauf; Modell `takeover_sources` mit Migration; Kommando `altbestand_import` und Deploy-Aktion `altbestand-import`; Seed-Datei mit den 113 Ordnern des Auftraggebers (keine Doppelten); Ablage wartet bei fehlender Zielstruktur statt Fehlversuch | `tests/integration/pipeline/test_altbestand.py`; Ergänzung `test_defer_without_drive.py` (Ablage wartet ohne Objektordner); Seite in `test_seiten_rendern.py` |
| 11.09.2026 | M13 | Designüberarbeitung | Oberfläche modernisiert in der Farbwelt der Hausverwaltung Müller GmbH: haftender Kopf mit Kennlinie und aktiver Navigation (Bereich aus dem URL-Pfad), Karten mit weichem Schatten, Tabellen mit ruhigem Kopf und Orange nur als Akzentlinie, Statusmarken als farbige Pillen (gelb ergänzt, war unformatiert), Meldungen nach Art (Erfolg, Hinweis, Warnung, Fehler), Schaltflächen primär und sekundär, Formulare mit Fokusrahmen, Reiter als Pillen, Review Center mit Seitenleiste und Textbereich, Ausklappbereiche, Druckansicht, Umbruch für schmale Bildschirme; Darstellungsfehler behoben: Gedankenstriche als Platzhalter in fünf Vorlagen ersetzt (keine Angabe, offen), veralteter Hinweis „Drive-Ordner ab M4“ in Objektansicht und Objektformular entfernt | `tests/unit/test_nav_section.py`; `tests/integration/test_seiten_rendern.py` (alle Seiten rendern) |
| 11.09.2026 | M4, M5 | Bestand aus Drive übernehmen | Ordnerbrowser je Objekt („Bestand aus Drive übernehmen“ in Objektansicht und Dokumentseite): Wurzelordner abwärts oder beliebiger Ordner per ID oder Drive-Link, Pfadanzeige, Unterordner, Dateien mit Stand (frei, in Objekt …, Verknüpfung); Übernahme ausgewählter Dateien, aller Dateien eines Ordners oder mit Unterordnern (Grenze `drive.takeover_max_files`, Vorgabe 500); Registrierung als Bestandsdokumente mit Herkunftspfad und Discover-Job, Lauf startet oder hängt sich an den laufenden; bereits registrierte Dateien und Verknüpfungen werden übersprungen; Protokoll `drive.takeover`; nichts wird gelöscht | `tests/integration/pipeline/test_drive_takeover.py` (5: Durchsehen mit Pfad und Link, Übernahme und Wiederholung ohne Dubletten, Auswahl und Unterordner, Grenze und leerer Ordner, Link-Erkennung) |
| 11.09.2026 | M2, M4, M5 | Akten-Vorlage | Mit der Sollzahl Einheiten entstehen Platzhalter-Einheiten WE 1 bis WE n und je Einheit Eigentümer- und Mieterakte (Name WE01 …); nach dem Objektordner legt der Abgleich die Aktenordner an (05 mit elf Unterordnern, 04 je Einheit); Zuordnung von Eigentümer (Hand, Review, Import) oder Mieter (Import) benennt Akte und Drive-Ordner um (`WE01_Mustermann`), weitere Eigentümer derselben Gruppe erweitern den Namen; von Hand in Drive vergebene Namen bleiben; Knopf „Akten anlegen“ in der Objektansicht; Mieterdokumente werden in die Mieterakte der Einheit abgelegt; Schalter `owner_file.create_folders_eagerly` jetzt `true` (auf dem Server per `config-set` umgestellt, Deploy-Lauf 66, Protokoll `setting.update`); Deploy-Aktion `config-set`; Deploy-Lauf 65 (Commit ca11cda) erfolgreich; Dokumentseite zeigt bei Fehlern den Knopf „Erneut versuchen (n)“ | `tests/integration/pipeline/test_unit_files.py` (7: Platzhalter und Akten, Umbenennung mit Protokoll, manueller Name bleibt, Mieterakte als Ablageziel, Schalter aus, Objektanlage über die Oberfläche, Knopf) |
| 11.09.2026 | M4 | Google-Verbindung: Kontoprüfung umgestellt | Nach der Korrektur des Client-Secrets (Probe `oauth-check`: Zugangsdaten in Ordnung) scheiterte der Callback mit „Userinfo antwortete 401“. Ursache: Die Anwendung fordert nur den Drive-Bereich an, der Userinfo-Endpunkt verlangt einen Identitätsbereich. Die Kontoprüfung läuft jetzt über Drive `about.get` (`user.emailAddress`), das mit dem Drive-Bereich funktioniert; keine zusätzliche Berechtigung nötig | `tests/unit/test_oauth_kontoabfrage.py` (Abfrage über Drive, Ablehnung bei 401) |
| 11.09.2026 | M1, M13 | Ferndiagnose und Deployment-Tests | Drei lesende Workflow-Aktionen: `oauth-check` (Konfiguration der Google-Verbindung ohne Geheimnisse, Probe der Client-Zugangsdaten gegen den Token-Endpunkt), `deploy-tests` (T2, T3, T11, T14), `doc-status` (Dokumente je Objekt und Status, Jobs). Der Workflow wiederholt einen Verbindungsabbruch zum Server (Exit 255) bis zu dreimal. Befund `oauth-check`: Client-ID in Ordnung, Secret-Datei mit 89 Zeichen beginnend `GOOGLE_` statt rund 35 Zeichen `GOCSPX-`, Google antwortet `invalid_client`; Korrektur beim Auftraggeber. Deployment-Tests: T2, T3, T14 bestanden; T11 zeigte Worker-Logs als Text, weil Celery den Root-Logger übernahm, behoben mit `CELERY_WORKER_HIJACK_ROOT_LOGGER = False` | `tests/unit/test_celery_logging.py`; Ergebnisse in docs/betrieb/deployment-test.md |
| 11.09.2026 | M2, M5, M12 | Befunde aus dem ersten Praxistag auf dem Server | Vier Befunde des Auftraggebers geprüft und behoben: (1) Statusseite 500, Ursache `/srv/objektakte/backup` mit 700 für die Anwendung nicht betretbar, `Path.exists` warf PermissionError; jetzt Verzeichnis 711 und `status.json` 644 (Sicherungen selbst bleiben 700), gesetzt vom Sicherungsdienst bei jedem Start, zusätzlich fängt `backup_status` OSError ab. (2) Protokollseite 500, `audit/list.html` nutzte `has_perm` ohne `{% load perms %}`. (3) Dokumente im Status error wurden von einem neuen Lauf nicht mehr aufgegriffen; `dispatch_run` setzt sie jetzt zurück (Wiedereinstieg so spät wie möglich: ocr_done bei vorhandenen Seiten, hashed bei vorhandenem Hash) und schließt den Fall job_failed. (4) Ohne Google-Verbindung setzte `file_to_drive` das Dokument nach drei Fehlversuchen auf error; neue Ausnahme `DeferJob` lässt den Job ohne Verbrauch eines Versuchs warten (zehn Minuten), Dokument bleibt klassifiziert | Neu: `tests/integration/test_seiten_rendern.py` (13 Hauptseiten als Admin, Protokoll als Sachbearbeiter, Statusseite mit gesperrtem Sicherungsverzeichnis), `pipeline/test_reprocess.py`, `pipeline/test_defer_without_drive.py` |
| 11.09.2026 | M2 | Objekte archivieren und wiederherstellen | Auf Wunsch des Auftraggebers: „Archivieren“ in der Objektansicht (Step-up, Grund, Bestätigung) setzt deleted_at, deleted_by, delete_reason und Status archiviert; Objekt verschwindet aus Listen und nächtlichen Läufen, alle Daten bleiben, in Drive wird nichts gelöscht, verschoben oder umbenannt (die Anwendung hat keine Drive-Löschfunktion). Archivliste mit Wiederherstellung; Konflikt bei inzwischen neu vergebener Objektnummer wird abgewiesen; während offener Läufe gesperrt. Audit `object.archive`, `object.restore` | `tests/integration/test_object_archive.py` mit vier Fällen |
| 11.09.2026 | M2, M6 | Ordneranlage mit der Objektanlage verbunden | Auf Wunsch des Auftraggebers entsteht der Objektordner in Drive jetzt unmittelbar mit dem Speichern eines neuen Objekts. Die Objektanlage reiht dazu den Ordnerabgleich als Ausführungslauf ein (`trigger_object_folders`), wodurch die geprüfte Logik greift: ein vorhandener Ordner mit derselben Objektnummer wird übernommen, mehrere Treffer oder ein Treffer im Papierkorb führen zu einem Review-Fall, erst ohne Treffer entsteht ein neuer Ordner nach `drive.object_folder_name_pattern`. Der Lauf erfolgt als Auftrag, damit die Anlage nicht an der Antwortzeit oder an einem Drive-Fehler hängt; das Objekt wird immer gespeichert. Fehlt Verbindung oder Wurzelordner, nennt eine Meldung den Grund und den nächsten Schritt. Neuer Schlüssel `drive.create_folders_on_object_create` (Vorgabe `true`) | Neu: `tests/integration/test_object_create_folders.py` mit vier Fällen (Einreihen mit den erwarteten Argumenten und Audit-Eintrag, ohne Verbindung, ohne Wurzelordner, Schalter aus); Gesamtsuite 385 Tests grün, 1 übersprungen |
| 11.09.2026 | M5 | Fehler im Betrieb: Verarbeitung bricht ab | Nach dem ersten Hochladen auf dem Server scheiterte `pipeline.merge_pages` mit `OperationalError (1142) DELETE command denied to user 'app_worker' for table objektakte.document_pages`. Ursache: Sieben Jobs ersetzen den Inhalt einer Tabelle vollständig (Löschen und unmittelbares Neuschreiben, Voraussetzung der Wiederaufnahme nach Abbruch), die Rechteliste des Kontos `app_worker` sah dafür kein DELETE vor. Annahme A32 der Architektur galt als gesetzt, war aber nie gegen den Quellcode geprüft. Behoben in `grants_sql` mit der Menge WORKER_DELETE (document_pages, document_entities, document_owner_links, document_tenant_links, completeness_findings, drive_sync_actions, import_rows), Dokumentation in betrieb.md 3.9 und architektur.md 9.4 nachgezogen | Neu: `tests/unit/test_worker_delete_rechte.py` liest jede Löschstelle außerhalb der Web-Ansichten über den Syntaxbaum (zehn Fundstellen) und prüft sie gegen die Rechteliste; `test_grants_sql.py` prüft die erzeugte SQL auf DELETE für diese Tabellen und dessen Fehlen bei documents, processing_runs und audit_events |
| 11.09.2026 | M1 | TLS-Zertifikat und Abschluss der Erstinstallation | Traefik hat nach einer neuen Anforderung über die Workflow-Aktion `cert-retry` ein gültiges Zertifikat von Let's Encrypt erhalten (Aussteller CN = YR2, Inhaber CN = uebernahme.muellerhv.de, gültig vom 11.09.2026 bis 10.12.2026). Die vier vorangegangenen Fehlversuche zwischen 00:51 und 01:15 UTC scheiterten an der DNS-Zweitprüfung von Let's Encrypt, obwohl der A-Record unverändert auf 187.124.23.80 zeigt; die Ursache lag außerhalb des Servers und ist mit der neuen Anforderung entfallen. Traefik selbst wiederholt eine gescheiterte Anforderung nicht von allein, dafür gibt es nun die Aktion `cert-retry`. Die Anwendung ist damit produktiv unter https://uebernahme.muellerhv.de erreichbar | Aktion `smoke`, Lauf 34555104682: HTTPS mit Zertifikatsprüfung 200 (`tls 0`), Bereitschaftsabfrage `/readyz/` mit Datenbank, Schema (50 Migrationen), Redis und Speicher (175,1 GiB frei) in Ordnung; Deployment-Test T1 bestanden (docs/betrieb/deployment-test.md) |
| 11.09.2026 | M1 | Erstinstallation auf dem VPS | Anwendung läuft auf 187.124.23.80: Datenbank mit 50 Migrationen und geladenen Seeds (6 Kategorien, 15 Unterordner, 63 Unterarten, 6 Aufbewahrungszeilen, 54 Regeln, 45 Textbausteine, 24 Prüfpunkte, 117 Konfigurationsschlüssel), Redis, Web, drei Worker, Beat und Sicherung gesund; Bereitschaftsabfrage meldet Datenbank, Schema, Redis und Speicher in Ordnung (175,6 GiB frei bei 18 GiB Reserve); erstes Admin-Konto angelegt. Behoben auf dem Weg dorthin: Leserechte der Secret-Dateien, Datenbankkonten nach fehlgeschlagenem Init neu angelegt, virtuelle Umgebung je Image unter ihrem endgültigen Pfad, Umgebung des Sicherungs-Cronjobs mit Anführungszeichen, procps im Sicherungsimage, REVOKE nur mit vorhandenem Recht, start_interval der Healthchecks, Unterbefehle über das Entrypoint | Offen: TLS-Zertifikat. Let's Encrypt bricht die Zweitprüfung mit „networking error looking up A/AAAA für uebernahme.muellerhv.de“ ab, obwohl der A-Record auf 187.124.23.80 zeigt und identisch zu den funktionierenden Subdomains ist; Traefik liefert bis dahin sein Platzhalterzertifikat. Wiederholung nach Ablauf der Sperrfrist |
| 10.09.2026 | M0, M1 | Serverzugang und Befund | GitHub-Anbindung des VPS steht (Deploy-Nutzer, Deploy Key, Aktionsschlüssel mit erzwungenem Kommando, Workflow `Deploy` mit den Aktionen check, pull, befund, env-init, first-run, deploy, rollback, create-admin); Serverbefund über die Aktion `befund` erhoben und in docs/betrieb/serverbefund.md abgelegt (8 Kerne, 31,3 GiB, 189,6 GiB frei, Traefik 3.7 im Host-Netz mit Entrypoints web und websecure, Resolver letsencrypt, globaler Redirect, Server mit 17 fremden Containern geteilt); Produktionskonfiguration in deploy/env.produktion mit konservativen Limits; Proxy-Netz mit festem Subnetz wird vom Deploy-Skript angelegt; Redirect-Labels aus der Compose-Datei entfernt | Aktion check grün (Verbindung ok, Checkout d6d3d68), Aktion befund grün; Erstinstallation folgt |
| 10.09.2026 | M15 | Schritte 1 bis 5 und 8 (Dokumente) | Bedienungsanleitungen nach H 8 in docs/anleitungen/bedienung-objektanlage.md (Anmeldung, Objekt, Ordnerabgleich, Import, Verarbeitung, Listen, Vollständigkeit, Nachforderung, Suche und Berichte, häufige Fragen) und bedienung-review-center.md (Liste, Sichten, Detailansicht, Pflichtfelder, Aktionen, Aufteilen, Dublette, Zurückstellen, Massenbearbeitung, Serienmodus, Tastenübersicht, Begriffe); Admin-Konfiguration aus dem Katalog erzeugt (docs/anleitungen/admin-konfiguration.md, 117 Schlüssel in 18 Gruppen mit Seed, Bedeutung, Quelle, Wertebereich; `.env`-Variablen; Neustartbedarf); Runbook vervollständigt (Pflichtfälle aus 2.19, Zertifikatszeile); Lizenzen aus den Paketmetadaten (docs/architektur/lizenzen.md mit Ghostscript AGPL, Redis-Lizenzlage und Valkey, mysqlclient GPL und PyMySQL); Abnahmeprotokoll gegen die sechs Punkte der Definition of Done mit Wartungsplan (docs/plan/abnahme.md); README fortgeschrieben | Grep leer; PII-Prüfung nur mit synthetischen Namen (CSV in der Entwicklungsumgebung nicht vorhanden) |
| 10.09.2026 | M15 | offen | Bildschirmfotos ohne reale Daten (nach T1), Restore-Probe mit Protokoll G 7.4, Einweisung und Nutzeranlage nach V-20, schriftliche Abnahme; Punkte 1, 3 und 4 der Definition of Done hängen am Serverzugang (V-01), am Google-Zugang (V-05 bis V-09) und an der Freigabe des Dry-Run-Protokolls (V-19) | |
| 10.09.2026 | M14 | Vorlage | Ablauf Dry-Run, Sichtung, Freigabe, Ausführung, zweiter Lauf mit Kommandos, Kennzahlenfeldern und Prüfabfragen in docs/betrieb/abgleich-protokoll.md; alle Ergebnisfelder ausstehend, nichts vorab eingetragen | Prüfabfragen als SQL hinterlegt; Ausführung wartet auf V-05 bis V-09 und V-19 |
| 10.09.2026 | M13 | Schritte 1 und 2 (Vorbereitung) | Performance-Bericht als Vorlage mit allen Messgrößen aus 2.17 Schritt 4 (docs/betrieb/performance-bericht.md, Werte ausstehend); Deployment-Test-Protokoll T1 bis T14 mit Erwartung, Vorbereitung im Repository und Nachweisfeld (docs/betrieb/deployment-test.md, alle ausstehend); Korpusgenerator mit `--scan-share` für Lauf 2 (100 Prozent Scan); Latenzsonde auf die Endpunkte aus 2.17 erweitert (Objektansicht, Review-Liste, Detail, Vorschaubild, Eigentümersuche, Suche, Berichte) mit Statuszählern je Endpunkt und Kennzeichen, ob alle Endpunkte erreichbar waren; die Entscheidung (POST) misst weiterhin der Testclient | Sonde: Endpunkt ohne erfolgreiche Antwort setzt ok auf false; Läufe auf dem VPS ausstehend (V-01, V-08, F18, F27) |
| 10.09.2026 | M11, M12 | Korrektur CI | Der Image-Build für das Ziel web brach nach M11 ab: openpyxl fehlte im Web-Venv, weil apps.lists die Excel- und PDF-Module beim App-Start lädt. Prüfung mit simuliertem Web-Venv über alle Ansichten, Tasks und Kommandos: der Web-Prozess führt auch rapidfuzz, reportlab, python-docx, pydantic, Pillow, requests und den Google-Drive-Client aus (Nachforderung bei Freigabe, OAuth-Rückruf, Drive-Verwaltungsseite, Suche, Importzuordnung, Protokollexport). Neues Extra `web` in pyproject.toml; Lockfiles web, worker und dev neu erzeugt (bestehende Versionen unverändert, nur Ergänzungen); `manage.py check` im Web-Build als Importprüfung aller Ansichten; docs/architektur/image.md fortgeschrieben | Simulation ohne Fehlstelle (strikter Lauf: alle Importe der Web-Seite im Web-Lockfile, verbleibende optionale Importe Dritter fangen den Fehler selbst ab); CI-Lauf 34519975229 grün (Tests, Migrationen vorwärts und rückwärts, Grep, Image-Build web und worker); die Läufe zu M11 (4b43d1b) und M12 (e1b659d) waren am Image-Build gescheitert |
| 10.09.2026 | M12 | Schritte 1 bis 4 (Code) | Suche in src/apps/search (H 7.1): Filter Eigentümer, Mieter, Einheit, Objekt, Zeitraum (Jahr, von, bis), Dokumentunterart, Hauptordner, Status, Verwaltungsart und Volltext; Einheit über die normalisierte Bezeichnung (WE 14 und WE14 treffen dieselbe Einheit), Eigentümer und Mieter mit Präfixsuche und unscharfer Suche ab weniger als drei Treffern, Volltext auf document_pages.text_content im booleschen Modus mit ODER-verknüpften Umlautvarianten, Mindestlänge der Suchwörter aus dem Serverparameter (Anzeige in der Oberfläche), bester Seitentreffer je Dokument mit Textausschnitt, Ergebnisse nach Art gruppiert (Dokumente mit Ablage, Akte, Seitenbereich, Drive-Link und offenem Fall; Eigentümer mit Zuordnungen; Einheiten mit Eigentümern heute; Mieter; Objekte), Rechte serverseitig (Kategorie 05 nur mit owner_files.read, 04 nur mit tenant_files.read), Log ohne Suchtext, Schnittstelle SearchIndex.query, Prüfabfrage IBAN im Index; Reporting in src/apps/reporting (H 7.3, D 12.4): Kennzahlen je Objekt (Anteil 06 brutto aus dem letzten Lauf, aktuell und bereinigt; Vollständigkeitsstatus und fehlende Punkte je Gruppe; offene Fälle je Fallart mit Median und Maximum des Alters in Arbeitstagen und Warnung ab reports.review_age_warning_days; Trefferquote System je Stufe; Eigentümerakten mit leeren und Sonderakten; KI-Kosten gesamt, Monat und Anteil Stufe 3; Importstand), Objektübersicht als Tabelle mit CSV-Export (Semikolon, deutsche Dezimaltrennung) und Objektbericht mit offenen Punkten und Fällen; Statusseite vervollständigt nach G 9.3 (Review, Google-Token, Speicherbelegung je Bereich, Konfiguration mit Hinweis auf leere Aufbewahrungsfristen, Alarmbedingungen, Aktionen Sweeper, Google-Verbindung, Listen über die Objektansicht); Alarmierung per E-Mail nach G 9.5 in src/apps/status/alerts.py (Sicherung, Token, Heartbeat bei offener Queue, Platz, Fallback über eine Stunde, Monatsbudget 80 Prozent, TLS-Zertifikat unter 14 Tagen, Dateianzahl-Abweichung des Abgleichs; nur bei ALERTS_ENABLED und Empfänger, höchstens einmal je sechs Stunden, Entwarnung; Beat alle fünf Minuten); Navigation Suche und Berichte | 374 Tests grün (WE 14 und WE14 dieselbe Einheit; Umlautvarianten Müller, Mueller, Präfix; boolescher Ausdruck mit kurzen Wörtern; Volltexttreffer mit Seitenangabe und Ausschnitt, zwei Wörter, kein Treffer, IBAN-Prüfabfrage 0; Filterkombination mit offenem Fall, Rolle ohne owner_files.read sieht keine Eigentümerakten, Jahr ohne Treffer; KPI brutto, aktuell und bereinigt nachrechenbar, Review-Alter mit Warnung, Arbeitstage, CSV mit Kopf und Zeile; Statusseite mit allen Abschnitten unter zwei Sekunden, Berichte, CSV-Export, Objektbericht, Suchseite; Alarmierung ohne Freigabe stumm, mit Freigabe eine Mail, Wiederholsperre, Entwarnung, Zertifikatsprüfung ohne Domain) |
| 10.09.2026 | M12 | offen | Statusseite unter zwei Sekunden bei laufender Pipeline im Test-Compose (V-01); Alarmtest mit echtem SMTP-Zugang (F27); Suchlatenz p95 im Performance-Test (M13); Mindestlänge der Suchwörter auf dem Server 2 (docker/db), in der Entwicklungsdatenbank 3; Zielwerte der Kennzahlen nach F22 | |
| 10.09.2026 | M11 | Schritte 1 bis 8 (Code) | Listen in src/apps/lists: Datensatzbildung nach H 5.1 (eine Zeile je Zuordnung, Blätter Aktuell und Historie, Einheiten ohne Zuordnung als unvollständig, Sortierung Einheitennummer, Bezeichnung, Nachname), Spalten wortgetreu aus lists.owner_columns und lists.tenant_columns mit nicht entfernbarem Mindestumfang (Kaution als zwei Spalten, Vorschlag F21), Status und Quelle je Datensatz nach H 5.5 (Lücke vor Vorschlag vor bestätigt; Quelldokument, Importdatei oder manuell, Zusatz u. a. bei weiteren Herkunftsdokumenten), optionales verstecktes Blatt Herkunft (lists.include_provenance_sheet); Excel nach H 5.3 (openpyxl: Dokumentkopf Zeilen 1 bis 3, Tabellenobjekt mit Filter ab Zeile 5, Kopf in HVM-Orange, freeze_panes A6, Beträge mit zwei Nachkommastellen, echte Datumswerte, PLZ als Text, Spaltenbreiten, Zeilenumbruch, Dokumenteigenschaften, Markierungszeile bei leerem Blatt); PDF A4 quer im HVM-CI nach H 5.4 (Kennlinie, Logo, Kopf mit Objekt, Verwaltungsart, Stand und Seite X von Y über zwei Durchläufe, Abschnitte Aktuell, Historie, Offene Punkte als Tabellen mit wiederholtem Kopf, alternierenden Zeilen und Linien, Schrift aus lists.pdf_table_font_pt, Fußzeile mit Pflichtangaben); Blatt Offene Punkte aus der Requirement Engine (M10); IBAN-, Konto- und Ausweisprüfung über alle Zellwerte und den PDF-Text vor der Veröffentlichung (Treffer: Abbruch, Dateien gelöscht, list_generations failed, Fall move_proposal list_iban_found); publish_list nach F 7.2 (drive_nodes list_file, Hauptordner 05 beziehungsweise 04, Sollname aus lists.*_name_pattern mit Objektnummer in der Schreibweise des Objektordners, neue Revision derselben Datei, skipped_unchanged bei gleicher Prüfsumme, gelöscht oder Papierkorb ergibt neue Datei mit alter Zeile missing oder trashed, verschoben oder umbenannt wird zurückgeführt oder als Fall gemeldet, Wiederaufnahme ohne Datenbankzeile, Prüfsummenvergleich); Auslöser nach Lauf (nicht im Dry-Run), Review-Bestätigung (entprellt über lists.debounce_seconds), Import-Übernahme, Stammdatenänderung und manuell, Sperre je Objekt, Job in Queue lists mit Wiederholung, lokale Ablage unter DATA_DIR/lists bei Drive-Fehler mit Fall list_publish_failed; Oberfläche Listen je Objekt (Stand je Format, Verlauf, Knopf Listen jetzt erzeugen, Download) | 386 Tests grün (zwei Läufe ergeben je Format genau eine Datei im Hauptordner 05, zweite Generation skipped_unchanged mit gleicher Datei, nach Datenänderung neue Revision derselben ID, gemeinsame Eigentümer; Historie enthält beendete Zuordnungen, Blatt Offene Punkte aus M10; vollständige IBAN in einer Bemerkung bricht ab mit Fall, ohne Datei in Drive und lokal; Excel-Formatierung mit Tabellenobjekt, fixierten Zeilen, orangefarbenem Kopf, PLZ als Text, Beträgen und Datumsformaten; Szenarien Papierkorb, endgültig gelöscht, verschoben, umbenannt; Rendering mit langen Werten, Logo, Seitenzahl, Pflichtangaben; Mieterliste mit Gesamtmiete, Kaution, Leerstand; Entprellung und Job mit Automatikschalter) |
| 10.09.2026 | M11 | offen | Sichtprüfung von Excel und PDF durch den Auftraggeber (Spaltenbreiten, Schrift 8 pt, A-35); Schreibweise NNN im Dateinamen nach F4 (derzeit führende Ziffern des Objektordners, sonst Objektnummer); Rückführung verschobener Dateien nach F24 (Vorgabe lists.restore_position true); Erscheinen der Listen in Drive nach einer Review-Bestätigung innerhalb der Entprellzeit ist im Test-Compose zu prüfen (V-01) | |
| 10.09.2026 | M10 | Schritte 1 bis 7 (Code) | Requirement Engine und Nachforderung in src/apps/requirements: Prüfkatalog als Seed (db/seeds/completeness_checks.json, 15 WEG-Prüfpunkte wortgetreu aus CR 12 mit Gruppe, Nachweisunterarten und Sperrwirkung; 9 Punkte des Mietkatalogs als Vorschlag nach H 3.6; Basis 01 bis 04 erst nach F10), Textbausteine als Seed (db/seeds/request_text_blocks.json, Vorschläge mit Platzhaltern, Freigabe nach F23, im Admin geänderte Bausteine werden vom Seed nicht überschrieben); Bewerter als reine Funktionen über einem einmal geladenen Datenbestand, Zeitraumlogik nach H 3.3 (Übernahmezeitraum aus Objekt oder Vorgabe completeness.default_period_years, Wirtschaftsjahr ab Startmonat, Planjahr im letzten Quartal, Fälligkeit der Einzelabrechnung nach completeness.statement_expected_after, vorläufig bei Stichtag in der Zukunft), Negativnachweis-Muster für SEPA, Sonderumlagen, Zahlungsvereinbarungen und Mahnverfahren mit manueller Übersteuerung (Pflichtgrund, Audit, überlebt den Lauf), Folgefehler bei Einheiten ohne Eigentümer, Upsert auf position_key (idempotent, weggefallene Positionen ohne Übersteuerung entfallen), Zusammenfassung je Einheit und Objekt mit completeness_ratio und Schwellen, Ausgabe Offene Punkte nach H 3.4; Auslöser nach Verarbeitungslauf, Review-Entscheidung (entprellt über die Hooks aus M7), Import-Übernahme, Stammdatenänderung, manuell und nächtlich 02:30 für Objekte in Übernahme; HVM-CI nach src/hvm_ci portiert (Kennlinie, Hausumriss, Falz- und Lochmarken, Logo, Absenderzeile, Anschriftfeld, Infoblock, Betreff, Unterschriftsblock, Fußzeile mit Pflichtangaben, Folgeseite, Listenkopf quer, Wasserzeichen ENTWURF; Logo im Repository, Unterschriftsbild nur aus dem Betrieb); Nachforderungsgenerator nach H 4 (document_requests mit Version, Snapshot der Positionen, Frist, Bausteinversionen, PDF mit ReportLab und DOCX mit python-docx aus denselben Daten, Anlage ab requests.attachment_threshold, Statuskette draft, reviewed, approved, sent, withdrawn, Freigabe nur mit Recht demands.approve und erneuter Anmeldung, Datum gleich Freigabedatum, content_hash eingefroren, Versandvermerk setzt angeforderte Frist in die Offenen Punkte, Erinnerung als neue Version); Oberfläche Vollständigkeit je Objekt (Bewertung anstoßen, Einheitenstatus, Offene Punkte mit Übersteuerung und Nachforderungskennzeichen, alle Positionen) und Nachforderungen (Entwurf, Empfänger und Frist, Prüfung, Freigabe, Versandvermerk, Zurückziehen, Download); Migration requirements 0002 (reversibel), acht Audit-Aktionen, ein Konfigurationsschlüssel (117) | 360 Tests grün (Katalog wortgetreu mit 15 WEG-Punkten und 9 Mietpunkten; Zeitraumlogik: Stichtag 01.07.2026, Kalenderjahr, drei Jahre ergibt 2023 bis 2026, Fälligkeit 30.06., Wirtschaftsjahr ab Juli, Planjahr im letzten Quartal, vorläufig in der Zukunft; Bewertung aller 15 Prüfpunkte mit missing, partial, fulfilled, not_applicable und Folgefehler, Abrechnung 2025 vor dem 30.06.2026 noch nicht fällig, Alteigentümer außerhalb des Zeitraums ohne Positionen, Zusammenfassung mit Sperrwirkung, Offene Punkte, Audit nur beim manuellen Lauf; Idempotenz mit gleichen Positionen und neuer last_evaluated_at, manuelle Übersteuerung mit Pflichtgrund, Ausschluss aus der Nachforderung, weggefallene Zuordnung räumt Positionen; Eigentümerwechsel mit und ohne Nachweis, beide Eigentümer im Wechseljahr anwendbar; Mietkatalog mit Leerstand, Konditionen, Kaution, Anpassung, SEPA; 100 Einheiten mal vier Jahre unter 5 s, zweiter Lauf ohne Änderung; Nachforderung: Entwurf ohne Frist mit Wasserzeichen, Hinweiszeile und Platzhalter [FRIST] kann nicht auf geprüft wechseln, Freigabe erzeugt PDF ohne Wasserzeichen und DOCX mit identischem Positionsbestand, Datum gleich Freigabedatum, Audit mit Freigebendem, Textbausteinänderung ändert das Schreiben nicht, Logo auf Seite 1, Pflichtangaben in der Fußzeile, Versandvermerk mit Frist in den Offenen Punkten, Zurückziehen, Erinnerung als Version 2, Anlage ab Schwelle mit Tabelle im DOCX, kein Schreiben ohne offene Punkte; Oberfläche mit Bewertung, Übersteuerung, Entwurf, Prüfung, Download und Umleitung zur erneuten Anmeldung bei der Freigabe) |
| 10.09.2026 | M10 | offen | Sichtprüfung des Layouts (PDF und DOCX) durch den Auftraggeber; Freigabe der Seed-Texte und der DOCX-Gestaltung (F23, hier erzeugt aus den CI-Werten statt aus einer Vorlagendatei); Unterschriftsbild als Secret hvm_signature.jpg auf dem Server (nicht im Repository), bis dahin bleibt der Platz in der freigegebenen Fassung frei; Drive-Upload der freigegebenen Fassung nach F23 (Zielordner offen); Kandidaten-Prüfpunkte 01 bis 04 nach F10; Parameter nach F22 (Startwerte als ANNAHME); Freigaberolle Admin (ANNAHME); Hochladen einer extern geänderten Fassung (H 4.4) nicht gebaut | |
| 10.09.2026 | M9 | Schritte 1 bis 6 (Code) | Import Teil 2 in src/apps/imports: Profil pdf_digital_table (pdfplumber: Tabellen über Linien, sonst Wörter mit Koordinaten; Zeilen über die Oberkante y, Spalten über Lücken der x-Verteilung aller Seiten; Titel- und Fußzeilen bleiben bei der Spaltenbildung außen vor; wiederholte Kopfzeilen weiterer Seiten entfallen, Fußzeilen mit Seitenzahl stehen im Protokoll der Zuordnung), Profil pdf_scan_ocr (Rasterung mit pypdfium2 bei 300 dpi, Tesseract als TSV mit Wortkoordinaten und Wortkonfidenz, Zellkonfidenz als Mittelwert; Zellen unter import.ocr_cell_confidence_min machen die Zeile unsicher mit Grund low_ocr_confidence und Feldliste, nichts wird verworfen; auch Bilddateien png, jpg, tif), Profil domus_export als manuell wählbarer Platzhalter ohne automatische Erkennung (F11), generic_table liest auch PDF; Mieterlisten nach H 6.9: ein Mietverhältnis je Quellzeile, Mitmieter derselben Zeile teilen es (Rollen tenant und co_tenant), Kaltmiete als Zahl außer beim Immoware24-Export (Bedeutung offen, F11), Kautionsart über Synonymliste, unbekannt bleibt unknown; Auslösemodell B-34: Listenerkennung in der Seitenanalyse auch für digitale PDF, Art der Liste (owner_list, tenant_list, mixed) aus den erkannten Feldern als Untertyp des Falls; Aktion „Import starten“ im Review Center (Quelldatei aus Arbeitsverzeichnis, Transit oder Drive, import_batches über die M3-Kette, Einlesen als Job, Fall erledigt mit Verweis auf den Import, Audit review.import_started) | 349 Tests grün (digitale PDF-Tabelle mit Gitter und wiederholter Kopfzeile über drei Seiten ergibt eine Tabelle mit 90 Zeilen und einer Kopfzeile, Fußzeilen erkannt, Profil erkannt, Kette bis in_review mit allen fünf Zielfeldern; PDF ohne Linien über Wörter mit Koordinaten ergibt Titel, Kopfzeile, 40 Zeilen und drei Fußzeilen; Scan mit nachgestellter TSV: zwei Zellen unter 70 ergeben zwei unsichere Zeilen mit Spaltenangabe und Fall, übrige Zeilen sicher, rows_total gleich Summe; echte OCR eines gerasterten Gitters liest Kopfzeile und Zeilen (slow); Mieterliste ergibt drei leases mit Beträgen als Zahl, Datumsfeldern, Kautionsart und zwei Zuordnungen tenant und co_tenant auf einem Mietverhältnis; Domus nur manuell; Listenerkennung aus der Pipeline für ein digitales PDF und Import nach Bestätigung mit erledigtem Fall, eingelesenem Import und Audit) |
| 10.09.2026 | M9 | offen | Profil domus_export braucht eine Beispieldatei (F11); externe KI zur Strukturierung gescannter Listen nur nach F19 (nicht gebaut); Listenerkennung gescannter PDF in der Pipeline folgt, sobald OCR-Wortkoordinaten im Cache liegen (bis dahin Upload im Import mit Profil pdf_scan_ocr); verbundene Zellen über mehrere Spalten werden bei der Wortmethode einer Spalte zugeordnet (Korrektur im Review); Neubewertung von Listen und Vollständigkeit nach Übernahme bleibt Hook für M11 und M12 | |
| 10.09.2026 | M8 | Schritte 1 bis 8 (Code) | Stufe 3 in src/apps/ai: Anbieterschnittstelle mit Konfiguration ausschließlich in app_settings (ai.provider_order, ai.providers.<p> mit enabled, model, endpoint, region, timeout_s, max_attempts, cost_limit_eur_per_object; ai.monthly_budget_eur, ai.max_input_tokens, ai.chars_per_token, ai.wall_budget_s, ai.store_masked_prompts, ai.price_list, ai.reclassify_enabled), API-Schlüssel nur aus Docker Secrets, Basis-URL aus .env, ein Anbieter ist erst nach enabled aktiv; OpenAIProvider und AnthropicProvider mit striktem Antwortschema (Codes nach B-12), FakeClassificationProvider mit Skripten (Timeout, 5xx, 429, 4xx, Schemaverletzung, kein JSON); Request nach E 4.1 mit Datenminimierung nach Ü15 (Kontextfelder ohne Personennamen, nur Einheitenpräfixe, Taxonomie mit Codes, Dateiname maskiert), Auszug aus dem maskierten Seitentext (erste Seiten plus letzte, Kopf- und Fußzeilen entdoppelt, Tokenbudget); zweite Maskierungsprüfung unmittelbar vor dem Senden (blocked_by_mask_check); Sperren nach F19 (Ausweiskopie, Kategorie 01) in classify_ai mit Status skipped und Grund im Fall; Router mit Primär bis max_attempts, Fallback mit identischem Request, Reparaturversuch bei Schemaverletzung, Circuit Breaker je Anbieter im Cache, Kostenlimit je Anbieter und Objekt gegen SUM(cost_eur) mit Status budget_blocked, Gesamtzeitbudget je Dokument; ai_calls je Versuch mit Status, prompt_hash, Zeichen, Token, Kosten aus versionierter Preisliste, fallback_used, fallback_of_call_id, response_summary (kein Prompttext, maskierter Prompt nur bei ai.store_masked_prompts); Task classify_ai in der Kette classify, classify_ai, decide; Konfidenz nach Stufe 3 nach E 7.2 (Übereinstimmung hebt, Übernahme ab threshold_stage3_override mit decided_by stage3, Widerspruch ohne Wirkung), finale Klassifikationszeile Stufe 3 mit Anbieter; Ausfall beider Anbieter und Kostenlimit ergeben 06/01_Unklar mit Fall und Grund (B-28); Stichprobe classification.ai_sample_pct als Fall move_proposal mit Untertyp ai_sample ohne Ablagestopp; Kommando ai_reclassify (Nachklassifikation nach Ausfall oder Kostenlimit, --object, --dry-run, --force); Statusseite Abschnitt Stufe 3 (Anbieter, Modell, Region, Monatskosten, Budget, Kosten je Objekt, letzte Fehler); Maskierungsmuster Ausweisnummer erkennt auch „Personalausweis Nr.“; zwei Konfigurationsschlüssel (116) | 342 Tests grün (Provider-Wechsel in vier Szenarien: Timeout des Primären, 5xx, Schemaverletzung mit Reparatur beim Primären, Ausfall beider mit vier ai_calls und den Statuswerten timeout, provider_error, schema_error, rate_limited; fallback_used und fallback_of_call_id gesetzt, Fallback-Ergebnis gegen dasselbe Schema; Kostenlimit stoppt den zweiten Aufruf; Maskierungssperre vor dem Senden; Circuit Breaker öffnet nach drei Fehlern; Datenminimierung ohne Namen aus owners im Request; Taxonomie aus dem Katalog; Router ohne Freigabe liefert disabled; Kette: KI entscheidet mit Stichprobenfall und Ablage, Ausfall beider ergibt 06/01 mit Grund KI nicht verfügbar und Nachklassifikation, Übereinstimmung hebt die Konfidenz, Ausweiskopie ohne Aufruf, Statusseite) |
| 10.09.2026 | M8 | offen | Testlauf gegen beide echten Anbieter mit synthetischen Dokumenten wartet auf AVV, EU-Region, Opt-out und Schlüssel (V-10 bis V-13); Preisliste ai.price_list ist leer (Version nicht-festgelegt), bis der Auftraggeber die Listenpreise einträgt (Kosten werden bis dahin mit 0 berechnet); Monatsbudget ai.monthly_budget_eur wird angezeigt, nicht erzwungen; zeitgesteuerter Nachklassifikationslauf und Umfang der Stichprobe nach F17, bis dahin manuell; Aufbewahrung gespeicherter maskierter Prompts über retention_policies folgt mit dem Löschlauf (B-07) | |
| 10.09.2026 | M7 | Schritte 1 bis 10 (Code) | Review Center in src/apps/review: Arbeitsliste aus review_cases mit Filtern (Objekt, Fallart, Unterfall, Status, Bearbeiter, Zielbereich, Konfidenzband, Zeitraum, Jahr, Gruppen, Volltext, zurückgestellte, eigene), Zählern aus gruppierter Abfrage, Keyset-Paginierung mit 50 Zeilen über ix_review_queue, gespeicherte Sichten je Nutzer und geteilt; Detailansicht in drei Spalten (Fall mit Klassifikationsverlauf, Entitäten, Kandidaten, Verknüpfungen, Protokoll; Seitenvorschau mit Miniaturen, Blättern, Textauszug; Entscheidungsformular mit Zielbereich 01 bis 06, Eigentümersuche mit Vorschlägen objektbezogen zuerst, Einheitenliste, Zuordnung vorbelegt, ausdrückliche Auswahl Einheit oder Eigentümer unbekannt (B-30), neue Zuordnung mit Eigentumsbeginn, Jahr mit Vorschlag aus den Entitäten); Warnung bei Widerspruch zur Stichtagsabfrage, mit der Entscheidung protokolliert; Aktionen Bestätigen, Umklassifizieren, Dublette, Aufteilen, Zurückstellen mit snoozed_until, Verwerfen mit Pflichtgrund (Objektfälle nur Admin, B-17), Zuweisen, Wiedereröffnen, In anderes Objekt übernehmen, jede in einer Transaktion mit review_decisions (before_state, after_state, features_snapshot, text_hashes, Label-Spalten, system_was_correct), review_cases, audit_events und Trainingsdatum; Klassifikation Stufe 4 human; Drive-Verschiebung nur als Job; Massenbearbeitung über batch_key (Objekt, Masterdokument oder Lauf, Kategorie, Unterordner, Unterart, Jahr) und freie Auswahl mit Vorschau ohne Schreibwirkung (Zielpfad aus Benennungsfunktion und drive_nodes, Ordner vorhanden, Warnungen, Konfliktzeilen ausgenommen), Sammelfelder, Zeilenkorrektur, Entwurfsspeicher in der Sitzung, Ausführung als Hintergrundjob mit is_bulk und bulk_key, Fortschritt und Ergebnisseite, eine Listenerzeugung je Gruppe (Entprellung, Hooks für M11 und M12); Segmente von Gesamtdokumenten als Fälle in einer Gruppe (classification.segments_require_review); ein offener Fall je Seitenbereich, weitere Gründe in context.reasons; Serienmodus und Tastaturbelegung nach H 2.6 (eigenes Skript ohne externe Quellen), alle Aktionen auch per Schaltfläche; Latenzsonde tests/performance/latency_probe.py; drei Konfigurationsschlüssel (114) | 329 Tests grün (Liste mit Filtern, Zählern, Keyset-Paginierung und Sichten; Bestätigen mit Kandidat: Stufe 4, Verknüpfung confirmed, Entscheidung, Trainingsdatum, Audit, Job statt Drive-Aufruf, danach filed; historische Zuordnung mit Warnung im Protokoll; Pflichtfelder und neue Zuordnung; Zurückstellen, Zuweisen, Verwerfen nur Admin für Objektfälle, Wiedereröffnen; Dublette, Aufteilen mit Überschneidungsprüfung, Übernahme; Seitenbild ohne Sitzung; Eigentümersuche; Entprellung; Recording-Adapter ohne Schreibaufrufe in Anfragen; Ende-zu-Ende 40 Segmente mit 38 eindeutigen, 2 Kandidaten und 1 unbekannter Einheit: Vorschau ohne Schreibwirkung, Pfade gleich Benennungsfunktion, 40 Entscheidungen in einer Aktion, rote Zeile bleibt offen, eine Listenerzeugung, Audit vollständig; Sammelaktion blockiert bei Konflikt; Latenz p95 unter 2 s im Testclient) |
| 10.09.2026 | M7 | offen | Lastmessung mit fünf Nutzern bei laufender Pipeline im Test-Compose (A-29, B-35) und Ende-zu-Ende-Test mit Browser warten auf den Server (V-01); Rechte nach F15 (Standard: alle sehen alle Objekte); Auswertung je Bearbeiter (H 2.8) folgt mit dem Reporting (M12); Vorschau-Endpunkt als HTMX-Partial ersetzt durch serverseitige Vorschauseite (keine externen Skriptquellen) | |
| 10.09.2026 | M6 | Schritte 1 bis 13 (Code) | Klassifikation in src/apps/classification: Regelwerk als JSON-Seed (db/seeds/rules, 54 Regeln mit Codes, Geltungsbereich je Verwaltungsart, UND-Bedingungen, any_of, Negativbedingungen, Beispielen; Laden in classification_rules mit definition und Version), Kontextaufbau je Dokument (Dateiname, Herkunftsordner, erste Seiten plus letzte, Entitäten, Zeitbezug mit Präzedenz Abrechnungsjahr vor Zeitraum vor Forderungszeitraum vor Dokumentdatum, eigene Firma, Vertragspartner), Konfidenzmodell nach E 7.2 mit dem Schlüsselsatz classification.* (B-08) und NER-Stütze in der Kaltstartphase, lokaler Klassifikator TF-IDF plus logistische Regression (Modell A Kategorie, Modell B Unterordner und Unterart in 05, Artefakte unter models/<version>, Kaltstart nach B-27, synthetische Beispiele, Training mit Kreuzvalidierung und Makro-F1, Aktivierung nach Toleranzregel, Rollback, Beat-Task nächtlich 01:30, Kommando classifier), Entscheidungsalgorithmus E 6.1 mit Schritten 0, 0b, A, 1 bis 5, Konfliktregel Gesamtdokument vor Einzelbezug, sechs Zusatzprüfungen mit den Fällen a bis g und Sonderregel Eigentumsnachweise nach Parteien, Entscheidungstabelle B-09 (owner_candidates, missing_metadata, move_proposal, unclear nur ohne fachlichen Grund), Quellenweiche B-10 (Bestandsdatei unter Schwelle bleibt liegen, sichere 06-Entscheidungen auch für Bestand), Segmentierung von Gesamtabrechnungen, Gesamtwirtschaftsplänen und Protokollen in document_owner_links mit Seitenbereich, Eigentümerakten je Eigentümergruppe über die Benennungsfunktion, Sonderakten Unbekannte_WE und Unzugeordnet nur mit Fall; Tasks classify, decide, file_to_drive (Elternwechsel für Bestand, Upload mit Idempotenzprüfung, Rücklesen, Sperre je Objekt, Dubletten physisch nach 03_Dubletten), Pipeline-Dry-Run ohne Drive-Schreibzugriff mit Plan je Dokument, Übernahme in anderes Objekt in Grundform (B-29), KPI Anteil 06 brutto und bereinigt (B-31); Oberfläche: Dry-Run-Start, Plan und Klassifikation je Dokument, Verknüpfungen und Fälle in der Dokumentansicht, Kaltstartphase auf der Statusseite; Testkatalog E 8 als JSON-Fixture (32 Dokumente, vier Testobjekte, synthetische Stammdaten) | 316 Tests grün (Regelbeispiele je Regel, Codes statt Ordnernamen, Konflikt harter Treffer, Konfidenzmodell; Testkatalog vollständig: Kategorie, Unterordner, Unterart, Fallart, Eigentümerakten, T03 ein Segment, T10 zwölf Segmente ohne Drive-Duplikate, T18 und T19 relational zu beiden Akten, T20 Alteigentümer, T27 Mieterakte, T28 Vorschlag 08_Korrespondenz, T30 Vorschlag Objekt 631, T23 Kandidatenliste ohne Raten, Prüfabfrage 06 ohne Fall leer, KPI je Lauf; Bestandsdatei unter Schwelle bleibt liegen; Bestandsdatei als Dublette nach 03_Dubletten; Pflichtmetadatum fehlt; Unbekannte_WE und Unzugeordnet; Dry-Run nur Lesezugriffe und Ausführung ohne erneute OCR; Übernahme in anderes Objekt; Kaltstart und Training) |
| 10.09.2026 | M6 | offen | Stufe 3 (classify_ai) folgt in M8, bis dahin entscheidet decide direkt; physische Zweitablage von Segmenten bei documents.duplicate_owner_documents_in_drive true und Backfill folgen mit dem ersten Bedarf (Vorgabe false); Google-Dokumente werden bis zum Drive-Export in die Prüfung gegeben; Regeln als JSON statt YAML (keine zusätzliche Abhängigkeit, gleiche Struktur); Testlauf im Test-Compose bis filed wartet auf den Server (V-01) | |
| 10.09.2026 | M5 | Schritte 1 bis 12 (Code) | Pipeline in src/apps/pipeline: Jobs mit Idempotenzschlüssel nach B-03 (Datenbank ist Quelle der Wahrheit, Reservierung per UPDATE mit Statusbedingung, Heartbeat, Ereignisse je Übergang, Wiederholung mit Backoff, nach max_attempts failed mit Review-Fall job_failed); Versandmodus JOB_DISPATCH und lokaler Runner für Tests und Messlauf; Ingest für Uploads (Transitbereich, documents registered, Lauf incremental je Objekt); hash mit Download nach work/<sha256>, Größenlimit, Dublettenprüfung je Objekt vor der OCR mit Fall 03_Dubletten (T29), gleicher Hash in anderem Objekt als Hinweis; Formatweiche (PDF, Bilder über img2pdf, Office ohne OCR, Google-Dokumente und Unbekanntes in die Prüfung), Seitenanalyse mit Textebenen-Vorprüfung nach A-11 (Zeichen, alphanumerischer Anteil, seitenfüllendes Bild), Listen in Office-Dateien als import_candidate (B-34); Chunking als Konstruktion (B-04) mit ocr:<objekt>:<sha256>:<block>, Wiederverwendung gecachter Seiten, Merge unter Zeilensperre nach dem letzten Block; OCR je Block mit ocrmypdf (skip-text, jobs 1, sidecar, OMP_THREAD_LIMIT 1, nice, ionice); Maskierung vor jeder Persistierung (Cache, document_pages, document_entities nur mit letzten vier Stellen und HMAC); Seitenbilder JPEG 1.200 px, Auslieferung nur rechtegeprüft; Entitäten (Einheitenvarianten, Beträge mit Kontext, Datum, Zeitraum, Objektmarker eigen und fremd, Personen über Kontext und rapidfuzz, IBAN über HMAC gegen Stammdaten); Sweeper (Beat jede Minute, Worker-Start, Statusseite) mit Stale-Fristen je Jobtyp, verwaisten work/-Verzeichnissen und Objektserialität (processing.max_parallel_objects, Warteposition); object_progress aus Zählabfragen; Lauf-KPIs; Oberfläche: Upload mehrerer Dateien, Dokumentliste mit Fortschritt und Läufen, Dokumentansicht mit maskierten Seitentexten, Entitäten, Jobs und Seitenbildern, Statusseite mit Queues, Läufen und Zeile je Objekt; Korpusgenerator mit Gesamtabrechnungen und Segmentliste; Kommando perf_run mit Messprotokoll; CI installiert Tesseract und Ghostscript; vier neue Konfigurationsschlüssel (107) | 246 Tests grün (digitale PDF endet ohne OCR-Job in ocr_done; 5 Scanseiten in drei Blöcken mit Seitentexten in Originalreihenfolge; Dublette vor der OCR mit Fall 03; Abbruch während Block 1 mit veraltetem Heartbeat, Sweeper, Wiederaufnahme ohne doppelte Seiten und mit Wiederverwendung der gesicherten Seite; Sweeper setzt nach max_attempts auf failed mit Fall; Objektsperre: zweites Objekt wartet pending mit Position 1 und startet nach Laufende; Statistikzeile gleich Zählabfrage; Dokument-IBAN gleich Stammdaten-IBAN ohne Klartext in Seiten, Cache, Treffern und Entitäten; Deckblatt, Zeichensalat und Digitalseite richtig erkannt; Reservierung doppelt zugestellt ohne Wirkung; echte OCR mit Tesseract; Oberfläche; Messlauf mit Generator) |
| 10.09.2026 | M5 | offen | Schritt 13 (Messlauf 1.000 Seiten auf dem VPS, docs/betrieb/performance-bericht.md) und Deployment-Test T5 warten auf den Serverzugang (V-01); Google-Dokumente werden bis M6 in die Prüfung gegeben (Export folgt); Wiederaufnahme der Drive-Session-URI aus dem Payload folgt mit dem ersten Upload nach Drive (M11); Prüfpunkt P1 kann erst mit den VPS-Messwerten stattfinden | |
| 10.09.2026 | M2 | Nachtrag | Eigenschaftstest der Benennung fand einen Randfall: Einheitentoken länger als die Höchstlänge ergab einen Ordnernamen ohne Trennzeichen; `_fit_length` kürzt jetzt das Token und behält Trennzeichen und mindestens drei Zeichen des Namensteils | Eigenschaftstest dreimal mit 300 Beispielen grün |
| 10.09.2026 | Betrieb | GitHub-Anbindung | Workflow `Deploy` (manuell, SSH mit Schlüssel aus Secrets, Aktion deploy oder rollback), Server-Skript scripts/deploy_remote.sh als erzwungenes Kommando, Anleitung docs/betrieb/github-deploy.md (Deploy Key für Git, Aktionsschlüssel für den Workflow, Secrets, Probelauf, Fehlerbilder); Einrichtung der Schlüssel durch den Auftraggeber offen | |
| 10.09.2026 | M4 | Schritte 1 bis 9 (Code) | Drive-Adapter in src/apps/drive: Schnittstelle nach F 2.8, InMemoryDriveAdapter (Papierkorb, Verknüpfungen, Revisionen, Fehlerinjektion, Operationsprotokoll, deterministische IDs, Szenario-Lader), RecordingDriveAdapter (Idempotenznachweis), GoogleDriveAdapter mit supportsAllDrives, Felderauswahl, Escaping, Paginierung, Backoff mit Retry-After, Ratenbegrenzung, Metriken und Fehlerabbildung; OAuth nach B-16 (offline, consent, include_granted_scopes false, state, PKCE, Kontoprüfung gegen DRIVE_ACCOUNT_EMAIL, Tokens mit TOKEN_KEY verschlüsselt, Refresh mit Redis-Sperre, invalid_grant setzt revoked, Audit drive.authorize und drive.token_refresh); Beat-Tasks stündlicher Lesetest und täglicher erzwungener Refresh; Ordnerabgleich nach F 4.2 mit Planungs- und Ausführungsphase, Objektordner-Erkennung mit allen Fällen, Alias-Umbenennung mit Zählung und ID-Hash vorher und nachher, Anlage 05_Eigentümerakte danach, Sonderfälle A bis H als drive_structure-Fälle, Inventur mit documents (registered, drive_md5) und Jobs discover, Dry-Run ohne Schreibzugriff, Nachbedingungen; Protokoll als JSON und Excel, Sammelfassung; Befehle drive_reconcile, drive_undo_rename, drive_export_protocol, drive_oauth_proof; Eigentümerakten-Ordner mit zwölf drive_nodes und Cache; Verwaltungsseite Google Drive (Verbindung, Wurzelauflösung, Nachweiskette), Abgleiche und Planliste in der Objektansicht, Statusseite zeigt Token; Live-Test-Gerüst; docs/betrieb/google-oauth.md | 224 Tests grün (drei Testobjekte mit Dry-Run, Ausführung und zweitem Lauf: Dateizahl und ID-Hash gleich, no_changes, nur Lesezugriffe; Objektordner-Erkennung 0631 gleich 631 als Doppelfall, 6230 kein Treffer, Papierkorb-Treffer ohne Anlage, 700 wird angelegt, Archiv ignoriert; Sonderfälle A bis H; Zählabweichung stoppt Schreibaktionen; Backoff 403, 403, 200 mit gefälschter Uhr; OAuth-Ablehnungen; Widerruf) |
| 10.09.2026 | M4 | offen | Schritt 10 (8-Tage-Nachweis auf dem VPS) und der Live-Test gegen das Test-Wurzelverzeichnis warten auf V-05 bis V-09; Resumable-Upload mit Abbruch nach zwei Chunks und Schreibidempotenz für Uploads (B-45) folgen mit dem ersten schreibenden Anwendungsfall (Listen, M11); Wiederaufnahme der Session-URI aus processing_jobs.payload in M5 | |
| 10.09.2026 | M3 | Schritte 1 bis 10 | Importkette in src/apps/imports: Annahme mit SHA-256, Transitablage und documents-Zeile (source import); Formatprofile csv_generic (Trennzeichen und Zeichensatz erkannt), xlsx_generic (Blattwahl, verbundene Zellen, Kopfzeile über Synonymtreffer), immoware24_export (einheiten.csv und kontakte.csv, Objektfilter über Zahlenwert, Status steuert Vorschlag), generic_table; Spaltenzuordnung mit Synonymwörterbuch import.column_synonyms und Inhaltsmustern, manuelle Korrektur, Zielfeld nur einmal; Feldnormalisierung (Adresse mit Hausnummer, Telefon oder Mobil, E-Mail, Datum, Betrag, PLZ als Text, Land ISO-2, Bruch, Wahrheitswert); IBAN beim Einlesen maskiert, letzte vier Stellen und HMAC gesichert; Namens-Splitter nach H 6.4 mit Konfidenzbändern; Einheitennormalisierung; unscharfer Abgleich gegen owners (rapidfuzz, Signale, Nachname allein reicht nie, objektübergreifend nie automatisch); Konflikt mit bestehender Zuordnung mit den Optionen Wechsel, Mehrfacheigentum, Dublette; Review-Fälle import_row_uncertain; Übernahme je Zeile in eigener Transaktion mit field_provenance und Audit, Zusammenführung gleicher Personen innerhalb eines Imports, Mehrfacheigentum aus derselben Liste; Mieter aus gemischten Listen als tenants, leases (Miete lt. Export in notes) und tenant_unit_assignments; Importansicht mit Zählern, Zuordnung, Zeilen, Vorschau, gesammelter Bestätigung, Einzelentscheidung, Protokoll als Excel; Jobs in Queue io; sechs neue Konfigurationsschlüssel (103 im Katalog) | 200 Tests grün (Namens-Splitter elf Fälle, Normalisierung, Profile und Zuordnung mit synthetischen Dateien in Originalspaltenstruktur, Abgleich, Immoware24-Kette Ende zu Ende, Konflikt mit drei Optionen, IBAN nirgends im Klartext, rows_total gleich Summe, Oberfläche mit 40 Zeilen); 1.000 Zeilen Einlesen, Erkennung und Übernahme in 44 s (ANNAHME A-21 bestätigt, Grenze 120 s) |
| 10.09.2026 | M3 | offen | PDF-Profile folgen nach der OCR-Pipeline (M9); Profil Domus wartet auf eine Beispieldatei (F11); Listen und Vollständigkeit nach Übernahme werden in M11 und M12 angebunden (after_commit_hooks); Ablage der Quelldatei in Drive ab M4 | |
| 10.09.2026 | M2 | Schritte 1 bis 9 | Alle Tabellen aus Fachentwurf D plus Ergänzungsmigration als Django-Modelle in den Modulen objects, parties, documents, drive, pipeline, review, imports, requirements, lists, ai (59 Tabellen einschließlich M1 und Bibliothek); Migrationen mit Nachtrag der zyklischen Fremdschlüssel und Volltextindex; Seeds für sechs Kategorien wortgetreu (05_Eigentümerakte mit Umlaut), elf plus vier Unterordner, 57 Dokumentunterarten mit Pflichtmetadaten, sechs Aufbewahrungszeilen ohne Werte; Bezeichnerregister aus dem Code erzeugt und per Test gegen den Code geprüft; Anhang Bezeichner E zu D, Verweise in F, G, H; Benennungsfunktion build_owner_folder_name mit Konfiguration owner_file.*; Objektnummer-Erkennung 2 bis 6 Stellen; Einheitennormalisierung mit Präfix-Mapping; Stichtags- und Zeitraumabfrage; Zuordnung mit Überlappungsprüfung in der Transaktion; Datensatzstatus aus field_provenance; nächtlicher Konsistenzlauf als Beat-Task (Review-Fall data_consistency); Objektverwaltung mit Einheiten, Eigentümern (IBAN nur letzte vier Stellen und Hash), Zuordnungen, Eigentümerwechsel und Stichtagsansicht; Rechte-SQL deckt die neuen Tabellen ab | 156 Tests grün gegen MariaDB (32 Benennungsfälle in beiden Modi plus Eigenschaftstest, historische Zuordnung 03/2025 bei Wechsel 01.07.2026, 6230 ungleich 623 und 0631 gleich 631, WE 14 gleich WE14, Seeds zweiter Lauf ohne Änderung, Migrationen vorwärts und rückwärts, Grep-Prüfung leer, Register aktuell) |
| 10.09.2026 | M2 | offen | Frage F5 (Umlaut) und F6 (Präfixmodus) mit Vorschlagswerten gebaut, Umstellung per Konfiguration; Unterstrukturen 01 bis 04 leer (F10); Ergänzungen aus M2 als Vorschlag in docs/plan/entscheidungen.md | |
| 10.09.2026 | M1 | Nachtrag | CI-Fehler behoben: docker/backup war durch das Muster backup/ in .gitignore ausgeschlossen; Laufzeitmuster auf das Wurzelverzeichnis beschränkt. Image-Builds web und worker in der CI erfolgreich | |
| 10.09.2026 | M1 | Schritte 1 bis 11, 13, 14 | Repository-Gerüst (pyproject, Lockfiles je Ziel, ruff, pytest, CI), Dockerfile mit Zielen web und worker, Compose mit Variablen, Datenbank-Init und Rechte-SQL aus dem Modellregister, Django-Projekt mit Secrets aus Dateien, JSON-Logs mit Maskierung, Request-ID, Healthchecks, Login mit TOTP-Pflicht, Rollen Admin und Sachbearbeiter, Sperre nach Fehlversuchen, Nutzerverwaltung mit Step-up, Audit append-only mit Triggern, Konfigurationsregister mit 97 Schlüsseln und Admin-Formularen, Backup-Container, deploy.sh und rollback.sh, Statusseite in Grundform, Runbook-Gerüst | 48 Tests grün gegen MariaDB, Migrationen vorwärts und rückwärts (T8), Grep-Prüfung leer, Compose-Datei gültig |
| 10.09.2026 | M1 | offen | Schritt 4 (Verzeichnisse und Secrets auf dem Server), Schritt 12 (Host-Härtung) und die Deployment-Tests T1 bis T3, T7, T11 bis T14 brauchen den VPS; Image-Build braucht einen Docker-Daemon (CI-Workflow angelegt, erster Lauf nach Push) | |
| 10.09.2026 | M1 | Start | Freigabe FG-1 bis FG-3 erhalten, Entscheidungen protokolliert, Gerüst wird aufgebaut | |
| 10.09.2026 | M0 | Vorbereitung | Werkzeuge für Serverbefund und OCR-Probelauf liegen vor (docs/betrieb/m0-anleitung.md); Durchführung wartet auf SSH-Zugang (V-01) und Freigabe des Messcontainers (V-03) | Unit-Tests Rechenmodell 10 grün, Funktionstest in der Entwicklungsumgebung |
| 10.09.2026 | Entwurf | Abschluss | Architektur, Umsetzungsplan, Betriebshandbuch und Arbeitspapiere im Repository | Stil-, Fakten- und Konsistenzprüfung |

## Blockiert

- M0 auf dem VPS: SSH-Zugang für Deploy-Nutzer (V-01), Freigabe Messcontainer (V-03)
- M4 gegen das echte Drive: technisches Konto (V-05), OAuth-App (V-06), Test-Wurzelverzeichnis (V-08), Freigabe in der Admin-Console (V-09)
- M5 Messlauf auf dem VPS und Deployment-Test T5: SSH-Zugang (V-01); GitHub-Anbindung: Deploy Key und Aktionsschlüssel auf dem Server, vier Secrets im Repository (docs/betrieb/github-deploy.md)
- M8: AVV mit OpenAI und Anthropic, EU-Region, Opt-out, API-Schlüssel (V-10 bis V-13); Testlauf gegen die echten Anbieter erst danach
