# Entscheidungen des Auftraggebers

Jede Antwort des Auftraggebers wird hier mit Datum festgehalten und ist danach für den Bau bindend (Umsetzungsplan, Abschnitt 7). Nummern beziehen sich auf docs/umsetzungsplan.md.

| Datum | Nummer | Entscheidung | Wirkung |
|---|---|---|---|
| 10.09.2026 | FG-1, FG-2, FG-3 | Freigabe erteilt („weiter und auch freigabe"): Stack A mit den Auflagen aus Anhang C, Datenmodell D als Bezeichnerquelle mit Ergänzungsmigration, Meilensteinreihenfolge mit den Planänderungen | Umsetzung beginnt mit M1; M0 folgt, sobald der SSH-Zugang zum VPS besteht (V-01, V-03) |
| 10.09.2026 | FG-4, F1 bis F31 | Keine Einzelantworten. Es gelten die im Umsetzungsplan Abschnitt 4 genannten Vorschlagswerte („Ohne Antwort"), bis eine abweichende Antwort mit Nummer eingeht | Seeds, Konfiguration und Compose werden mit den Vorschlagswerten gebaut; spätere Antworten werden per Konfiguration umgesetzt, soweit im Plan so vorgesehen |
| 10.09.2026 | F1 | Umsetzung durch den beauftragten KI-Assistenten in Python und Django; Vertretung nicht benannt | Risiko R-18 bleibt offen |
| 10.09.2026 | FG-5, V-01 bis V-09 | Noch nicht erfüllt: SSH-Zugang, Messcontainer-Freigabe, DNS, technisches Konto, OAuth-App, Test-Wurzelverzeichnis | M0 und M4 verschieben sich, bis die Voraussetzungen vorliegen; alle Serverwerte bleiben Variablen |

## Offene Antworten mit Wirkung auf den Bau

Die folgenden Fragen werden mit dem Vorschlagswert gebaut und lassen sich später umstellen. Sie bleiben zur Beantwortung offen:

- F4 Objektnummer ohne Nullauffüllung, Erkennung 2 bis 6 Stellen
- F5 Ordnernamen wortgetreu mit Umlaut
- F6 Präfix der Eigentümerakten immer WE (CR-Wortlaut), Umstellung auf typabhängig per Konfiguration
- F8 Ablage bei mehrdeutiger Zuordnung nach Entscheidungstabelle docs/architektur.md 6.5
- F10 Requirement Engine und Nachforderung als minimaler Neubau, Unterstrukturen 01 bis 04 leer
- F13 OAuth-App mit den Redirect-URIs aus docs/betrieb.md 7.1 (anzulegen durch den Auftraggeber)
- F16 IBAN nur als letzte vier Stellen und Hash
- F17 Externe KI bis zur AVV-Freigabe deaktiviert
- F18 Entscheidung nach dem OCR-Probelauf in M0

## Ergänzungen aus der Umsetzung (Vorschläge des Umsetzers, gelten bis zu einer abweichenden Entscheidung)

| Datum | Meilenstein | Ergänzung | Begründung und Wirkung |
|---|---|---|---|
| 10.09.2026 | M2 | `review_cases.case_type` um `data_consistency` (Untertyp `assignment_overlap`) | Der nächtliche Konsistenzlauf meldet überlappende Zuordnungen derselben Person auf derselben Einheit; nach B-09 wäre `unclear` falsch, weil ein fachlicher Grund vorliegt. Rückbau: Wert aus CHECK und Task entfernen. |
| 10.09.2026 | M2 | Audit-Aktion `assignment.end` | Beendigung einer Zuordnung (Eigentümerwechsel) wird getrennt vom Anlegen protokolliert. |
| 10.09.2026 | M2 | Codes der 57 Dokumentunterarten (`db/seeds/document_types.json`) | CR 5 und 6 nennen Namen, keine Codes; die Codes sind ab jetzt stabil, weil Regeln, Klassifikator und KI-Schema darauf verweisen (B-12, B-21). Namen folgen dem CR-Wortlaut. Zusätzlich `sonstiges_eigentuemer` als Sammelunterart der Eigentümerakte. |
| 10.09.2026 | M2 | Vergleichsschlüssel der Einheiten ohne führende Nullen (`units.normalize_strip_leading_zeros = true`) | Vorschlag H 6.5; WE01 und WE 1 gelten als dieselbe Einheit. Abschaltbar per Konfiguration, falls eine Vorverwaltung beide Schreibweisen als verschiedene Einheiten führt. |
| 10.09.2026 | M2 | Konsistenzlauf täglich 03:15 Serverzeit (ANNAHME) | Zeitfenster außerhalb der Verarbeitung; endgültige Uhrzeit mit Antwort F27. |
| 10.09.2026 | M2 | Technische Abweichungen von der DDL in D (Typen, Primärschlüssel, CHECK mit AUTO_INCREMENT, FULLTEXT als eigene Migration) | Begründung je Punkt in docs/architektur/datenmodell.md Abschnitt 3; fachlich ohne Wirkung. |
| 10.09.2026 | M3 | Sechs Konfigurationsschlüssel `import.column_synonyms`, `import.legal_form_markers`, `import.owner_match_weights`, `import.owner_match_thresholds`, `import.unit_status_active_values`, `import.deposit_type_synonyms` | Aus H 6.3, 6.4, 6.6, 6.9 und 6.2.1 abgeleitet; Werte sind Vorschläge (A-19, A-20) und zur Laufzeit änderbar. |
| 10.09.2026 | M3 | Audit-Aktionen `import.upload`, `import.parse`, `import.row_commit` | Annahme, Erkennung und Übernahme je Zeile werden getrennt protokolliert; `import.commit` bezeichnet den Lauf. |
| 10.09.2026 | M3 | Leerzeilen und Summenzeilen werden beim Einlesen automatisch mit Grund `not_a_record` abgelehnt | Sie bleiben im Protokoll sichtbar (nichts wird stillschweigend verworfen), erzeugen aber keinen Review-Fall. |
| 10.09.2026 | M3 | Zeilen mit Grund `other_object` oder `unit_status_not_active` erhalten den Vorschlag „nicht übernehmen“ und bleiben unsicher | Entscheidung im Review (F11), gesammelte Ablehnung möglich. |
| 10.09.2026 | M3 | Mehrere Personen einer Quellzeile (z. B. „Mustermann, Erika & Max“) werden bei der Übernahme als Mehrfacheigentum behandelt, kein Konflikt | Die Liste der Vorverwaltung definiert die aktuellen Eigentümer; Konflikte entstehen nur gegen bereits vorhandene Zuordnungen anderer Quellen. |
| 10.09.2026 | M3 | Einheitenbezeichnung endet vor dem Lagezusatz („WE 4 - 1.OG rechts“ ergibt Bezeichnung „WE 4“, Lage „1.OG rechts“) | H 6.5 Schritt 1; die Rohzeile bleibt vollständig erhalten. |
| 10.09.2026 | M3 | Gleiche Personen innerhalb eines Imports (Wohnung und Stellplatz) werden zu einem Eigentümer zusammengeführt (Schlüssel Nachname plus Vorname) | H 6.6 letzter Absatz; Übernahme in Zeilenreihenfolge. |
| 10.09.2026 | M4 | Audit-Aktion `drive.reconcile` | Anstoß eines Ordnerabgleichs aus der Oberfläche wird protokolliert. |
| 10.09.2026 | M4 | Unterordner auf Objektebene nur für Kategorien mit Geltungsbereich Objekt oder Sonstiges | Die elf Unterordner der Eigentümerakte liegen je Akte (F 6.3), nicht direkt unter 05_Eigentümerakte; der Abgleich legt sie mit der Akte an. |
| 10.09.2026 | M4 | Dry-Run setzt `no_changes` aus dem Plan (keine Schreibaktion, kein Review-Fall geplant) | F 4.6 definiert no_changes über ausgeführte Schreibaktionen; der Probelauf braucht denselben Indikator für die Planliste. |
| 10.09.2026 | M4 | Review-Fälle des Abgleichs sind je Objekt, Kategorie und Untertyp einmalig offen (`batch_key drive:<objekt>:<kategorie>:<untertyp>`) | Wiederholte Läufe erzeugen keine doppelten Fälle; nach Erledigung entsteht bei Bedarf ein neuer. |
| 10.09.2026 | M4 | Stündlicher Lesetest um Minute 7, täglicher Refresh 04:00 Serverzeit (ANNAHME) | Zeitpunkte außerhalb der Verarbeitung; endgültig mit F27. |
| 10.09.2026 | M4 | Token-Chiffrate als Base64-Text in den VARBINARY-Spalten (`v1:`-Präfix des FieldCipher) | einheitliches Chiffratformat mit Schlüsselversion für alle Zwecke (docs/architektur.md 9.6). |
| 10.09.2026 | M5 | Versandmodus `JOB_DISPATCH` (`celery` im Betrieb, `none` in Tests und im lokalen Messlauf) | Die Jobtabelle bleibt Quelle der Wahrheit; ohne Celery-Transport führt `apps.pipeline.local.run_pending_jobs` dieselben Tasks aus. Kein Einfluss auf den Betrieb. |
| 10.09.2026 | M5 | Wiederholung eines abgeschlossenen Schritts erhält den Idempotenzschlüssel mit Suffix `#n` | B-03 verhindert doppelte offene Jobs. Läuft ein Vorgänger legitim erneut (Arbeitsverzeichnis geräumt, Dokument erneut geladen), braucht der Folgeschritt einen neuen Job; der Grundschlüssel bleibt erhalten und auswertbar. |
| 10.09.2026 | M5 | Upload legt je Objekt einen Lauf `incremental` an oder hängt sich an den laufenden Lauf | Uploads unterliegen damit der Objektserialität (B-26) und erscheinen mit Warteposition; kein Sonderpfad neben den Läufen. |
| 10.09.2026 | M5 | Review-Fälle der Pipeline: `unclear` mit Untertypen `duplicate` (Vorschlag 03_Dubletten), `file_too_large`, `unsupported_format` (Vorschlag 02_Unlesbar), `job_failed`; `import_candidate` mit Untertyp `owner_list` | Untertypen folgen E 1.2 und B-34; je Dokument höchstens ein offener Fall (`batch_key`). |
| 10.09.2026 | M5 | Audit-Aktionen `document.ingest`, `processing.start`, `processing.sweep` | Upload, Laufstart aus der Oberfläche und manueller Sweeper werden protokolliert. |
| 10.09.2026 | M5 | Konfigurationsschlüssel `ocr.language` (deu), `ocr.image_cover_ratio` (0,9), `processing.work_orphan_hours` (48), `processing.heartbeat_seconds` (30) | A-11 und A-13 als Werte im Register statt im Code; Vorschlagswerte. |
| 10.09.2026 | M5 | Dokumente der Eigentümerakte (Kategorie 05) und Mieterakte (04) sind nur mit `owner_files.read` bzw. `tenant_files.read` einsehbar; Seitenbilder nur über die rechtegeprüfte Route | B-15; vor der Klassifikation (Kategorie leer) gilt der Zugriff für angemeldete Nutzer. |
| 10.09.2026 | M5 | Google-Dokumente (`application/vnd.google-apps.*`) gehen bis M6 mit Fall `unsupported_format` in die Prüfung | Der Export nach PDF braucht den Drive-Exportpfad; kein Datenverlust, Dokument bleibt registriert. |
| 10.09.2026 | Betrieb | Deployment aus GitHub nur manuell (`workflow_dispatch`), Aktionsschlüssel mit erzwungenem Kommando `scripts/deploy_remote.sh` | Deployments im Zeitfenster nach F27 und außerhalb laufender Verarbeitung; ein kompromittierter Schlüssel erlaubt kein Arbeiten auf dem Server. Umstellung auf automatisches Deployment bei Push nach Antwort F27 möglich. |
| 10.09.2026 | M6 | Regeln und Testkatalog als JSON statt YAML | Gleiche Struktur wie E 2.2 (id, version, priority, scope, when, then, examples); vermeidet eine zusätzliche Abhängigkeit im Web-Image. Rückbau: Konverter, wenn YAML gewünscht wird. |
| 10.09.2026 | M6 | `classification_rules` um `definition` (JSON) und `version` erweitert, `rule_kind` um `composite` | Die Tabelle aus D kennt nur einzelne Muster; zusammengesetzte Regeln nach E 2.2 brauchen die vollständige Definition. Zusammenfassung (Ziele, Gewicht, hard) bleibt in den D-Spalten. |
| 10.09.2026 | M6 | NER-Stütze in der Kaltstartphase: weiche 05-Regel plus erkannter Eigentümer oder Einheit ergibt `classification.ner_support_bonus` (0,05) | E 7.1 nennt Eigentümer, Einheit und Zeitbezug als Stützsignale der Stufe 2; ohne Modell trägt Stufe 2 nur diese Signale (B-27). Ohne die Stütze gingen in der Kaltstartphase alle Dokumente weicher Regeln in die Prüfung. |
| 10.09.2026 | M6 | Konfigurationsschlüssel `classification.own_company_names`, `classification.contract_partners`, `classification.first_pages`, `classification.text_max_chars`, `classification.ner_support_bonus`, `classification.segment_types` | Eingangsgrößen aus E 2.1, 3.3 und 6.3 als Werte statt im Code; Vertragspartner zunächst global (je Objekt folgt mit Bedarf). |
| 10.09.2026 | M6 | Dokumentunterarten `verwaltervertrag`, `verwalterbestellung` (01) und `mietvertrag`, `kaution`, `betriebskostenabrechnung`, `mieterkorrespondenz` (04) ergänzt | E 2.3 nennt die Regelgruppen; ohne Codes wären die Regeln nicht abbildbar (B-12). |
| 10.09.2026 | M6 | Dokument mit Fall und Ablage in 06 trägt `category 06` mit dem physischen Unterordner; die fachlich erkannte Kategorie steht in der finalen Klassifikationszeile und im Fall (`context.intended`) | KPI Anteil 06 brutto nach CR-Definition zählt `documents.category = 06` (B-31); der bereinigte Wert nutzt `features.kpi_misc_adjusted` der finalen Zeile. |
| 10.09.2026 | M6 | Benannter Eigentümer schlägt das Dokumentdatum, wenn kein expliziter Zeitraum vorliegt (T21) | E 6.2 P4 stellt Forderungszeitraum und Name über das Dokumentdatum; ein Anwaltsschreiben von 2026 zu Rückständen 2025 gehört zum benannten Alteigentümer. Bei explizitem Zeitraum ohne passende Zuordnung bleibt es beim Fall `owner_candidates`. |
| 10.09.2026 | M6 | Fallart bei Fremdobjekt, Mieterdokument in reiner WEG und WEG-Begriffen in Mietverwaltung: `unclear` mit Untertypen `foreign_object`, `tenant_document_in_weg`, `misplaced_weg_document` und `proposed_action` | B-09 nennt für diese Gründe keine eigene Fallart; der Untertyp trägt den fachlichen Grund, der Vorschlag die Aktion (Übernahme in Objekt NNN, Ablage 08_Korrespondenz). |
| 10.09.2026 | M6 | Dokument mit offenem Fall bleibt nach physischer Ablage im Status `review`, Dubletten behalten `duplicate` | `filed` bezeichnet abgeschlossene Ablagen; der Fall entscheidet über den endgültigen Ort (E 7.3). |
| 10.09.2026 | M6 | Akte Unzugeordnet ohne Zeile in `document_owner_links` | Die Tabelle verlangt einen Bezug (Eigentümer, Einheit oder Zuordnung); die Akte steht im Fall (`context.owner_file_id`) und als Ablageziel. |
| 10.09.2026 | M6 | `processing_runs.misc_share_adjusted_pct` | Bereinigter Anteil 06 nach B-31 als eigene Spalte. |
| 10.09.2026 | M6 | Nachtraining täglich 01:30 Serverzeit (ANNAHME) | Vor dem Konsistenzlauf (03:15) und dem Token-Refresh (04:00); endgültig mit F27. |
| 10.09.2026 | M6 | Übernahme in anderes Objekt: alte Zeile `moved_out` verweist über `duplicate_of_document_id` auf die neue Zeile | Grundform B-29 ohne zusätzliche Spalte; Audit beidseitig. Eigene Spalte, falls das Review Center den Verweis getrennt braucht. |
| 10.09.2026 | M7 | Segmente von Gesamtdokumenten werden als Fälle einer Gruppe vorgelegt (`classification.segments_require_review`, Standard true); bei false werden eindeutige Segmente automatisch bestätigt (E 6.3) | H 2.5 und der Ende-zu-Ende-Test des Umsetzungsplans setzen voraus, dass alle Segmente einer Gesamtabrechnung in einer Sammelaktion bestätigt werden; E 6.3 sieht die Automatik vor. Der Schalter erlaubt den Wechsel nach den ersten Objekten ohne Codeänderung. |
| 10.09.2026 | M7 | Ein offener Fall je Dokument und Seitenbereich; ein zweiter Grund ergänzt `context.reasons[]` (H 2.1 Regel 1) | Dedupe über (Dokument, Seitenbereich, Status) statt über `batch_key`; `batch_key` ist damit frei für die Gruppierung nach H 2.5 (Objekt, Herkunft, Kategorie, Unterordner, Unterart, Jahr). |
| 10.09.2026 | M7 | Untertypen `segment_proposed` (eindeutiges Segment) und `split_segment` (Aufteilung im Review) für `owner_candidates` | Kennzeichnen die Herkunft des Vorschlags in Liste und Massenbearbeitung. |
| 10.09.2026 | M7 | Entwurfsspeicher der Massenbearbeitung in der Datenbank-Sitzung des Nutzers | Serverseitig wie gefordert (Gutachten 2), ohne eigene Tabelle; verfällt mit der Sitzung. Eigene Tabelle, wenn Entwürfe Sitzungen überdauern sollen. |
| 10.09.2026 | M7 | Keine externen Skriptquellen (kein HTMX aus einem CDN); Tastaturbelegung und Suche in `static/js/review.js`, Vorschau der Massenbearbeitung als eigene Seite | Betrieb hinter Traefik ohne Abhängigkeit von Drittservern; HTMX kann später als vendorte Datei ergänzt werden. |
| 10.09.2026 | M7 | Audit-Aktionen `review.filter_saved`, `review.filter_deleted`, `review.bulk_preview` | H 2.8 nennt `review.filter_saved`; Vorschau und Löschen ergänzt, damit jede Nutzeraktion im Review Center nachvollziehbar ist. |
| 10.09.2026 | M7 | Konfigurationsschlüssel `review.bulk_max_cases` (500), `review.snooze_default_days` (7) | ANNAHME A-36 und H 2.4 als Werte statt im Code. |
| 10.09.2026 | M7 | Nach Bestätigung mit offenem Fall bleibt eine Verschiebung als Job; Segmentbestätigungen bewegen die Masterdatei nicht | H 2.4 Regel 3 und CR 6 (Master bleibt in 02 bzw. 03). |
| 10.09.2026 | M8 | Preisliste als Konfiguration `ai.price_list` (Version, EUR je 1.000 Token je Modell) statt im Code; `ai_calls.price_list_version` trägt die Version | Preise ändern sich ohne Release; die Kostenrechnung bleibt je Aufruf nachvollziehbar. Der Seed enthält keine Preise (Version `nicht-festgelegt`), damit keine erfundenen Werte im Repository stehen; ohne Eintrag ist `cost_eur` 0 und das Kostenlimit greift nicht. |
| 10.09.2026 | M8 | Versuchsnummer in `ai_calls.response_summary.attempt` statt eigener Spalte | D 11.1 kennt keine Spalte; eine Zeile je Versuch reicht mit `fallback_of_call_id` für die Kette aus. |
| 10.09.2026 | M8 | Circuit Breaker je Anbieter im Cache: drei Fehler in Folge öffnen den Schalter für 300 Sekunden (ANNAHME) | Auflage aus Gutachten C; Werte bis zur Antwort F17. Zustand liegt im Redis-Cache, damit alle Worker denselben Schalter sehen. |
| 10.09.2026 | M8 | Sperren nach F19 (Ausweiskopie, Kategorie 01) prüft `classify_ai` über `stage3_allowed`; Ergebnis `skipped` mit Grund im Fall (`context.stage3_status`, `context.stage3_reason`) | Der Sachbearbeiter sieht im Fall, warum kein KI-Aufruf erfolgte; kein Text erreicht den Anbieter. |
| 10.09.2026 | M8 | Stichprobe (`classification.ai_sample_pct`) als Fall `move_proposal` mit Untertyp `ai_sample`, der die Ablage nicht aufhält (CasePlan `informational`); nur wenn Stufe 3 die Kategorie entschieden hat und kein anderer Fall vorliegt | F17 sieht die Stichprobe an Sachbearbeiter vor; das Dokument ist abgelegt, der Fall dient der nachträglichen Prüfung. Liegt ohnehin ein Fall vor, prüft der Mensch bereits. |
| 10.09.2026 | M8 | Entscheidung durch Stufe 3: finale Klassifikationszeile mit `stage 3` und `provider` gleich Anbieter; die Anbieterantwort bleibt als nicht finale Stufe-3-Zeile mit `ai_call_id` | Umsetzungsplan verlangt `document_classifications.provider` gleich Fallback; Antwort und Entscheidung bleiben getrennt nachvollziehbar. |
| 10.09.2026 | M8 | Nachklassifikationslauf nur manuell (`manage.py ai_reclassify`), Schalter `ai.reclassify_enabled` (Standard false) | F17 offen; ein Beat-Task folgt mit der Antwort. Der Lauf reiht nur Dokumente ein, die noch kein Mensch bearbeitet hat. |
| 10.09.2026 | M8 | Maskierungsmuster Ausweisnummer erlaubt ein Leerzeichen vor „Nr.“ („Personalausweis Nr. …“) | B-11; das Muster erkannte bisher nur „Personalausweis-Nr.“ und „Personalausweisnummer“. |
| 10.09.2026 | M8 | Basis-URL der Anbieter aus `.env` (`OPENAI_BASE_URL`, `ANTHROPIC_BASE_URL`), Schlüssel aus Docker Secrets `openai_api_key`, `anthropic_api_key`; SDK-Wiederholungen abgeschaltet, der Router wiederholt selbst | Umsetzungsplan 2.12 Schritt 1; nur so zählt jeder Versuch einmal in `ai_calls`. |
| 10.09.2026 | M9 | Wörter mit Koordinaten und Linientabellen aus pdfplumber statt PyMuPDF; Spalten aus Lücken der x-Verteilung aller Zeilen (Wortspannen vereinigt, wenn der Abstand unter 90 Prozent der Medianhöhe liegt), Titel- und Fußzeilen bleiben bei der Spaltenbildung außen vor | H 6.2 nennt PyMuPDF für Wörter mit Koordinaten; pdfplumber liegt bereits im Worker-Image und liefert beides, keine zusätzliche Abhängigkeit. Rückbau: PyMuPDF, falls die Genauigkeit an echten Listen nicht reicht. |
| 10.09.2026 | M9 | Tesseract für Listen-Scans mit Seitensegmentierung 3 und `textord_tabfind_find_tables=0` (TSV-Ausgabe) | Mit der Vorgabe (psm 6) und eingeschalteter Tabellenerkennung ließ Tesseract den Text innerhalb von Gitterlinien weg (nur die Fußzeile blieb, Test); Zeilen und Spalten werden ohnehin über Koordinaten gebildet. |
| 10.09.2026 | M9 | Zellen mit mittlerer Wortkonfidenz unter `import.ocr_cell_confidence_min` (70, A-33) ergeben den Grund `low_ocr_confidence` mit Spaltenliste in `parsed_fields._low_confidence_cells`; die Zeile wird unsicher (FA6), der Vorschlag bleibt | Nichts wird verworfen; der Sachbearbeiter sieht, welche Felder er prüfen muss. |
| 10.09.2026 | M9 | Ein Mietverhältnis je Quellzeile; Mitmieter derselben Zeile teilen `leases` (Zeile der ersten Person) | H 6.9; bisher entstand je Person ein Mietverhältnis. |
| 10.09.2026 | M9 | Kaltmiete aus generischen Listen in `leases.base_rent`; nur beim Immoware24-Export bleibt der Betrag in `notes` (Bedeutung offen, F11) | Die Spalte Kaltmiete ist eindeutig, `Miete_EUR_mtl` nicht. |
| 10.09.2026 | M9 | `domus_export` als registriertes Profil ohne Erkennung (Score 0), manuell wählbar, liest wie `generic_table` | H 6.2: Profil wird angelegt, sobald eine Beispieldatei vorliegt; bis dahin greift `generic_table`. Der Platzhalter hält `source_format = domus` nutzbar und wird mit der Beispieldatei gefüllt. |
| 10.09.2026 | M9 | Listenerkennung (B-34) auch für digitale PDF in der Seitenanalyse; Untertyp des Falls `import_candidate` ist die Art der Liste (`owner_list`, `tenant_list`, `mixed`) | Art aus den erkannten Feldern (Mietfelder gegen Eigentümerfelder); im Review vorbelegt und änderbar. Gescannte Listen erst nach der OCR über den Import selbst. |
| 10.09.2026 | M9 | Aktion „Import starten“ (`review.import_started`) erledigt den Fall `import_candidate` mit Verweis auf `import_batches`; die Übernahme bleibt die Entscheidung im Import | H 6.7 und B-34: kein automatischer Import; dieselbe Datei ergibt denselben Import (SHA-256). |
| 10.09.2026 | M10 | Logo der HVM im Repository (`src/hvm_ci/assets`), Unterschriftsbild nur aus dem Betrieb (Secret `hvm_signature.jpg`, Pfad `HVM_SIGNATURE_PATH`) | Das Logo ist öffentliche CI; die Unterschrift ist schutzwürdig und gehört nicht in die Versionsverwaltung. Ohne Datei bleibt der Platz in der freigegebenen Fassung frei. |
| 10.09.2026 | M10 | Statuskette `draft`, `reviewed`, `approved`, `sent`, `withdrawn`; `sent` bedeutet „als versendet vermerkt“ | H 4.5 nennt `marked_sent`; der Wert `sent` aus D bleibt, damit keine Umbenennung nötig ist; der Vermerk trägt Datum und Kanal als Freitext. |
| 10.09.2026 | M10 | `completeness_checks` um `category` und `blocking`, `request_text_blocks` um `version` und `updated_by`, `document_requests` um Version, Frist, Positions-Snapshot, Bausteinversionen, Pfade, Prüfsumme, Prüfer und Versandvermerk (Migration requirements 0002, reversibel) | H 4.1 und 4.3 als Vorschlag; Gruppierung im Schreiben und Sperrwirkung brauchen Katalogfelder statt Code. |
| 10.09.2026 | M10 | Mietkatalog auf Einheitsebene (Mietername in `details.tenant`); Zuordnungsebene nur für Eigentümerzuordnungen | `completeness_findings.assignment_id` verweist auf `owner_unit_assignments`; eine zweite Spalte für Mieterzuordnungen folgt erst mit der Struktur von 04_Mieterakte (Befund 6.3). |
| 10.09.2026 | M10 | Sonderumlagen je Zuordnung nur bei bekannter Umlage im Zeitraum; Zahlungsvereinbarungen und Mahnverfahren je Zuordnung `not_applicable` statt `missing`; Objektfindings dieser Punkte bleiben offen bis zur manuellen Übersteuerung | H 3.2 Negativnachweis: Abwesenheit ist kein Mangel je Eigentümer, die Aufstellung oder Negativerklärung der Vorverwaltung ist der Nachweis. |
| 10.09.2026 | M10 | Positionen weggefallener Zuordnungen werden beim Lauf gelöscht, manuell übersteuerte bleiben | Upsert nach H 3.5 hält den Bestand aktuell; Übersteuerungen sind Entscheidungen von Menschen und bleiben nachvollziehbar. |
| 10.09.2026 | M10 | Bewertungslauf schreibt Audit nur bei manuellem Anstoß; automatische Läufe protokollieren im Anwendungslog | Läufe nach jedem Verarbeitungslauf und jeder Review-Entscheidung würden das Protokoll füllen, ohne eine Nutzerhandlung zu belegen. |
| 10.09.2026 | M10 | Nächtliche Bewertung 02:30 Serverzeit für Objekte im Status `takeover` (ANNAHME) | Zwischen Nachtraining (01:30) und Konsistenzlauf (03:15); endgültig mit F27. |
| 10.09.2026 | M10 | Einzelaufstellung wandert ab `requests.attachment_threshold` (25, ANNAHME H 4.2 Nr. 6) in die Anlage | Wert als Konfiguration statt im Code. |
| 10.09.2026 | M10 | Freigabe über `reauthentication_required` (allauth) mit Recht `demands.approve`; Versand nie durch die Anwendung, nur Vermerk | H 4.5; Step-up wie in der Nutzerverwaltung. |
| 10.09.2026 | M10 | DOCX wird aus den CI-Werten erzeugt (Kennlinie als eingefärbte Zellen, Logo, Fußzeile, Anlagentabelle) statt aus einer Vorlagendatei | Die Vorlage `nachforderung_hvm.docx` existiert laut Befund nicht; die Erzeugung aus Werten hält PDF und DOCX inhaltlich gleich. Vorlagendatei möglich, wenn der Auftraggeber eine freigibt (F23). |
| 10.09.2026 | M10 | Seed überschreibt Textbausteine mit `version > 1` nicht | Im Admin geänderte Bausteine (H 4.3) bleiben erhalten; Schreiben speichern die verwendeten Versionen. |
| 10.09.2026 | M10 | `completeness_findings.evidence_document_id` und `evidence_link_id` mit `ON DELETE SET NULL` statt `PROTECT` | Nachweise sind abgeleitete Verweise; mit `PROTECT` blockierte ein Finding das Ersetzen vorgeschlagener Verknüpfungen im Review (Sammelaktion, Test) und künftig den Löschlauf. Beim nächsten Bewertungslauf wird der Nachweis neu gesetzt. |
| 10.09.2026 | M11 | Eigentümerliste für `weg` und `weg_with_se`, Mieterliste für `rental` und `weg_with_se` (dort nur Einheiten mit `se_managed`) | CR 12a nennt je Objekt eine Eigentümer- und eine Mieterliste; eine leere Mieterliste für eine reine WEG hätte keinen Nutzen. Umstellung per Konfiguration möglich, falls beide Listen immer gewünscht sind. |
| 10.09.2026 | M11 | Leere Blätter erhalten eine Markierungszeile („Keine Datensätze“ beziehungsweise „Keine offenen Punkte“) | Excel verlangt für ein Tabellenobjekt mindestens eine Datenzeile; H 5.3 Nr. 7 sieht die Zeile für Offene Punkte vor, sie gilt hier für alle Blätter. Werte selbst bleiben ohne Platzhalter. |
| 10.09.2026 | M11 | Schalter `LISTS_AUTO_GENERATE` (Umgebung, Vorgabe an; in Tests aus) für die automatische Erzeugung nach Lauf, Review, Import und Stammdatenänderung; der manuelle Knopf läuft immer | Die Automatik würde in jedem Pipeline-Test Excel und PDF erzeugen und Drive-Uploads auslösen, die dortige Prüfaussagen (keine Schreibaktionen) verfälschen; im Betrieb bleibt sie an. |
| 10.09.2026 | M11 | Keine Listenerzeugung nach einem Pipeline-Dry-Run | Der Dry-Run darf keine Drive-Schreibaktion auslösen (B-10, E 6.4). |
| 10.09.2026 | M11 | IBAN-Treffer und Drive-Fehler erzeugen Fälle `move_proposal` mit Untertypen `list_iban_found` und `list_publish_failed`; verschobene oder umbenannte Dateien ohne Rückführung ergeben `drive_structure` mit `list_moved` beziehungsweise `list_renamed` | H 5.6 Nr. 4 und F 7.2; Untertypen tragen den Grund. |
| 10.09.2026 | M11 | Objektnummer im Dateinamen aus den führenden Ziffern des Objektordners, sonst Objektnummer (F4 offen) | F 7.1: NNN in der Schreibweise des Objektordners. |
| 10.09.2026 | M11 | Herkunft je Feld als verstecktes Blatt nur bei `lists.include_provenance_sheet` | H 5.5 Vorschlag; Standard aus, um die Datei schlank zu halten. |
| 10.09.2026 | M12 | Volltextsuche als boolescher Ausdruck: jedes Suchwort muss vorkommen (Präfix +), Umlautvarianten (ae, oe, ue, ss gegen ä, ö, ü, ß in beide Richtungen) als ODER-Gruppe; Wörter unter der Mindestlänge werden angezeigt und ignoriert | H 7.2 Umsetzungshinweise; der Sachbearbeiter sieht, warum ein kurzes Wort keinen Treffer bringt. |
| 10.09.2026 | M12 | Dokumentsuche nur mit dokumentbezogenem Filter oder Volltext; Volltext ohne relationalen Filter läuft über alle Objekte mit Trefferlimit | H 7.2 empfiehlt die Kombination mit einem relationalen Filter; die Liste wird gekürzt und meldet das. |
| 10.09.2026 | M12 | Rollen ohne `owner_files.read` sehen in der Suche keine Eigentümer und keine Dokumente der Kategorie 05, ohne `tenant_files.read` keine Mieter und keine Kategorie 04; nicht klassifizierte Dokumente bleiben sichtbar | CR 10, B-15; die bestehenden Rechte werden serverseitig auf die Suche angewendet. |
| 10.09.2026 | M12 | Review-Alter in Arbeitstagen Montag bis Freitag ohne Feiertage (ANNAHME) | reports.review_age_warning_days ist in Arbeitstagen definiert (A-30); Feiertagskalender als Folgeergänzung. |
| 10.09.2026 | M12 | Reporting berechnet Kennzahlen je Aufruf aus den Tabellen, keine Momentaufnahmen außer misc_share_pct je Lauf | Zahlen bleiben nachrechenbar (D 12.4); bei 3 bis 6 Nutzern und wenigen Objekten ausreichend, Zwischenspeicher bei Bedarf. |
| 10.09.2026 | M12 | Alarmierung nur mit `ALERTS_ENABLED=true` und `ALERT_EMAIL_TO`; SMTP aus `.env` und Secret `smtp_password`; jede Bedingung höchstens einmal je sechs Stunden, Entwarnung wird gemeldet; Zertifikatsprüfung entfällt für localhost | G 9.5; ohne SMTP-Zugang (F27) bleibt die Statusseite die einzige Anzeige der Bedingungen. |
| 10.09.2026 | M12 | Speicherbelegung der Arbeitsverzeichnisse wird fünf Minuten im Cache gehalten | Die Größenermittlung liest alle Dateien; ohne Cache würde jeder Aufruf der Statusseite das Dateisystem durchlaufen. |
| 10.09.2026 | M11 | Das Ziel web erhält die reinen Python-Dokumentbibliotheken über das Extra `web` (rapidfuzz, openpyxl, reportlab, python-docx, pydantic, Pillow, requests, Google-Drive-Client); keine OCR-Binärdateien und keine ML-Bibliotheken. Module, die nur der Worker ausführt, importieren ihre Bibliotheken innerhalb der Funktion | B-44 bleibt gewahrt (kein Tesseract, Ghostscript, Compiler im Web-Image). Die Nachforderung entsteht bei der Freigabe im Web-Prozess, OAuth-Rückruf und Drive-Verwaltungsseite laufen dort ebenfalls. Alternative (Erzeugung nur im Worker) hätte Freigabe und Datei zeitlich getrennt; verworfen, weil ohne fachlichen Nutzen. `manage.py check` im Build verhindert die Wiederholung. |
| 10.09.2026 | M13 | Die Latenzsonde löst keine Entscheidungen (POST) aus, sondern misst nur lesende Endpunkte; die Laufzeit der Entscheidung wird im Testclient und über die Aktionsdauer im Audit erfasst | Ein Lasttest darf keine Fälle verändern und keine Drive-Jobs erzeugen; die Zielgröße p95 unter 2 s gilt für alle sieben lesenden Endpunkte aus 2.17. |
| 10.09.2026 | M15 | Bedienungsanleitungen ohne Bildschirmfotos, bis die Oberfläche auf dem Server läuft; Texte beschreiben Schaltflächen und Felder mit ihren tatsächlichen Beschriftungen | Bildschirmfotos aus der Entwicklungsumgebung würden Testdaten zeigen und nach dem Deployment veralten; sie werden nach T1 ohne reale Daten ergänzt (H 8). |
| 10.09.2026 | M1 | Der VPS wird mit anderen Anwendungen geteilt (17 fremde Container). Ressourcenlimits unter 0,5 x M statt 0,85 x M: OCR_PROCESSES 4 statt 7, DB_MEM 3 GiB statt 4,7 GiB; Anhebung nach Messung im Performance-Test | Die Formel aus docs/betrieb.md 3.10 setzt einen exklusiven Host voraus; ohne Abschlag würden die Mitnutzer bei OCR-Last verdrängt. Die Zielzeit unter drei Stunden für 10.000 Seiten ist mit vier OCR-Prozessen am Prüfpunkt P1 nachzurechnen. |
| 10.09.2026 | M1 | Traefik läuft im Host-Netz; die Anwendung erhält ein eigenes Bridge-Netz `objektakte-proxy` mit festem Subnetz 172.31.250.0/24, angelegt vom Deploy-Skript; der Redirect-Router entfällt, weil Traefik global auf HTTPS umleitet | Traefik erreicht Container im Host-Modus über deren Adresse in jedem Bridge-Netz; das feste Subnetz macht TRUSTED_PROXY_CIDR deterministisch. Ein doppelter Redirect wäre wirkungslos, aber unnötig. |
| 10.09.2026 | M1 | Image-Tags: MariaDB 11.8 (LTS), Redis 7.4-alpine | Beide Versionen sind zum Umsetzungszeitpunkt gepflegt; Redis 7.4 steht unter RSALv2 und SSPLv1, für den internen Betrieb unkritisch (docs/architektur/lizenzen.md); Wechsel auf Valkey bleibt möglich (REDIS_IMAGE). |
