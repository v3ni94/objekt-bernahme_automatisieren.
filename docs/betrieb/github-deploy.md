# GitHub-Anbindung des VPS über SSH

Stand: 10.09.2026. Zwei getrennte Schlüsselpaare, zwei Richtungen. SFTP wird nicht benötigt: Der Server holt den Code per Git, GitHub Actions stößt das Deployment per SSH an. Alle Schritte laufen auf dem VPS als Deploy-Nutzer (docs/betrieb.md 2.1 Nr. 1, V-01). Die Entwicklungsumgebung hat keinen Zugang zum Server und keine Rechte auf die Repository-Secrets; die Schritte 1 bis 4 führt der Auftraggeber oder eine Person mit Serverzugang aus.

## Überblick

| Richtung | Schlüssel | Liegt wo | Zweck |
|---|---|---|---|
| VPS holt Code von GitHub | Deploy Key (nur Lesen) | privater Teil unter `~deploy/.ssh/github_deploy`, öffentlicher Teil im Repository unter Settings, Deploy keys | `git fetch` und `git pull` in `scripts/deploy.sh` |
| GitHub Actions löst Deployment aus | Aktionsschlüssel | privater Teil als Repository-Secret `DEPLOY_SSH_KEY`, öffentlicher Teil in `~deploy/.ssh/authorized_keys` mit `command=`-Einschränkung | Workflow `.github/workflows/deploy.yml` ruft `scripts/deploy_remote.sh` auf |

Der Aktionsschlüssel kann auf dem Server ausschließlich die in der Tabelle unten genannten Aktionen auslösen (erzwungenes Kommando, kein Terminal, keine Weiterleitungen). Ein kompromittierter Schlüssel erlaubt damit kein Arbeiten auf dem Server.

## Kurzweg: `scripts/bootstrap_vps.sh`

Die Schritte 1 bis 3 auf dem Server (Deploy-Nutzer, Verzeichnisse nach docs/betrieb.md 3.3, Secrets nach 3.8, beide Schlüsselpaare, Werte für GitHub) erledigt `sudo bash scripts/bootstrap_vps.sh` in einem Lauf; das Skript ist idempotent und überschreibt nichts Vorhandenes. Es schreibt die drei Werte für GitHub nach `/home/deploy/github-werte.txt` (nur für `deploy` lesbar); die Datei wird nach dem Eintragen mit `shred -u` gelöscht. SSH-Härtung und Firewall bleiben bewusst manuelle Schritte (docs/betrieb.md 2.1), damit der Schlüssel-Login vorher geprüft wird. Die Einzelschritte darunter erklären, was das Skript tut.

## Schritt 1: Deploy Key (VPS liest von GitHub)

```bash
# auf dem VPS als deploy
ssh-keygen -t ed25519 -N "" -C "objektakte-vps-deploy-key" -f ~/.ssh/github_deploy
cat ~/.ssh/github_deploy.pub
cat >> ~/.ssh/config <<'CFG'
Host github.com
  IdentityFile ~/.ssh/github_deploy
  IdentitiesOnly yes
CFG
chmod 600 ~/.ssh/config
ssh -T git@github.com   # erwartete Antwort: "Hi v3ni94/objekt-bernahme_automatisieren.! You've successfully authenticated"
```

Den Inhalt von `github_deploy.pub` im Repository unter Settings, Deploy keys, Add deploy key eintragen; Schreibrecht nicht setzen. Danach Checkout über SSH (Erstinstallation, docs/betrieb/deployment.md Schritt 4):

```bash
sudo mkdir -p /opt/objektakte && sudo chown deploy:deploy /opt/objektakte
git clone git@github.com:v3ni94/objekt-bernahme_automatisieren..git /opt/objektakte
```

Ist das Repository bereits per HTTPS ausgecheckt: `git -C /opt/objektakte remote set-url origin git@github.com:v3ni94/objekt-bernahme_automatisieren..git`.

## Schritt 2: Aktionsschlüssel (GitHub Actions verbindet sich zum VPS)

```bash
# auf dem VPS als deploy; der private Teil wird gleich in ein Secret kopiert und dann gelöscht
ssh-keygen -t ed25519 -N "" -C "github-actions-deploy" -f /tmp/gh_actions_deploy
echo "command=\"/opt/objektakte/scripts/deploy_remote.sh\",no-port-forwarding,no-agent-forwarding,no-X11-forwarding,no-pty $(cat /tmp/gh_actions_deploy.pub)" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
cat /tmp/gh_actions_deploy          # Inhalt in das Secret DEPLOY_SSH_KEY übernehmen
ssh-keyscan -t ed25519 187.124.23.80 # Ausgabe in das Secret DEPLOY_KNOWN_HOSTS übernehmen
shred -u /tmp/gh_actions_deploy /tmp/gh_actions_deploy.pub
```

## Schritt 3: Secrets im Repository

Settings, Secrets and variables, Actions, Environment `production` (oder Repository-Secrets):

| Secret | Wert |
|---|---|
| `DEPLOY_HOST` | `187.124.23.80` (oder der DNS-Name, wenn der Host-Key dazu passt) |
| `DEPLOY_USER` | `deploy` |
| `DEPLOY_SSH_KEY` | vollständiger privater Schlüssel aus Schritt 2 (mehrzeilig) |
| `DEPLOY_KNOWN_HOSTS` | Zeile aus `ssh-keyscan` (Schritt 2) |

Für die Umgebung `production` empfiehlt sich in GitHub die Freigabepflicht durch eine benannte Person (Required reviewers), damit kein Deployment ohne Vier-Augen-Prinzip startet.

## Schritt 4: Aktionen des Workflows

Actions, Workflow `Deploy`, Run workflow; Eingaben Branch, Aktion und (nur für create-admin) Argument. Auf dem Server führt `scripts/deploy_remote.sh` genau diese Aktionen aus, alles andere weist es ab. Reihenfolge der Erstinstallation: `check`, `befund`, `env-init` (nachdem `deploy/env.produktion` aus dem Befund befüllt wurde), `first-run`, `create-admin`.

| Aktion | Wirkung | Schreibt auf dem Server |
|---|---|---|
| `check` | Hostname, Nutzer, ausgecheckter Branch, ob `.env` vorhanden ist, Verzeichnisse mit Belegung und ob die Startpasswortdatei noch existiert (ohne Inhalt) | nichts |
| `pull` | Checkout auf den angegebenen Branch bringen (`git fetch`, `checkout`, `pull --ff-only`) | nur den Checkout |
| `befund` | Serverbefund nach docs/betrieb/m0-anleitung.md ins Log (Kerne, RAM, Platte, Docker, Traefik, Host-Sicherheit; Geheimnisse geschwärzt) | nichts |
| `env-init` | `.env` aus `deploy/env.produktion` anlegen; ist `.env` vorhanden, nur Abweichungen anzeigen; legt das Proxy-Netz an und prüft die Compose-Datei | `.env`, Docker-Netz |
| `ps` | Zustand aller Container | nichts |
| `smoke` | Anwendung über die Domain aufrufen, Zertifikat anzeigen, Traefik-Meldungen zur Ausstellung | nichts |
| `cert-retry` | Web-Container neu aufbauen, damit Traefik den Router neu aufnimmt und ein Zertifikat anfordert; wartet bis zu drei Minuten. Höchstens einmal je Stunde, Let's Encrypt begrenzt fehlgeschlagene Validierungen auf fünf je Stunde und Name | Web-Container |
| `logs` | letzte 40 Logzeilen; ohne Argument die der Anwendungsdienste, sonst der genannte Dienst | nichts |
| `db-status` | Datenbankkonten und Tabellenzahl anzeigen, dazu Reste der Scratch-Datenbanken `objektakte_restore_probe` und `objektakte_probe` (nach abgebrochener Probe, docs/betrieb.md 5.4.1) | nichts |
| `oauth-check` | Konfiguration der Google-Verbindung ohne Geheimnisse (Client-ID gekürzt, Secret nur Länge und Präfix, Redirect, Konto, Wurzelordner, Token-Status) und Probe der Client-Zugangsdaten gegen den Token-Endpunkt: `invalid_client` heißt Client-ID und Secret passen nicht zusammen, `invalid_grant` heißt in Ordnung | nichts |
| `deploy-tests` | Deployment-Tests lesend: T2 (Host-Ports), T3 (Isolation `data`), T11 (Logs ohne IBAN, JSON), T14 (`/healthz/`, `/readyz/` ohne Token); seit 26.09.2026 zusätzlich die Prüfabfragen zu T4/T5 (Doppelte je Objekt und Prüfsumme, vom Sweeper erneut eingereihte Jobs, wiederholte OCR-Blöcke, Sweeper-Logzeilen), T12 (`PermitRootLogin`, `PasswordAuthentication` aus `sshd -T` oder den Konfigurationsdateien, `ufw` nur mit sudo, `unattended-upgrades`, Zeitzone und NTP) und T13 (Pflichtrollen für TOTP, Nutzer je Rolle ohne Namen, `auth.denied` der letzten 30 Tage) | nichts |
| `restart-probe [echt]` | T4/T5: ohne Argument Jobzähler (Jobs und Läufe nach Status, offene Jobs je Art, Doppelte, Dokumente im Fehlerstatus) und Anzahl der Sweeper-Logzeilen; mit `echt` wird ausschließlich der Dienst `worker` neu gestartet, Zähler vorher und nachher sowie Sweeper-Logzeilen seit dem Neustart. Laufende OCR-Blöcke werden abgebrochen und vom Sweeper erneut eingereiht; nur mit Freigabe und außerhalb großer Läufe. Der Serverneustart bleibt Handarbeit | Worker (nur mit `echt`) |
| `restore-probe` | T6: `restore_probe.sh` im Backup-Container, jüngste Sicherung in die Scratch-Datenbank `objektakte_restore_probe` einspielen, Zeilenzahlen der Kerntabellen und Token-Status gegen die Produktion, Scratch-Datenbank löschen, auch nach Abbruch durch Signal; Ausgabe als Protokoll nach docs/betrieb.md 5.6, Exit 0 nur ohne fehlende Kerntabelle. Erst nach einem Deploy mit dem neuen Backup-Image verfügbar; bei großen Dumps in tmux | Scratch-Datenbank (wird gelöscht, Reste nach KILL entfernt das nächste `deploy`) |
| `migrations-roundtrip` | T8: `scripts/check_migrations_roundtrip.sh` im Web-Image als `app_migrate` gegen die Scratch-Datenbank `objektakte_probe` (zu Beginn angelegt, am Ende gelöscht); bricht ab, wenn der Name der Produktionsdatenbank getroffen würde | Scratch-Datenbank (wird gelöscht) |
| `oauth-proof [tage]` | T9: `drive_oauth_proof --days` (Standard 8) lesend, Token-Metadaten und Refresh-Ereignisse ohne Chiffrate als Markdown für docs/betrieb/oauth-nachweis.md | nichts |
| `perf-probe [pages=N+procs=1,2]` | T10: `scripts/perf_probe.sh` mit kleinen Parametern (Standard 20 Seiten, Prozesse 1,2, höchstens 200 Seiten) nur im Leerlauf der Verarbeitung (kein Lauf wartend oder laufend, kein Job wartend oder laufend; sonst vorher `worker-stop`, danach `worker-start`), Ausgabe unter `/srv/objektakte/deploy/perf-probe/`, danach OOM-Kill-Status aller Container; Freigabe V-03 | Messverzeichnis, temporäres Image |
| `doc-status` | Dokumente je Objekt und Status, offene und fehlgeschlagene Jobs, Läufe, Ordnerabgleiche, offene Fälle, Wartegründe; ohne Dateinamen und Personendaten | optional Objektnummer: dann je Dokument Kategorie, Unterordner, Unterart, Konfidenz, Stufe, Entitätenzähler und Fälle (nur Codes und Zähler, keine Namen) |
| `reconcile-all` | Ordnerabgleich aller aktiven Objekte als echter Lauf; legt fehlende Struktur an, etwa die Unterordner der Stammakte nach der Katalogerweiterung; läuft im Dienst `worker-io`, damit die Sammelfassung unter `/data/exports/drive-sync` geschrieben werden kann (der Web-Container ist schreibgeschützt); schlägt das Schreiben fehl, steht `summary_error` im Ergebnis und der Abgleich gilt trotzdem als abgeschlossen | optional `ohne-ordner`: nur Objekte ohne registrierten Objektordner |
| `jobs-bereinigen` | überzählige wartende Wiederholungsjobs (`#n`) zum selben Schlüssel auf `skipped` setzen, der älteste offene bleibt; Argument `echt` führt aus, sonst Vorschau; Protokoll `processing.jobs_dedupe` | processing_jobs |
| `paperless-archivfassung` | Dokumente mit Paperless-Verknüpfung und offenem Fall „nicht unterstütztes Format" auf die PDF-Archivfassung umstellen (nur Formate, die die Kette nicht selbst liest, etwa HTML und alte Office-Formate; Auswahl über die Verknüpfung, unabhängig von der Dokumentquelle: auch Drive-Bestandsdateien, die die Übernahme per Prüfsumme mit Paperless verknüpft hat; deren Drive-Datei bleibt unverändert, die Kette liest die lokale PDF-Fassung): Archivfassung laden, Datei und Metadaten ersetzen, Dokument zurück auf registriert, Fall erledigt (`reprocess_archive`), Protokoll `sync.paperless_archive`; Argumente mit `+`: `objekt=NR`, `limit=N`, `echt`; ohne `echt` Vorschau (gebündelte Abfrage der Archivfassung bei Paperless, je 100 Dokumente eine Anfrage) mit Verteilung der Fälle ohne Verknüpfung nach Quelle und Dateiendung; Fortschritt alle 200 Dokumente; danach `verarbeitung-alle echt` | documents, review_cases, external_links |
| `formate-wiederaufnehmen` | Offene Fälle „nicht unterstütztes Format", deren Format die Kette inzwischen kennt (E-Mails `.eml` und `.msg` seit 25.09.2026), erledigen (`reprocess_format`) und die Dokumente zurück auf registriert setzen (Hash, Seiten, Klassifikation neu; Drive-Bestandsdateien lädt die Kette erneut aus Drive), Protokoll `pipeline.format_reprocess`; Argumente mit `+`: `objekt=NR`, `limit=N`, `gruppe=bekannt|temporaer|html|office`, `echt`; Gruppe `bekannt` (Standard) nimmt Fälle mit inzwischen bekanntem Format (seit 26.09.2026 auch HTML, nicht aber Office-Altformate), `temporaer` Fälle ohne Endung, mit Temporär-Endung (`.tmp`, `.herunterladen`, `.indir`, `.hed`, `.part`, `.crdownload`) oder sonst unbekannter Endung, deren Inhalt die Kette noch nicht geprüft hat, `html` nur HTML, `office` Office-Altformate (`.doc`, `.xls`, `.rtf`, `.odt`, `.ods` nach Endung, MIME-Typ oder Kontext `detected_kind`, wenn nur die Inhaltsprüfung das Altformat erkannt hat; Umwandlung über LibreOffice im Worker-Image, auch Fälle mit gescheiterter Umwandlung; keine Präsentationen `.ppt`); Fälle mit bereits geprüftem Inhalt (`content_checked`) nimmt außer `office` keine Gruppe erneut auf, auch nicht bei bekanntem MIME-Typ aus Drive; ohne `echt` Vorschau mit Verteilung nach Endung, Format, Quelle und Objekt sowie Zähler der weiterhin unbekannten Endungen, der Office-Altformate und der bereits geprüften Inhalte (keine Dateinamen); danach `verarbeitung-alle echt`. Fälle mit inzwischen bekanntem Format überspringt `paperless-archivfassung` mit eigenem Zähler | documents, review_cases |
| `restformate-bereinigen gruppe=temporaer+objekt=NR+limit=N+echt` (oder `gruppe=archive-medien`) | Restformate nach der Entscheidungsvorlage E-1 bereinigen (Kommando `restformate_bereinigen`, Pflichtargument `gruppe`). Gruppe `temporaer`: offene Fälle „nicht unterstütztes Format", deren Dokument eine Temporär-Endung (`.tmp`, `.herunterladen`, `.indir`, `.hed`, `.part`, `.crdownload`) oder keine Endung trägt, deren Fallkontext `content_checked=true` hat und deren Inhaltsprüfung keine Signatur erkannt hat (`content_recognized=false`; Altfälle ohne das Feld nur mit Notiz „(Inhalt unbekannt)"): Drive-Datei in den Papierkorb (nie endgültig, Wiederherstellung in Drive 30 Tage), Dokument als gelöscht markiert (Soft-Delete, keine Zeile entfernt), Drive-Verknüpfung auf „im Papierkorb", Fall erledigt (`trashed_temp_file`), Protokoll `pipeline.temp_file_trashed`; Fälle ohne `content_checked` bleiben unangetastet (zuerst `formate-wiederaufnehmen gruppe=temporaer`), Dokumente ohne Drive-Datei werden nur gezählt; ebenso ausgenommen und getrennt gezählt: erkanntes Office-Altformat mit gescheiterter Umwandlung (`detected_kind=office_legacy`, gehört in `formate-wiederaufnehmen gruppe=office`) und erkannte, aber nicht verarbeitbare Signaturen (Präsentation, ZIP-Archiv, Makro-Office, Office-Paket ohne bekannten Hauptteil), die im Prüfcenter bleiben. Abweichung zur Entscheidungsvorlage E-1: die Endung `.css` (dort in der Gruppe „Temporär und System") gehört nicht zur Auswahl, weil die Formatweiche dieselbe Endungsmenge nutzt; solche Fälle erscheinen als „andere Endung" und bleiben Einzelfall im Prüfcenter. Wird eine vom Kommando in den Papierkorb gelegte Datei in Drive wiederhergestellt, belebt das Drive-Änderungsprotokoll das Dokument wieder (Löschkennzeichen zurück, Status registriert, Kette ab `discover`, Protokoll `sync.drive_restored`); wird der Papierkorb geleert, schließt es die Drive-Verknüpfung als bestätigte Löschung ohne Abgleichskonflikt. Gruppe `archive-medien`: Fälle mit `.zip`, `.mp4`, `.mov`, `.avi`, `.mkv`, `.mp3`, `.wav` ohne offenen Fall `file_too_large`: Dokument auf 06_Sonstiges/02_Manuelle_Prüfung ohne Dokumentart, Ablagejob `file_to_drive` wie bei Dubletten (Bestandsdatei wird verschoben), Fall erledigt (`filed_manual_check`), Protokoll `pipeline.manual_check_filed`. Ohne `echt` Vorschau mit Zählern je Endung und Objekt sowie der nicht zur Gruppe gehörenden Fälle (keine Dateinamen); mit `echt` Ergebniszähler, Fehler je Dokument mit Dokument-ID ohne Abbruch des Laufs; ein zweiter Lauf findet nichts mehr. Freigabe der Geschäftsführung vor `echt` (Papierkorb-Aktion) | documents, review_cases, external_links, processing_jobs |
| `redis-status` | Redis (Broker und Cache): belegter Speicher und Grenze, Schlüsselzahl, Nachrichten je Warteschlange, unbestätigte Nachrichten, größte Schlüssel; nur lesend, ohne Passwort in der Ausgabe | nichts |
| `env-set` | Betriebswerte in `.env` setzen, Argument `SCHLUESSEL=wert`, mehrere Paare mit `+` getrennt (`OCR_PROCESSES=6+IO_CONCURRENCY=12`); nur freigegebene Schlüssel (Ressourcen und Parallelität: `REDIS_MEM`, `REDIS_MAXMEMORY`, `OCR_PROCESSES`, `IO_CONCURRENCY`, `NLP_CONCURRENCY`, `GUNICORN_TIMEOUT`, Container-Grenzen), Sicherung `.env.bak`, Eintrag im Deploy-Protokoll; wirksam erst mit `deploy` | `.env` |
| `disk-status` | Plattenbelegung nur lesend: `df` für `/srv`, `du` je Datenverzeichnis unter `/srv/objektakte`, Anzahl und Alter der Arbeitsverzeichnisse unter `work/` (älter als 48 und 6 Stunden), Transit-Kopien, Vorschaubilder, Docker-Belegung; keine Dateinamen | keine |
| `lauf-monitor [Sekunden]` | Fortschritt der Verarbeitung live, nur lesend: alle N Sekunden (Standard 10, mindestens 5) eine Zeile mit Läufen nach Status, offenen Jobs nach Art, Fortschritt in Prozent, Durchsatz und Hochrechnung des Endes aus der Abnahme der offenen Jobs seit Monitorstart; je Minute die Dokumente der betroffenen Objekte nach Status; vorab die fehlgeschlagenen Jobs der Läufe der letzten 24 Stunden nach Art und Fehlerklasse ohne Dateinamen. Endet von selbst, wenn nichts mehr offen ist; Strg+C beendet nur den Monitor | keine |
| `fehler-wiederaufnehmen objekt=NR+klasse=A,B+echt` | Dokumente im Status Fehler gezielt wieder in die Kette nehmen, auch jenseits der drei automatischen Wiederaufnahmen (nach einer Fehlerkorrektur im Code). Auswahl nach Objekt und/oder Fehlerklasse des letzten fehlgeschlagenen Jobs, Wiedereinstieg so spät wie möglich, offene Fälle job_failed werden mit reprocess_manual geschlossen. Ohne `echt` Vorschau. Danach `verarbeitung-alle echt` | Dokumente |
| `ai-reclassify [Objekt]+echt+ohne=133,216+limit=200+unterfall=manual_check+alle+details` | KI-Nachklassifikation: Fälle unter Schwelle ohne Stufe-3-Ergebnis (Standard) oder mit `unterfall=manual_check` die KI-Einstufung Sonstiges; `alle` nimmt auch Fälle mit früherem Stufe-3-Ergebnis (nach Änderung von Auftrag oder Regeln, für manual_check nötig); `ohne=` nimmt Objekte aus, `limit=` begrenzt (Pilot), `details` eine Zeile je Dokument. Ohne `echt` Vorschau mit Zusammenfassung je Objekt. Kosten je Dokument aus `ai-check`. Nur über den serverseitigen Wrapper `d` (Komma-Listen), nicht über den GitHub-Workflow | Klassifikation |
| `transit-bereinigen` | Transit-Kopien (Uploads, Downloads der Paperless-Übernahme) löschen, deren Dokument bereits in Drive liegt (`drive_file_id` gesetzt, Status `filed` oder `duplicate`), leere Eingangsordner mit; ohne Argument Vorschau mit Anzahl und MB, Argument `echt` löscht; Protokoll `processing.transit_cleanup` | transit/ |
| `drive-dubletten echt+objekt=NR+limit=N` | Altkopien in Drive in den Papierkorb legen, die bei einer erneuten Ablage desselben Dokuments entstanden sind (Nachklassifikation bis 23.09.2026: Zweitkopie hochgeladen, Altkopie blieb in 06/01). Quelle Protokoll `drive.upload`; jede Altkopie wird in Drive nachgelesen und über `document_id` oder `sha256` bestätigt, sonst bleibt sie liegen. Nie endgültig löschen. Ohne `echt` Vorschau mit Anzahl je Objekt; Protokoll `drive.trash_duplicate_copy` | Drive |
| `worker-drosseln` | CPU-Last der laufenden Worker ohne Neustart senken. Argument Kerne für den OCR-Worker, optional `+Anteil` in Prozent (`2` oder `2+50`, letzteres 2 Kerne zu je höchstens 50 Prozent, Quote 1,0); Ablage- und Klassifikations-Worker 1 Kern mal Anteil. Setzt die harte Quote (`--cpus`), niedrige Priorität (`--cpu-shares 128`, andere Anwendungen gewinnen bei Konkurrenz) und versucht die Celery-Poolgröße anzupassen; gilt bis zum nächsten Neustart, dauerhaft über `env-set` und `deploy` | Worker |
| `work-bereinigen` | Arbeitsverzeichnisse unter `work/` in einem Durchgang räumen: älter als eine Stunde, Dokument nicht mehr `hashed`, kein offener Job (ein Datenbankabgleich statt zwei Abfragen je Verzeichnis wie im Minuten-Sweep); ohne Argument Vorschau mit Anzahl, Argument `echt` löscht und meldet freien Platz vorher und nachher | work/ |
| `last-status` | Momentaufnahme der Serverlast, nur lesend: `uptime`, CPU und Speicher je Container (`docker stats`), gesetzte CPU-Grenzen der Dienste, Top-Prozesse des Hosts | keine |
| `worker-stop` | Verarbeitung sofort anhalten: OCR-, Klassifikations- und Ablage-Worker sowie Beat stoppen (`docker compose stop`), Web, Datenbank und Redis laufen weiter; Warteschlangen und Jobs bleiben erhalten, laufende Jobs werden nach dem Wiederanlauf wiederholt | Worker |
| `worker-start` | Verarbeitung nach `worker-stop` wieder anlaufen lassen (`docker compose start`, dieselben Container, CPU-Grenzen aus `worker-drosseln` bleiben) | Worker |
| `config-set` | Konfigurationswert aus dem Katalog setzen, Argument `schluessel=wert`; Werte ohne Anführungszeichen: `true`, `false` und Zahlen werden als solche gelesen, alles andere als Text (Listen und Objekte über das Admin-Formular); gleiche Validierung und gleicher Protokolleintrag (`setting.update`) wie im Admin-Formular, unbekannter Schlüssel oder ungültiger Wert wird mit Meldung abgelehnt; JSON-Werte (Objekte, Listen) in der interaktiven Shell in einfache Anführungszeichen setzen, sonst entfernt die Shell die Anführungszeichen und expandiert geschweifte Klammern; stehen gebliebene Platzhalter in Zahlenfeldern (zum Beispiel `EUR_EINGABE` statt `0.0001`) werden als solche gemeldet und müssen durch Zahlen mit Dezimalpunkt ersetzt werden | app_settings |
| `altbestand-import` | Quellordner aus `db/seeds/altbestand_ordner.txt` in die Altbestand-Tabelle aufnehmen (Doppelte werden erkannt), Namen und Objektnummern aus Drive lesen, vorhandene Objekte zuordnen; idempotent | takeover_sources |
| `altbestand-objekte` | Objekte für alle Altbestand-Quellen mit erkannter Nummer und ohne Objekt anlegen (`manage.py altbestand_objekte_anlegen`); ohne Argument nur Vorschau, Argument `echt` legt an. Bezeichnung und Verwaltungsart aus `db/seeds/objektregister.csv` (Immoware24-Export 01.07.2026), sonst aus dem Ordnernamen mit Vorgabe WEG und Hinweis in den Notizen; Quellordner werden gebunden, die Ordneranlage in Drive läuft wie bei der Anlage von Hand; idempotent | objects, takeover_sources, Drive-Ordner |
| `paperless-feld-alle` | Feld MHV Objekt für alle Paperless-Speicherpfade mit zugeordnetem Objekt setzen (`manage.py paperless_feld_setzen`); ohne Argument Vorschau je Pfad (Dokumente, ohne Feld, bereits gesetzt, abweichend), Argument `echt` setzt nur fehlende Werte und startet einen Bestandslauf mit allen betroffenen Objektnummern; Übernahme nach Drive über die Operationsliste; idempotent | Paperless (Zusatzfeld), sync_inventory_runs, sync_operations |
| `paperless-feld-abgleich` | Feld MHV Objekt in Paperless je Dokument eines Objekts gegen die Zuordnung in der Anwendung prüfen (`manage.py paperless_feld_abgleich <objekt> [--echt]`); Argument `<objekt>` Vorschau mit Befund je Dokument (ok, leer, anderes_objekt, unbekannt, fehlt, fehler), `<objekt>+echt` übernimmt Dokumente mit geleertem Feld in das Eingangsobjekt und mit anderer bekannter Nummer in dieses Objekt (Aktion „In anderes Objekt übernehmen“, Drive-Verschiebung folgt); Korrekturweg, wenn in Paperless nach der Übernahme das Feld geleert oder geändert wurde, denn das wirkt nicht von selbst zurück; jede Übernahme wird als Lernbeispiel gespeichert (reject bzw. correct), das verworfene Objekt wird für dieses Dokument nicht erneut automatisch gewählt | documents, processing_jobs, sync_assignment_examples, Drive-Ordner |
| `objekt-anschriften` | Weitere Anschriften desselben Gebäudes (Eckobjekte, mehrere Hausnummern) aus den Objektbezeichnungen ableiten (`manage.py objekt_anschriften_ergaenzen`); ohne Argument Vorschau je Objekt mit Hinweisen (nicht erkannte Bestandteile, nicht auswertbare Hausnummer), Argument `echt` setzt fehlende Hauptanschriften aus der Bezeichnung und ergänzt neue weitere Anschriften, überschreibt nie vorhandene Werte; Audit `object.update` mit Grund; idempotent. Argument `korrigieren` zeigt, welche von diesem Kommando gesetzten Hauptanschriften die aktuelle Erkennung anders zerlegen würde (Ausgangspunkt ist der Audit-Zustand vor dem ersten Lauf, seitdem von Hand geänderte Objekte bleiben mit Hinweis stehen), `korrigieren+echt` speichert diese Korrekturen | objects |
| `anschrift-kandidaten objekt=467,469+ausser=Rheinpromenade+min=5+top=5+echt` | Anschrift eines Objekts aus den Seitentexten seiner Dokumente vorschlagen (Anschriftenblöcke und Inline-Anschriften der ersten Seiten, gezählt je Dokument); ohne `objekt=` alle Objekte ohne Straße; `ausser=` schließt Fremdanschriften wie die eigene Firmenanschrift aus; Schreibweisen und Hausnummern einer Straße bilden eine Gruppe; belegt heißt: die Straße steht in der Objektbezeichnung (Hausnummer oder Bereich wie 67-85 aus der Bezeichnung), die Anschrift kommt im Text als WEG-Name vor, oder die Gruppe hat mindestens `min` Dokumente und dreimal so viele wie die zweite; `echt` setzt belegte Vorschläge als Hauptanschrift (nie überschreiben, Audit `object.update`). Ohne `echt` Vorschau. Nur über den Wrapper `d` | Objekte |
| `buchhaltung-jahresordner objekt=NR+limit=N+echt` | Bereits in 03_Buchhaltung abgelegte Dokumente in die Jahresordner `03_Buchhaltung/JJJJ` bringen (seit 24.09.2026 legt die Ablage Belege, Kontoauszüge, Gesamtabrechnungen und Wirtschaftspläne nach Abrechnungsjahr, sonst Zeitraum, sonst Dokumentdatum in Jahresordner; ohne Jahr flach in 03). Der Befehl setzt die Dokumente auf classified und reiht die Ablage erneut ein, die Worker verschieben die vorhandene Drive-Datei per Elternwechsel und legen den Jahresordner bei Bedarf an; nie löschen. Ohne `echt` Vorschau je Objekt und Jahr mit der Zahl der Dokumente ohne Jahr. Danach `lauf-monitor` | Dokumente, Drive |
| `backup-voll` | Volle Sicherung sofort im Backup-Container (`backup.sh`, Datenbank und Fachverzeichnisse, zuletzt 1.315 s); schreibt `status.json` und macht den Healthcheck wieder grün; nur in tmux starten. Der nächtliche Cron-Lauf blieb vom 10.09. bis 22.09.2026 aus (Zeile in `/etc/cron.d` ohne Benutzerfeld, behoben im Entrypoint) | Backup-Verzeichnis |
| `zuordnung-pruefen` | Gegenprobe der Objektzuordnung für aus Paperless über das Feld übernommene Dokumente (`manage.py paperless_zuordnung_pruefen`); ohne Argument Vorschau je Objekt (eindeutig, zweiter Bezug, schwacher Bezug, kein Bezug, ohne Text; lokal eindeutig oder offen), kein KI-Aufruf; `<objekt>` beschränkt, `echt` markiert Eindeutige und löst Nichteindeutige mit der KI auf (bestätigt, umgehängt, duplikat wenn der Inhalt im Zielobjekt schon liegt, Eingang, belassen), `limit=N` begrenzt die Auflösungen je Lauf, `ohne-ki` löst ohne KI, `details` listet die Dokumente; Argumente mit `+` verbinden (`623+echt+limit=50`); wiederholbar, geprüfte Dokumente werden übersprungen. Läuft im worker-io (nur das Worker-Image enthält die Anbieterbibliothek der Stufe 3). Ist die KI technisch nicht verfügbar, wird das Dokument übersprungen und bleibt ungeprüft; nach drei Fehlversuchen in Folge hält der Lauf mit Fehlercode an. Ausnahmen je Dokument zählen als `fehler`, die Zusammenfassung nennt Fehlerarten und bis zu fünf Beispiele | documents, review_cases, ai_calls, Drive-Ordner |
| `zuordnung-stichprobe [je=50+seed=N+objekt=NR+csv=<datei>+ueberschreiben]` oder `zuordnung-stichprobe ergebnis=<pfad>` | Stichprobe der Paperless-Zuordnung als Prüfliste (`manage.py zuordnung_stichprobe`, nur lesend; Vorlage E-3, Nachweis für Abnahmepunkt 3): je Hauptkategorie 01 bis 06 zufällig `je` abgelegte Dokumente mit Paperless-Verknüpfung (Standard 50), `seed=N` macht die Auswahl wiederholbar (ohne Angabe das Tagesdatum), `objekt=NR` beschränkt, `csv=<datei>` schreibt in einen anderen Dateinamen unter `exports/stichproben/`, eine Tagesliste mit Einträgen in `pruefung` wird nur mit `ueberschreiben` ersetzt; CSV `/srv/objektakte/exports/stichproben/<JJJJ-MM-TT>_stichprobe.csv` mit Objekt, Kategorie, Unterordner, Dokumentart, Drive-Ordnerkette, Herkunft des Objektbezugs und leerer Spalte `pruefung`; Konsole nur Zähler und Pfad. Zweite Form `ergebnis=<datei>.csv` (nur Dateiname unter `exports/stichproben/`) wertet die vom Sachbearbeiter ausgefüllte Spalte `pruefung` (richtig, falsch, unklar) aus: Quote je Kategorie und gesamt, IDs mit `falsch`, Markdown-Tabelle `<JJJJ-MM-TT>_ergebnis.md` für docs/betrieb/abgleich-protokoll.md. Läuft im worker-io (Schreibrecht auf `exports`); Ablauf in docs/betrieb/paperless-sync.md 6.2 | exports/stichproben/ |
| `altbestand-aufarbeiten` | Alle Altbestand-Quellordner mit zugeordnetem Objekt nacheinander aufarbeiten (`manage.py altbestand_aufarbeiten`); ohne Argument Vorschau der Ordner und Stand des letzten Sammellaufs, Argument `echt` reiht die Sammelaufarbeitung als Celery-Aufgabe ein (Stand auf der Altbestand-Seite, Protokoll `drive.takeover_all`); bereits übernommene Dateien werden übersprungen, in Drive wird nichts gelöscht | documents, processing_jobs, processing_runs, takeover_sources |
| `verarbeitung-alle` | Verarbeitungsläufe für alle aktiven Objekte mit offener Arbeit einreihen (`manage.py verarbeitung_alle_starten`, Dokumente registered, hashed, ocr_done oder error); ohne Argument Vorschau, Argument `echt` reiht ein, `ohne=133,216` nimmt Objekte aus (Argumente mit `+` verbinden: `echt+ohne=133,216`); die Läufe starten nach `processing.max_parallel_objects` nacheinander, Objekte mit wartendem oder laufendem Lauf werden ausgelassen; Protokoll `processing.start_all` | processing_runs, processing_jobs |
| `db-reset` | Datenverzeichnis der Datenbank leeren und neu initialisieren; bricht ab, sobald `django_migrations` vorhanden oder nicht prüfbar ist | Datenverzeichnis der Datenbank |
| `first-run` | Erstinstallation `scripts/deploy.sh --first-run <branch>` (Images bauen, db und redis starten, Migration, Seeds, Rechte, alle Dienste, Smoke-Test) | alles |
| `deploy` | Deployment `scripts/deploy.sh <branch>` | alles |
| `rollback` | vorherige Version (`scripts/rollback.sh`) | Container |
| `create-admin` | Admin mit der E-Mail aus dem Argument anlegen; Startpasswort nur in `/home/deploy/admin-startpasswort.txt` auf dem Server, nie im Log | Datenbank, eine Datei |

Die Aktionen kennt nur die Fassung von `deploy_remote.sh`, die auf dem Server ausgecheckt ist. Nach dem ersten Checkout deshalb einmal von Hand `git -C /opt/objektakte pull` als deploy; danach genügt die Aktion `pull`.

## Schritt 5: Probelauf

1. Actions, Workflow `Deploy`, Run workflow, Branch `main`, Aktion `deploy`.
2. Erwartung: Das Log zeigt die Schritte von `scripts/deploy.sh` (Dump, Migration, Containerwechsel, Smoke-Test). Auf dem Server steht der Aufruf in `/srv/objektakte/deploy/remote.log`.
3. Rollback-Probe: Aktion `rollback` (ein Befehl, docs/betrieb.md 4.3).

Der Workflow läuft ausschließlich manuell. Ein automatisches Deployment bei jedem Push auf `main` ist bewusst nicht eingerichtet, weil Deployments außerhalb laufender Objektverarbeitung und im Zeitfenster nach F27 stattfinden sollen. Umstellung später durch einen `push`-Auslöser in `deploy.yml`, sobald F27 beantwortet ist.

## Was nicht eingerichtet wird

- Kein Passwort-Login, kein Root-Login (Härtung Nr. 3), kein SFTP-Konto: Dateien werden nicht per SFTP übertragen, der Code kommt aus Git, Daten liegen in Sicherungen (docs/betrieb.md 5).
- Kein Schlüssel im Repository, keine Zugangsdaten in `.env` (Secrets nur in GitHub und in `~deploy/.ssh`).
- Kein Deploy Key mit Schreibrecht: Der Server muss nie nach GitHub pushen.

## Fehlerbilder

| Fehler | Ursache | Maßnahme |
|---|---|---|
| `Host key verification failed` oder `REMOTE HOST IDENTIFICATION HAS CHANGED` | `DEPLOY_KNOWN_HOSTS` fehlt oder enthält einen anderen Schlüssel als der Server | Auf dem Server `ssh-keyscan -t ed25519 127.0.0.1 2>/dev/null \| sed 's/^127.0.0.1/187.124.23.80/'` ausführen, Zeile als Secret ersetzen; Kontrolle: `ssh-keyscan -t ed25519 127.0.0.1 2>/dev/null \| ssh-keygen -lf -` zeigt denselben SHA256-Fingerabdruck wie die Fehlermeldung im Workflow-Log |
| `Permission denied (publickey)` | öffentlicher Teil nicht in `authorized_keys` oder Datei mit falschen Rechten | Zeile prüfen, `chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys` |
| `Nur check, pull, befund, ... erlaubt` | erzwungenes Kommando greift, Eingabe unbekannt | Workflow-Eingaben prüfen; direkter Shell-Zugang mit diesem Schlüssel ist nicht vorgesehen |
| `git fetch` schlägt fehl (`Repository not found`) | Deploy Key nicht eingetragen oder `remote` zeigt auf HTTPS | Schritt 1 wiederholen, `git remote -v` prüfen |
| `Host key verification failed` bei `git fetch` auf dem Server | Der Deploy-Nutzer kennt den Host-Key von github.com nicht; ein Aufruf ohne Terminal kann ihn nicht bestätigen | Die Aktionen `pull`, `deploy` und `first-run` tragen ihn selbst ein, geprüft gegen den von GitHub veröffentlichten Fingerabdruck. Bei einem Checkout mit älterem Skript einmalig: `sudo -u deploy bash -c 'ssh-keyscan -t ed25519 github.com >> ~/.ssh/known_hosts'` |
