# Deployment-Tests T1 bis T14 (Protokoll)

Stand: 26.09.2026 (Werkzeuge je Test ergänzt, Nachweise unverändert). Definition aus Fachentwurf G Abschnitt 12 und Umsetzungsplan 2.17 Schritt 6. Alle Tests laufen auf dem VPS; Ergebnisse mit Datum, Durchführendem, Befehlsausgaben und Bildschirmfotos (ohne Secrets). Nichts wird vorab als bestanden eingetragen; ausstehende Zeilen bleiben offen, bis der Nachweis auf dem Server vorliegt. Die Tests T8 (Migration vorwärts und rückwärts) und die Prüfabfragen werden zusätzlich in der CI beziehungsweise in der Entwicklungsumgebung ausgeführt; das ersetzt den Nachweis auf dem Server nicht.

| Nr. | Test | Erwartung (Kurzfassung) | Vorbereitung im Repository | Werkzeug (Deploy-Aktion) und Prüfablauf | Ergebnis auf dem VPS | Datum, Durchführender | Nachweis |
|---|---|---|---|---|---|---|---|
| T1 | Frischer Checkout | Alle Dienste `healthy` in 3 min, HTTPS mit gültigem Zertifikat, HTTP leitet um | `scripts/deploy.sh`, `docker-compose.yml`, docs/betrieb/deployment.md | `first-run`, `smoke`: Erstinstallation, danach Anwendung und Zertifikat über die Domain prüfen. | **bestanden am 11.09.2026**: alle acht Dienste `healthy` (Anwendungsdienste binnen 11 Sekunden nach dem Start), HTTP leitet global auf HTTPS um (Traefik), HTTPS mit Zertifikatsprüfung antwortet mit 200 (`tls 0`), Zertifikat von Let's Encrypt (Aussteller CN = YR2, Inhaber CN = uebernahme.muellerhv.de, gültig vom 11.09.2026 bis 10.12.2026) | 11.09.2026, Deploy-Workflow | Workflow-Läufe `first-run`, `cert-retry`, `smoke` (Lauf 34555104682), Bereitschaftsabfrage `/readyz/` |
| T2 | Kein Host-Port | Nur Traefik veröffentlicht 80 und 443 | Compose ohne `ports:` an Anwendungsdiensten | `deploy-tests`: veröffentlichte Host-Ports aller Container lesend auflisten. | **bestanden am 11.09.2026**: kein Anwendungscontainer veröffentlicht einen Host-Port (`EXPOSE` ohne Veröffentlichung); auf dem Host nur die zwei fremden Container 32768 und 32771 sowie Traefik im Host-Netz | 11.09.2026, Aktion `deploy-tests` | Lauf 34598173584 |
| T3 | Isolation `data` | Kein Weg ins Internet aus `db` und `redis` | Netz `data` mit `internal: true` | `deploy-tests`: Verbindungsversuch aus `db` und `redis` nach außen, erwartet blockiert. | **bestanden am 11.09.2026**: Verbindungsversuch zu 1.1.1.1:443 aus `db` und `redis` blockiert | 11.09.2026, Aktion `deploy-tests` | Lauf 34598173584 |
| T4 | Serverneustart | Lauf wird fortgesetzt, keine Doppelverarbeitung, Sweeper im Log | Jobs mit Idempotenzschlüssel (B-03), Sweeper beim Start (M5) | `deploy-tests` (Prüfabfragen lesend: Doppelte je `object_id` und `sha256`, vom Sweeper erneut eingereihte Jobs, Sweeper-Logzeilen) und `restart-probe` (ohne `echt` nur Zähler); der Serverneustart selbst bleibt Handarbeit im Zeitfenster F27, danach `deploy-tests` erneut. | ausstehend | | Abfrage, Log |
| T5 | Container-Abbruch Worker | Chunk wird wiederholt, Dokument endet in DONE | Stale-Fristen je Jobtyp, Wiederaufnahme ohne doppelte Seiten (Test in M5) | `restart-probe echt`: nur der Dienst `worker` wird neu gestartet, Jobzähler vorher und nachher sowie Sweeper-Logzeilen seit dem Neustart; anschließend `deploy-tests` für die Prüfabfragen. | ausstehend | | Log, Abfrage |
| T6 | Backup und Wiederherstellung | Zeilenzahlen identisch, Stichproben nach G 7.4, Token-Status `active` | Backup-Container, `restore.sh` | `restore-probe`: jüngste Sicherung in die Scratch-Datenbank `objektakte_restore_probe` einspielen, Zeilenzahlen der Kerntabellen und Token-Status gegen die Produktion, Scratch-Datenbank löschen, Ausgabe als Protokoll nach G 7.4 (Stichproben in der Anwendung von Hand ergänzen); Exit 0 nur, wenn das Einspielen gelang und keine Kerntabelle fehlt, fehlende Kerntabellen stehen im Abschnitt Abweichungen. | ausstehend | | Protokoll G 7.4 |
| T7 | Rollback | Vorherige Version in einer Minute, `tags.log` mit ROLLBACK | `scripts/rollback.sh`, Deploy-Workflow mit Aktion rollback | `rollback`: vorherige Version mit einem Befehl, danach `ps` und `tags.log` prüfen. | ausstehend | | Ausgabe, Bildschirmfoto |
| T8 | Migration vorwärts und rückwärts | Fehlerfrei, `schema_migrations` konsistent | `scripts/check_migrations_roundtrip.sh` (in der Entwicklungsumgebung nach jeder Migration grün) | `migrations-roundtrip`: `check_migrations_roundtrip.sh` im Web-Image gegen die Scratch-Datenbank `objektakte_probe` (angelegt, migriert, zurück, erneut migriert, gelöscht), nie gegen die Produktionsdatenbank. | ausstehend | | Ausgabe |
| T9 | OAuth 8 Tage | Kein `drive.authorize` über 8 Tage, alle Refreshes `ok` | Beat-Tasks Lesetest und täglicher Refresh, `drive_oauth_proof` | `oauth-proof [tage]`: `drive_oauth_proof` lesend, Token-Metadaten und Refresh-Ereignisse ohne Chiffrate, Kriterium mindestens 8 Tage ohne `drive.authorize`. | ausstehend (V-05 bis V-09) | | Export nach G 10.7 |
| T10 | Ressourcenlimits | Kein OOM-Kill, `web` p95 unter 2 s, Seiten pro Minute protokolliert | `scripts/perf_probe.sh`, Latenzsonde | `perf-probe [pages=N+procs=1,2]`: Messcontainer `scripts/perf_probe.sh` mit kleinen Parametern nur im Leerlauf der Verarbeitung, danach OOM-Kill-Status aller Container; die Latenzsonde für `web` p95 bleibt offen (M13). | ausstehend (mit M13) | | Messdatei, Bericht |
| T11 | Logging | JSON je Zeile, keine IBAN in Logs | JSON-Logs mit Maskierung (M1), Prüfabfrage | `deploy-tests`: IBAN-Muster in den letzten 2.000 Logzeilen je Dienst, JSON-Zeilen im Worker-Log. | **bestanden am 11.09.2026**: 0 IBAN-Treffer in 2.000 Zeilen je Dienst; Anwendungsmeldungen von Web und Worker als JSON je Zeile (erster Lauf zeigte Worker-Text, weil Celery den Root-Logger übernahm; behoben mit `CELERY_WORKER_HIJACK_ROOT_LOGGER = False`, Nachprüfung im zweiten Lauf) | 11.09.2026, Aktion `deploy-tests` | Läufe 34598173584, 34598772657 |
| T12 | Sicherheitseinstellungen Host | Root- und Passwort-Login aus, UFW 22, 80, 443, unattended-upgrades, Zeitzone Europe/Berlin | docs/betrieb.md Abschnitt 1.4 | `deploy-tests`: `PermitRootLogin` und `PasswordAuthentication` aus `sshd -T` (ohne passwortloses sudo aus `sshd_config` und `sshd_config.d`), `ufw status verbose` (nur mit sudo), `unattended-upgrades` aktiv, `20auto-upgrades`, Zeitzone und NTP aus `timedatectl`. | nicht bestanden, Abweichung vom Betreiber übernommen: Befund `deploy-tests` 26.09.2026 16:28 Uhr, `sshd_config` und `sshd_config.d/0-mhvp-password.conf` setzen `PermitRootLogin yes` und `PasswordAuthentication yes`; Entscheidung des Vorstands vom 26.09.2026, die SSH-Konfiguration unverändert zu lassen (docs/plan/status.md). Unattended-upgrades, 20auto-upgrades und Zeitzone Europe/Berlin mit NTP in Ordnung; UFW nur mit sudo prüfbar, Nachweis offen | 26.09.2026 | Ausgabe `deploy-tests`, Entscheidung Vorstand |
| T13 | Login und Rollen | TOTP-Einrichtung erzwungen, Zugriff verweigert mit `auth.denied` | Login mit TOTP-Pflicht, Rollen (M1) | `deploy-tests`: Pflichtrollen aus `security.mfa_required_roles`, Nutzer je Rolle (Anzahl, aktiv, mit TOTP, ohne Namen), `auth.denied` der letzten 30 Tage nach Entität und Recht; die Umleitung auf die Einrichtungsseite ist zusätzlich im Browser zu belegen. | ausstehend | | Bildschirmfoto, Abfrage |
| T14 | Healthcheck-Endpunkte | `/healthz/` nur `ok`, `/readyz/` ohne Token 401 | Endpunkte (M1) | `deploy-tests`, `smoke`: `/healthz/` und `/readyz/` ohne Token über die Domain. | **bestanden am 11.09.2026**: `/healthz/` 200 mit Inhalt `ok`, `/readyz/` ohne Token 401, mit Token vollständiger Befund | 11.09.2026, Aktionen `smoke` und `deploy-tests` | Läufe 34595946648, 34598173584 |

## Prüfabfragen

```sql
-- T4, T5: keine Doppelverarbeitung
SELECT sha256, COUNT(*) FROM documents WHERE deleted_at IS NULL GROUP BY object_id, sha256 HAVING COUNT(*) > 1;
-- T11: IBAN in Logs
docker compose logs | grep -E 'DE[0-9]{2}[0-9 ]{18,}'
```

## Abschluss

Definition of Done für den Betriebsteil: T1 bis T14 bestanden und dokumentiert, Serverbefund abgelegt, Runbook vorhanden, Wiederherstellungsprotokoll vorhanden. Stand am 11.09.2026: T1, T2, T3, T11, T14 bestanden, T4 bis T10, T12, T13 ausstehend; Serverbefund abgelegt (docs/betrieb/serverbefund.md), Runbook vorhanden (docs/betrieb.md), Wiederherstellungsprotokoll ausstehend (T6).

Stand am 26.09.2026: unverändert T1, T2, T3, T11, T14 bestanden; T4 bis T10, T12, T13 bleiben ausstehend, bis der Nachweis vom Server vorliegt (Lauf-ID, Datum, Durchführender). Neu sind die Werkzeuge je Test (Spalte Werkzeug): `deploy-tests` deckt jetzt zusätzlich die Prüfabfragen zu T4/T5 sowie T12 und T13 lesend ab; `restart-probe`, `restore-probe`, `migrations-roundtrip`, `oauth-proof` und `perf-probe` sind eigene Deploy-Aktionen (docs/betrieb/github-deploy.md). `restore-probe` setzt ein Deployment mit dem neuen Backup-Image voraus (`restore_probe.sh`), `migrations-roundtrip` und `restore-probe` brauchen die Rechte auf die Scratch-Datenbanken aus `grants_sql` (werden bei jedem Deploy gesetzt, `migrations-roundtrip` setzt sie zusätzlich selbst). Kein Test wird durch das Werkzeug allein als bestanden gewertet.
